# Same-package contract fixtures for market-storefront boundary messages.
#
# These are importable as ``tests.fixtures.*`` from any test file within
# ``domains/vms/storefront/`` because pytest adds the package root to
# sys.path.
#
# For cross-package boundaries (e.g. storefront-client HTTP responses),
# see ``core/storefront-client/fixtures/`` instead.
