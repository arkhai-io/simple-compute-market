# Development inventory for the bare-metal end-to-end lane: deliberately empty.
# The lane's site runs the provisioning mock profile, which never opens a
# connection to a host, and publication dispatches no job. Not for any real site.
