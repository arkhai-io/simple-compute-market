# Development placeholder, not a key. The bare-metal stack requires an SSH
# private key file for its executor; the lane's site runs the provisioning mock
# profile, which never opens an SSH connection.
