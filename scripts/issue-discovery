#!/usr/bin/env bash
set -Eeuo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
TOOL_DIR="$ROOT_DIR/tools/issue-discovery"

exec uv run --no-config --project "$TOOL_DIR" --directory "$TOOL_DIR" issue-discovery \
  --repo-root "$ROOT_DIR" \
  "$@"
