# Compute Provisioning Migration Moved to OpenSpec

The remaining ownership moves, composition cleanup, and executor migration are tracked by [`migrate-compute-provisioning`](../../openspec/changes/migrate-compute-provisioning/).

Current scheduling, fulfillment, job, and lease contracts are in the [physical provisioning specification](../../openspec/specs/physical-provisioning/spec.md). Shared authority and reservation contracts are in the [site capacity specification](../../openspec/specs/site-capacity/spec.md).

The original heading-level disposition and landed-versus-pending classification remain in the `migrate-planning-to-openspec` migration ledger. This redirect is non-normative and must not accumulate new tasks.
