{{/*
E2E signing credentials are operator-managed. Keeping this template as a
fail-closed guard prevents a values override from reintroducing signing
material into chart-rendered manifests.
*/}}
{{- if .Values.createSecret }}
{{- fail "e2e-tests.createSecret is unsupported; use e2e-tests.secretName" }}
{{- end }}
