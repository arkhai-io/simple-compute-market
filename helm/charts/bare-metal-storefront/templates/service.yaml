apiVersion: v1
kind: Service
metadata:
  name: {{ include "bare-metal-storefront.fullname" . }}
  labels:
    {{- include "bare-metal-storefront.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "bare-metal-storefront.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
