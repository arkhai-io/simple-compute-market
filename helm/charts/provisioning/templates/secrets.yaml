{{/*
Provisioning credentials are operator-managed. Fail closed if a legacy values
override attempts to render SSH, decryption, inventory, or callback secrets.
*/}}
{{- if or .Values.sshKey.createSecret .Values.sshDecryptionKey.createSecret }}
{{- fail "provisioning chart-managed Secrets are unsupported; reference pre-existing sshKey.secretName and sshDecryptionKey.secretName" }}
{{- end }}
