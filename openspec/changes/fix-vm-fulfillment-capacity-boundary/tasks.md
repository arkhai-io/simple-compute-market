## 1. Corrupted GPU-attachment-discovery shell logic

- [ ] 1.1 Restore `[ -z "$pci_addr" ] && continue` at `domains/vms/provisioning/iac/ansible/roles/vm-management/tasks/vm-create.yml` line 403 (currently `continueThen remove all task that contains installation of GPU driver that is not`). Apply the identical fix to the unreferenced duplicate at `roles/vm-management/backup/original-main.yml` line 451 for repository hygiene; confirm it is still unreferenced (`grep -rl "original-main"`) before touching it, since if anything now imports it that changes its priority.
- [ ] 1.2 Add a shell-logic test harness to `domains/vms/provisioning/iac/tests`: a helper that (a) extracts a named task's literal shell-script content from a task YAML file (this block has no Jinja templating; confirm that remains true or extend the helper to accept pre-rendered variable substitutions if a future case needs it), (b) writes fake `virsh`/`lspci` executables into a temp directory as `#!/bin/sh` scripts returning fixture-controlled output, (c) runs the extracted script via `subprocess.run(["bash", "-c", script], env={**os.environ, "PATH": f"{fake_bin_dir}:{os.environ['PATH']}"})`, (d) exposes the script's result (stdout, or a value it writes to a file/env var for the test to read back) to the caller. Keep this reusable — a second shell-logic bug elsewhere should not need its own harness.
- [ ] 1.3 Add a test using the harness that feeds the GPU-attachment-discovery loop a `virsh dumpxml`-shaped fixture containing at least one empty `pci_addr` entry among real ones, asserting the empty entry is skipped and does not appear (garbled or otherwise) in the resulting `ATTACHED_GPUS_LIST`. This is the regression test for the corrupted line — it must fail against the pre-fix text and pass after.
- [ ] 1.4 Confirm the existing substring-assertion tests in `test_vm_management_contracts.py` still pass; they are not being replaced, only supplemented — they still catch structural/ordering regressions the harness doesn't (and vice versa).

## 2. Reservation-commit boundary cleanup

- [ ] 2.1 Make `CommitRequest.resource_id` optional (`str | None = None`) in `kit/site/src/market_site/http_models.py`. Add a docstring/comment explaining why: `CapacityLedgerService.commit()` already ignores this value whenever `capacity_reservation_id` is supplied (true for every current caller, since the `/commit` endpoint takes `capacity_reservation_id` as a mandatory path parameter, not from the body), so requiring it here over-constrains the wire contract relative to what the method behind it needs.
- [ ] 2.2 Document, at `ledger.commit()`'s `resource_id` parameter and at `_find_reservation`'s `resource_id`-only lookup branch (`kit/site/src/market_site/ledger.py`), what caller shape would actually exercise resource_id-only lookup: a caller with a `resource_id` but no `capacity_reservation_id` — not the current resource-pinned-listing case, which still uses `capacity_reservation_id` normally. No current caller does this; this is forward documentation, not a functional change.
- [ ] 2.3 Remove `fulfill_vm_obligation`'s `if not reserved_vm_host: raise RuntimeError(...)` guard in `domains/vms/storefront/src/market_storefront/services/vm_fulfillment_service.py`. Keep `vm_host`/`resource_id` flowing into `stage_event(...)` calls as best-effort telemetry (already their only other use here); do not add a replacement guard elsewhere.
- [ ] 2.4 Stop `str(reserved.get("resource_id"))` from producing the literal string `"None"`. Either stop persisting `settlement_resource_id` at the `fulfillment_phase="capacity_reserved"` write in `fulfill_vm_obligation` (deferring entirely to the correct post-`schedule_resource()` write `_do_provision` already performs), or persist an actual `None`/omit the field if an early informational write is still wanted. Same fix applies to `_commit_capacity_hold`'s and `_commit_fresh_reservation`'s equivalent `str(...get("resource_id"))` conversions if they have the same pattern — audit both before closing this task.
- [ ] 2.5 Remove `_commit_fresh_reservation`'s `if not capacity_reservation_id or not resource_id: raise RuntimeError("Reserved capacity is missing reservation identity")` guard's dependence on `resource_id` specifically (keep the `capacity_reservation_id` check — that one is genuinely required). Confirm nothing else in `_reserve_capacity_for_obligation`'s call chain silently depends on `resource_id` being non-None between this point and `schedule_resource()`.
- [ ] 2.6 Add an integration test exercising the real `RemoteCapacityClient` → `kit/site` HTTP router boundary (real ASGI app, not an in-process fake) for a full VM obligation fulfillment — reserve, commit, `schedule_resource()`, `begin_fulfillment()` — asserting it completes without ever requiring `resource_id`/`vm_host` from the reservation response at any step. This is the test that would have caught the original defect; an in-process fake that doesn't enforce the wire contract's field-stripping proves nothing here.
- [ ] 2.7 Add a boundary-contract test on `kit/site`'s `/reservations` endpoint response shape directly: assert it never contains `resource_id`, `capacity_bucket_id`, or `backing_resource_id`, independent of any specific caller. Protects every future caller, not just this one.
- [ ] 2.8 Add a regression test for 2.4: interrupt fulfillment between the capacity-reserved persist and `schedule_resource()` (e.g. raise from a patched `schedule_resource`), assert the persisted `settlement_resource_id` is never the literal string `"None"`.
- [ ] 2.9 Unit test `ledger.commit()` directly: a commit with `resource_id=None`/omitted succeeds identically to one with it supplied, given `capacity_reservation_id` is present.

## 3. Derive VM shape from committed reservation dimensions

- [ ] 3.1 Confirm `PhysicalSettlementScheduler.schedule_resource()`'s reservation payload (via `tx.reservation_payload(reservation_row)`) exposes `CapacityReservationDebit.dimensions` in a form usable downstream; if not already exposed on `SettlementResource` or an adjacent type, add it as a domain-neutral field (dimensions are opaque to `kit/fulfillment`, which must not know these are VM-specific) rather than inventing a VM-specific field on a generic type.
- [ ] 3.2 Thread the reservation's committed dimensions from `schedule_resource()`'s output through to wherever `AnsibleFulfillmentProvider` builds `VmFulfillmentRequirements` (currently in `domains/vms/provisioning/adapter`). Confirm during implementation whether this is cleaner as (a) a field on the type already flowing from scheduling into `prepare_create`, or (b) a direct reservation-dimensions lookup the provider performs itself given its existing ledger access — design.md leaves this open at the task level.
- [ ] 3.3 Add the canonical VM compute-requirement field-name mapping (`gpu_count`/`vcpu_count`/`ram_gb`/`disk_gb` ↔ `vm_gpu_count`/`gpu_provisioned`/`vm_vcpus`/`vm_ram`/`vm_disk_size`) to `arkhai_vms` (`domains/vms/domain`), consumed by both `vm_job_spec_service.compute_capacity_claim_from_order` (storefront side, unchanged behavior, just sourcing field names from the shared module) and the new dimension-to-requirements derivation added in 3.2 (provisioning side). Do not leave two independently-maintained copies of this mapping.
- [ ] 3.4 Remove the now-unnecessary storefront-side transmission path: confirm `fulfillment_service.py`'s `request_payload` construction does not need to change at all (it already doesn't send shape fields — the fix is entirely on the provisioning-derivation side), and that no other caller expects the fulfillment request to carry shape fields the storefront would now be expected to send.
- [ ] 3.5 Add an end-to-end contract test proving a GPU=1 listing's fulfillment request reaches the provider as `gpu_provisioned=true`, `vm_gpu_count=1`, with the correct CPU/RAM/disk values, sourced from the reservation's committed dimensions — not from any value the storefront sent. Include a case with `gpu_count=0`/no GPU declared, confirming `gpu_provisioned` comes through `false`/unset rather than leaking a stale default.
- [ ] 3.6 Add a code comment at the `resize_reservation` definition (`kit/site/src/market_site/ledger.py`) recording the invariant this decision depends on: any future caller must resize before the corresponding `schedule_resource()` call, not after, or fulfillment-time shape derivation silently goes stale. Point to this change's `design.md` for the full reasoning.

## 4. Closeout

- [ ] 4.1 Run the full existing suite for every package touched (`kit/site`, `domains/vms/storefront`, `domains/vms/provisioning/adapter`, `domains/vms/provisioning/iac`) together, not just per-package, before considering this change done.
- [ ] 4.2 Promote the accepted invariant from Decision 4 ("VM shape requirements are derived from the committed reservation, not re-transmitted") into `openspec/specs/site-capacity/spec.md` and/or `openspec/specs/physical-provisioning/spec.md` as a normative requirement once 3.1–3.6 are implemented and validated — this is current-system behavior at that point, not forward-looking, and belongs in permanent documentation per `openspec/README.md`'s placement rules. The forward-looking direct-resource-path notes in `design.md` stay in the change document; they do not get promoted until that path is actually built.
- [ ] 4.3 Confirm no promotion is needed for Decisions 1–3 (`resource_id`/`vm_host` cleanup) beyond the in-code documentation added in 2.1–2.2 — these are bug fixes restoring already-documented intent (`site-capacity/spec.md` already states the opaque-reservation principle these fixes satisfy), not new normative behavior.
