## 1. Prerequisite acceptance and interface lock

- [ ] 1.1 Verify `storefront-domain-parameterization` is implemented, accepted, and promoted: `build_vm_storefront_app` passes one exact validated `compute.v1` contract through `domains/vms/storefront/src/market_storefront/{server.py,startup.py,container.py,utils/sqlite_client.py,services/listing_service.py,controllers/negotiate_controller.py,utils/sync_negotiation.py,settlement_composition.py}` with no lower-layer default/getter/module lookup. Record the exact permanent `market-composition`/`storefront-publication` headings and focused evidence in `design.md`; stop before implementation if the seam is absent.
- [ ] 1.2 Verify `pool-declared-offering-modes` is implemented, accepted, and promoted with the canonical pool declaration reader, explicit requested mode, shared reservation/scheduling/provisioning predicate, withdrawn-mode behavior, legacy reservation migration, and removal of every implicit VM executor fallback. Record the exact `resource-pool-management`, `site-capacity`, `fulfillment`, and physical-provisioning interfaces/evidence in `design.md`; stop before implementation if enforcement is incomplete.
- [ ] 1.3 Verify the production bare-metal hooks and selected-site lifecycle needed for full evidence are accepted in `market-platform-bare-metal-10-storefront-composition` and `pools-7-storefront-fulfillment-cutover`: publication, negotiation, settlement plan, scheduling, begin/status/result, restart recovery, teardown, and capacity restoration. Record exact package/client models and evidence; block Sections 6, 7, and the full proof rather than substituting a no-op, VM adapter, route-internal fake, or success flag.
- [ ] 1.4 Reconcile `publish-multidimensional-listing-shape` against the live canonical `offer_resource.virtualization_type` schema. Record which landed builder/fixture owns capacity dimensions, reuse it if present, and keep this change as the sole owner of discriminator equality/routing without adding another public field.
- [ ] 1.5 Inventory the live listing, derived mapping, negotiation, escrow/settlement, fulfillment-context, result, and teardown record shapes plus every `get_market_domain_contract`, `app.state.market_domain`, `executor_kind or "vm"`, cold-cache fan-out, and VM/bare-metal route branch. Update `design.md` if the accepted interfaces or source destinations differ before editing production files.

## 2. Frozen registry and shared application composition

- [ ] 2.1 Add immutable `DomainContractKey`, `StorefrontDomainRegistration`, `StorefrontDomainBinding`, and `StorefrontDomainRegistry` in `core/storefront/src/core_storefront/domain_registry.py`; validate through `market_core.validate_domain_contracts`, require the full storefront capability set, reject duplicate modes/contributions/identities and unsupported versions, and resolve only to the exact pre-registered object.
- [ ] 2.2 Add storefront contribution discovery in `core/storefront/src/core_storefront/domain_plugins.py` and package exports/entry-point fixtures. Keep it distinct from concept-level `market.storefront_domains`, verify configured mode/identity/version assertions against each returned complete contract, and load all contributions only during startup.
- [ ] 2.3 Change `core/storefront/src/core_storefront/app_composition.py` to build the shared shell from the frozen registry and an injected runtime resolver; replace `app.state.market_domain` with a safe immutable registration projection and do not expose a request-time application-global selection API.
- [ ] 2.4 Replace the one-contract parameterization across `domains/vms/storefront/src/market_storefront/{server.py,startup.py,container.py,domain_runtime.py,utils/sqlite_client.py,services/listing_service.py,controllers/negotiate_controller.py,utils/sync_negotiation.py,settlement_composition.py}` with registry/binding injection while preserving the exact object path established by the prerequisite.
- [ ] 2.5 Add core focused tests in `core/storefront/tests/unit/{test_domain_registry.py,test_app_composition.py,test_domain_plugins.py}` for two valid registrations, one explicit registration, duplicate mode/identity/contribution, unsupported/incomplete contract, frozen lookup, order independence, absent contribution, and proof that no contract is reconstructed from binding strings.
- [ ] 2.6 Extend architecture-import tests in `domains/vms/storefront/tests/unit/test_architecture_imports.py` and the core package suite so core and kit import no VM/bare-metal implementation and neither domain contribution imports the other domain's composition.

## 3. Immutable persistence and transactional legacy migration

- [ ] 3.1 Add ordered schema migrations in `core/storefront/src/core_storefront/sqlite_migrations.py` for `storefront_listing_bindings`, `storefront_domain_artifacts`, negotiation binding/site columns, indexes, foreign-key/equivalence checks, and database triggers that reject changes to offering mode, domain identity/version, or site after first persistence.
- [ ] 3.2 Implement typed common binding/mapping repository methods in `core/storefront/src/core_storefront/sqlite_client.py`: atomic listing+binding upsert, exact load, collision-safe derivation lookup, thread binding copy/load, binding-set inventory, and fulfillment-context equivalence. Identical retries are idempotent; any changed binding fails without partial state.
- [ ] 3.3 Implement common domain-artifact persistence in `core/storefront/src/core_storefront/sqlite_client.py`, keyed by negotiation/artifact slot and validated against the thread binding before invoking the selected `message`, `terms`, `materialization`, `receipt`, or `result` codec. Do not duplicate settlement plans or provider-owned fulfillment results.
- [ ] 3.4 Replace the module-global legacy `set_accepted_escrows_synthesizer` path in `core/storefront/src/core_storefront/sqlite_migrations.py` with explicit migration inputs selected from the frozen legacy contribution; prove two databases/processes cannot contaminate each other's migration codec.
- [ ] 3.5 Add `domains/vms/storefront/src/market_storefront/domain_migration.py` and wire `market-storefront config migrate --scope storefront-domains --legacy-contribution/--legacy-offering-mode/--legacy-domain/--legacy-contract-version --check|--write --backup` through `cli.py`. Follow the repository's restrictive backup, fsync, atomic replacement, redacted preview, and unresolved-manifest conventions.
- [ ] 3.6 In `domains/vms/storefront/src/market_storefront/utils/migrations.py` and `domains/vms/listings/reconciler.py`, migrate valid `derived_compute_listings` rows to the common table with site/pool/resource/slice source envelopes and collision-safe keys; retire that table as a writable authority only after whole-population equivalence succeeds.
- [ ] 3.7 In `domains/bare_metal/storefront/src/arkhai_bare_metal_storefront/{migrations.py,sqlite_client.py}`, migrate valid `derived_bare_metal_listings` and `bare_metal_agreement_payloads` to common bindings/artifacts with exact site, Physical Resource, machine, and physical-host identity; explicitly reject the VM database's incomplete bare-metal table rather than fabricating missing provenance.
- [ ] 3.8 Backfill listing/thread/site/fulfillment context only from the explicitly selected legacy contribution, preserve listing/negotiation/plan/obligation/reservation/fulfillment/operation identities and opaque provider state, and abort on missing source input, mixed kinds, orphan relationships, duplicate derivation identity, unsupported version, or binding disagreement.
- [ ] 3.9 Add fresh bootstrap, valid VM source, valid bare-metal source, populated accepted/recoverable state, terminal history, missing site, ambiguous/mixed row, codec mismatch, injected mid-transaction failure, backup restoration, idempotent rerun, and schema-drift tests in `core/storefront/tests/unit/test_domain_binding_migrations.py`, `domains/vms/storefront/tests/unit/test_migrations.py`, and `domains/bare_metal/storefront/tests/test_migrations.py`.
- [ ] 3.10 Add repository tests for direct SQL mutation rejection, identical/conflicting upserts, delimiter collisions, thread/artifact/fulfillment-context disagreement, large existing identifiers, and migration canaries proving payloads and secrets do not enter preview/output or binding columns.

## 4. Multi-domain publication and trusted listing mapping

- [ ] 4.1 Update VM and bare-metal complete contract contributions in `domains/vms/storefront/src/market_storefront/domain_runtime.py` and `domains/bare_metal/storefront/src/arkhai_bare_metal_storefront/domain_runtime.py` to expose publication sources through the same registration-owned interface and return immutable contracts with no runtime singleton lookup.
- [ ] 4.2 Refactor `core/storefront/src/core_storefront/{publication_runner.py,publication_plugins.py}` and `domains/vms/storefront/src/market_storefront/{publication_wiring.py,services/publication_service.py,cli_publish.py}` to iterate frozen registrations and persist every candidate through the common listing binding/mapping repository; remove mode-specific mapping lookup branches from shared publication control flow.
- [ ] 4.3 Read the accepted pool deliverable-mode projection in the publication path and require the exact registration mode before opening/reopening a candidate. An absent/empty/withdrawn declaration publishes nothing for that mode and closes only its affected new-work listing without touching accepted records or sibling modes.
- [ ] 4.4 Update `domains/vms/domain/src/arkhai_vms/storefront_adapter.py` and `domains/bare_metal/storefront/src/arkhai_bare_metal_storefront/publication.py` so public `offer_resource.virtualization_type` is projected from the durable binding, domain listing normalization must agree, and VM/bare-metal candidates from one pool have distinct derivation/listing identities.
- [ ] 4.5 Replace `site_id_for_listing`/`pool_id_for_listing` domain-table authority with shared binding lookups and preserve site/pool/resource/source identity as private trusted state; no URL, authority credential, provider config, SSH material, or buyer assertion enters the public offer or common source envelope.
- [ ] 4.6 Add publication/reconciliation tests in `core/storefront/tests/unit/test_publication_runner.py`, `domains/vms/storefront/tests/unit/{test_publication_wiring.py,test_reconciler.py,test_domain_runtime_wiring.py}`, and bare-metal storefront tests for both modes on one pool, one/zero declared mode, withdrawal, identical site-local IDs, delimiter safety, conflicting public mode, changed contract version, identical replay, and per-mode close/reopen isolation.
- [ ] 4.7 Extend the registry publication contract fixture and integration coverage only at the existing producer/consumer fixture boundary so a real published VM and bare-metal listing each carry the exact canonical `virtualization_type` while registry filtering retains its existing fail-closed semantics.

## 5. Record-bound negotiation and acceptance

- [ ] 5.1 Extend `core/storefront/src/core_storefront/negotiation_sync.py`, `services/negotiation_service.py`, and `sqlite_client.py` so new negotiation performs listing/binding load, frozen-registry resolution, provision-envelope kind/version validation, thread/message/artifact insertion, and site/binding copy in one transaction before invoking policy.
- [ ] 5.2 Refactor `domains/vms/storefront/src/market_storefront/controllers/negotiate_controller.py` and `utils/sync_negotiation.py` into schema-opaque record selection: start resolves from listing; continuation resolves from thread; neither request kind, current listing config, installed-contract order, nor one-element registry selects a domain.
- [ ] 5.3 Adapt `domains/bare_metal/storefront/src/arkhai_bare_metal_storefront/{negotiation_service.py,negotiation.py}` to the same shared orchestration and hook signature, retaining bare-metal SSH/access and trusted-listing validation only inside the selected contract.
- [ ] 5.4 Route canonical Terms reduction, acceptance, and settlement-plan construction through the thread's recorded contract in `domains/vms/storefront/src/market_storefront/{utils/sync_negotiation.py,settlement_composition.py}` and the bare-metal contribution; reject any copied binding or artifact mismatch before changing terminal/agreed state.
- [ ] 5.5 Remove duplicate VM/bare-metal new/continue persistence and handler branches after every caller uses the shared path. Do not leave a compatibility endpoint that selects a domain by URL prefix, app instance, payload kind, or module getter.
- [ ] 5.6 Add focused/integration tests for VM→VM and bare-metal→bare-metal openings, both cross-swaps, unknown mode/domain/version, listing-binding/public-field disagreement, failure-atomic opening, continuation mismatch, listing/config change during an active thread, accepted restart, conflicting binding update, and proof that no unselected policy/hook/capacity call occurs.
- [ ] 5.7 Update canonical storefront-client fixtures in `core/storefront-client/src/storefront_client/fixtures/` and producer/consumer integration tests only if observable generic responses gain a safe binding projection; keep versioned provision envelopes unchanged and reject flat/alternate discriminator forms.

## 6. Settlement, capacity, fulfillment, result, recovery, and teardown

- [ ] 6.1 Replace VM-global settlement and fulfillment contract access in `domains/vms/storefront/src/market_storefront/settlement_composition.py` and `services/fulfillment_service.py` with accepted-thread binding resolution; adapt bare-metal `settlement_service.py`/`settlement.py` to the same shared plan/materialization flow without importing VM services.
- [ ] 6.2 Persist and compare the safe binding/site projection in escrow/settlement fulfillment context before physical dispatch. Every status/recovery worker loads `negotiation_id`, exact binding, site, reservation, fulfillment, and operation identity and fails on disagreement without touching a remote authority.
- [ ] 6.3 Pass the recorded offering mode as the explicit requested mode from `domains/vms/storefront/src/market_storefront/{utils/sync_negotiation.py,services/fulfillment_service.py,services/fulfillment_resume_runtime.py}` and the bare-metal hook into the accepted site/fulfillment ports; remove all request-side mode inference.
- [ ] 6.4 Extend `AggregateCapacityClient` in `core/storefront/src/core_storefront/aggregation.py` and `AggregateFulfillmentClient` in `domains/vms/storefront/src/market_storefront/services/capacity_client.py` with exact `site_id` routing for commit/release/truncate/schedule/begin/status/result/begin-teardown. Targeted calls verify the configured authority and never use `_route_order` or another site after refusal/error.
- [ ] 6.5 Schedule and begin fulfillment with the existing generic market namespace plus the selected contract's versioned fulfillment envelope; verify the provisioning authority preserves the explicit pool mode/executor identity and rejects undeclared/mismatched mode without adapter work.
- [ ] 6.6 Decode active `domain_result` only with the accepted contract's result codec, keep live access/credential material in its existing protected result channel, and surface a mismatched/unknown result kind as data-integrity failure rather than selecting another contract.
- [ ] 6.7 Route teardown and capacity release by the recorded site and fulfillment/reservation identities. Let the provisioning authority dispatch its recorded executor kind; never derive teardown executor from current publication mode, result kind, pool configuration, or a VM default.
- [ ] 6.8 Update timer/restart paths in `domains/vms/storefront/src/market_storefront/services/fulfillment_resume_runtime.py`, settlement workers, and bare-metal runtime adapters so missing contract/site trust blocks the exact record, equivalent retry retains one operation, and terminal history can remain opaque without enabling a fallback decoder.
- [ ] 6.9 Remove every remaining VM singleton/default, `executor_kind or "vm"`, request-time contract loader, cold-cache accepted-record fan-out, and VM-versus-bare-metal lifecycle branch. Add static inventory assertions in the affected architecture tests without testing source text as the sole behavioral proof.
- [ ] 6.10 Add deterministic tests at the lowest owning level for exact mode at all three pool enforcement layers, selected-site zero-call isolation, restart with cold caches, held-mode withdrawal, result cross-swap, status/result no-mutation reads, failure before/after durable acceptance, duplicate retry, VM failure beside live bare metal and inverse, repeated teardown, and restored capacity.

## 7. Domain contributions and clean route cutover

- [ ] 7.1 Declare full VM and bare-metal storefront-contribution entry points and exact runtime dependencies in `domains/vms/storefront/pyproject.toml` and `domains/bare_metal/storefront/pyproject.toml`; keep concept-level `market.storefront_domains` exports unchanged for independent domain conformance.
- [ ] 7.2 Make the bare-metal publication, policy, settlement, fulfillment, receipt/result, and teardown contribution satisfy the shared hook signatures using `domains/bare_metal/storefront/src/arkhai_bare_metal_storefront/{domain_runtime.py,publication.py,negotiation.py,settlement.py}` and accepted POOLS-7 clients; do not add provider behavior or VM-shaped carriers.
- [ ] 7.3 Reduce `domains/bare_metal/storefront/src/arkhai_bare_metal_storefront/{server.py,runtime.py,api.py,sqlite_client.py,negotiation_service.py,settlement_service.py}` to contribution adapters or a one-registration invocation of the shared app, then remove obsolete parallel route/persistence code and tests only after their observable contracts run through the common shell.
- [ ] 7.4 Preserve VM external behavior through the common route set in all existing VM listing, negotiation, settlement, fulfillment, operator, and recovery integration suites; any VM-only field stays inside VM codecs/hooks and never becomes a shared branch.
- [ ] 7.5 Add one shared domain conformance matrix for listing/message/Terms/materialization/receipt/result plus storefront capability completeness under both exact contracts; domain-specific cases add only schema-owned validation.

## 8. Configuration, packages, image, Compose, and Helm

- [ ] 8.1 Add versioned `[StorefrontDomains]` parsing, generated template/reference output, edit validation, and safe status projection in `domains/vms/storefront/src/market_storefront/utils/config.py`, CLI config helpers, settings/examples, and unit tests. Require non-empty explicit registrations and reject duplicates, unknown fields, secret/provider data, and config/entry-point assertion mismatch.
- [ ] 8.2 Update `core/storefront/pyproject.toml`, both storefront `pyproject.toml`/lock inputs, `domains/Makefile`, root `Makefile`, and both storefront Makefiles so `dist-arkhai-core-storefront`, VM/bare-metal domain contributions, and the combined storefront build/reinit exclusively from `.dist` wheels; add a first-class `dist-bare-metal-storefront` target and no editable sibling source.
- [ ] 8.3 Update `domains/vms/storefront/Dockerfile` and build/package review inputs so one staged image contains the shared shell and exact enabled VM/bare-metal contribution wheels, starts the one common command, and contains no retired duplicate app or private configuration.
- [ ] 8.4 Update `compose.vms.yml`, relevant VM/bare-metal Compose profiles and generated configs to run one compute-family storefront process/database with explicit registrations and trusted sites; disabling one domain must remove its package/config/waits without selecting the other as a default.
- [ ] 8.5 Update `helm/charts/storefront/{values.yaml,values.schema.json,templates/configmap.yaml,templates/deployment.yaml}`, parent `helm/{values.yaml,values.schema.json}`, and render fixtures to emit explicit public registrations, one RWO/Recreate SQLite volume, exact package/image pins, trusted public site bindings, migration/preflight configuration, and Secret-only credentials.
- [ ] 8.6 Add configuration/render tests for one domain, both domains, duplicates, absent contribution, unsupported version, missing trusted site, migration-required database, persistent restart, disabled-domain cleanup, and canaries proving signer/provider/SSH/result secrets are absent from ConfigMaps, arguments, image layers, status, and migration reports.
- [ ] 8.7 Implement activation preflight and operator sequence: stage artifacts, quiesce publication/settlement/recovery, run domain migration `--check` then `--write --backup`, compare migrated binding set to frozen registrations and pool/site readiness, start one process, and resume effects only after readiness. Reject live database merge and require forward recovery after any new effect.

## 9. Behavioral, integration, packaging, and strict validation

- [ ] 9.1 Run the core registry/persistence/migration focused suite with `make -C core/storefront test`; report any unrun test rather than narrowing silently.
- [ ] 9.2 Build current internal wheels, then run `make -C domains/vms/storefront test-unit`, `make -C domains/vms/storefront test-integration`, and `make -C domains/bare_metal/storefront test` so both one-registration regressions and shared two-registration routes use installed artifacts.
- [ ] 9.3 Run provisioning/site/resource-pool/fulfillment focused suites that own explicit mode enforcement, selected-site scheduling, result, teardown, and capacity restoration; include migration restart and no-default executor cases from the accepted prerequisites.
- [ ] 9.4 Add and run `e2e-tests/tests/e2e/roles/scenarios/compute_family/{conftest.py,test_multi_domain_storefront.py}` plus the exact `DealState` producer/consumer fields: one process publishes, negotiates, settles, reserves, fulfills, observes real VM and bare-metal domain results, restarts, tears both down, and observes capacity restoration; include payload/result/listing/settlement/teardown cross-swaps and independent failure. Reuse canonical typed clients and explicit synchronization, not sleeps or raw route calls.
- [ ] 9.5 Keep Compute-40's later two-authority topology out of this scenario, but run its prerequisite/contract fixture if available to prove this change exports the exact multi-domain storefront it expects; disclose the separate topology suite as not owned here.
- [ ] 9.6 Run `make dist-arkhai-core-storefront dist-domains`, clean wheelhouse installation/startup, wheel-content and dependency review, storefront image startup/health, `make test-deployment-packaging`, Helm one/two-domain renders, Compose config rendering, and affected configured typing checks. Verify no editable path, source mount, duplicate server, or disabled-domain wait remains.
- [ ] 9.7 Run the complete VM and bare-metal contract/canonical-client integration suites and the repository smoke path from staged artifacts. Attribute every failure to this change, a prerequisite, environment, or an unrelated baseline failure with the exact unrun/failed command.
- [ ] 9.8 Cross-check `proposal.md`'s six modified capabilities against the six delta-spec directories and every requirement heading/scenario; then run `openspec validate multi-domain-storefront-composition --strict` and `openspec validate --all --strict`, reporting unrelated pre-existing active-change failures separately.

## 10. Closeout

Per `openspec/README.md#plan-closeout-requirements`, complete this section only after Section 9 proves the behavior.

- [ ] 10.1 **Comment hygiene.** Run `make check-comment-hygiene`; directly read every changed comment/docstring around registry selection, migration, site routing, and recovery; remove change/task/history wording, temporary commentary, obsolete singleton/fallback explanations, and any review tombstones before completion.
- [ ] 10.2 **Import placement.** Review every import added or touched across core and both contributions, move function-local imports to module scope where safe, verify any retained local import through an actual circular-import or deliberate lazy-load test, rerun the affected suites, and confirm core/kit/domain dependency direction including `TYPE_CHECKING`.
- [ ] 10.3 **Documentation compliance and promotion.** Synchronize the verified deltas into `openspec/specs/{market-composition,storefront-publication,negotiation-protocol,settlement-servicing,deployment-state,test-compatibility}/spec.md`; promote rationale to each applicable companion `architecture.md`; update `docs/development/ARCHITECTURE.md` for the compute-family composition/authority flow, `docs/development/DEPLOYMENT_AND_CONFIG.md` for migration/config/rollout, and `docs/development/TESTING.md` for the proof boundary. Permanent text describes current behavior, not completion history.
- [ ] 10.4 **Narrative compression.** Move any durable alternatives/rationale into `design.md`, then compress completed task notes to final behavior, material evidence, unresolved prerequisite/environment failures, and permanent destinations while preserving every completed checkbox and deleting duplicated debugging narrative.
- [ ] 10.5 **Roadmap currency.** Update `docs/development/ROADMAP.md` Goal 3 current state and gap mapping: mark the accepted multi-domain storefront boundary accurately, retain `bare-metal-buyer-domain` and Compute-40 gaps that remain, and record the exact roadmap disposition in the design-promotion record.
- [ ] 10.6 **Promotion record and final closeout.** Replace `design.md`'s planned Design Promotion Record rows with exact accepted headings, mark every material decision permanent/temporary/superseded/rejected, confirm no production code references `openspec/changes`, rerun affected focused/integration/package/type/render/E2E checks plus strict change/all validation after promotion, disclose anything unrun, and leave the change ready for review/sync/archive with no temporary compatibility alias, singleton, domain branch copy, fake fulfillment, or default fallback.
