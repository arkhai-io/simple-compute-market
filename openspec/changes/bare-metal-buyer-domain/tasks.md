## 1. Prove and freeze every prerequisite

- [ ] 1.1 Build the prerequisite matrix in `openspec/changes/bare-metal-buyer-domain/design.md` from permanent headings, installed wheel metadata/public symbols, focused producer tests, and integration evidence for: `add-persistent-buyer-profiles`; `storefront-domain-parameterization`; `multi-domain-storefront-composition` plus pool-declared offering modes; `market-platform-bare-metal-10-storefront-composition`; `pools-7-storefront-fulfillment-cutover`; common Alkahest selection/recovery; and `consume-expanded-stripe-funding`. Treat active-change status, checked tasks, source-only modules, injected fake ports, and `fulfillment_available=false` as failed evidence.
- [ ] 1.2 Record the exact accepted public distributions, versions, carrier kinds/versions, and client methods for authenticated listing/domain binding, negotiation, settlement, agreement status, strict lease result/evidence, buyer-only SSH access, idempotent teardown, Alkahest, hosted profiles, and transient actions. Confirm the installed compute-family schema declares `offer_resource.virtualization_type="bare_metal"` and exact no-domain/no-site-fallback behavior.
- [ ] 1.3 Confirm `arkhai-core-buyer` exposes `core.resolved-buyer-identity.v1`, `BuyConfig.from_resolved_identity`, version-3 run identity, `resolve_recovery_buyer_identity`, exact accepted-state loading, common settlement selection, and transient-action handling without a concrete-domain import; run the prerequisite's focused identity create/rotate/resume and buyer-domain conformance evidence.
- [ ] 1.4 Stop all later implementation if any matrix row is absent, contradictory, not promoted, or not available from staged wheels. If an accepted producer differs from the consumer contract in this change, amend `proposal.md`, `design.md`, every affected delta spec, and these tasks before writing code; do not add a buyer-local HTTP literal, seller/provisioner import, fake result, direct database call, or compatibility adapter.

## 2. Create the independent buyer wheel and immutable plugin

- [ ] 2.1 Create `domains/bare_metal/buyer/{pyproject.toml,uv.lock,Makefile}` and `domains/bare_metal/buyer/src/arkhai_bare_metal_buyer/{__init__.py,plugin.py,cli.py}` as the `arkhai-bare-metal-buyer` distribution. Declare only released core buyer/core carrier, `arkhai-bare-metal`, registry/storefront client, identity/config/policy, and shared settlement wheel dependencies; declare `bare-metal = arkhai_bare_metal_buyer.plugin:domain` under `market.buyer_domains` and no console script or editable sibling source.
- [ ] 2.2 In `plugin.py`, derive one immutable `bare_metal.v1` contract from the installed domain contract, add only `DomainCapability.BUYER`, declare `core.resolved-buyer-identity.v1`, and wire command registration, `make_bare_metal_provision_terms`, common policy selection, and the accepted strict buyer-safe result codec. Reject unsupported contract/result versions and incomplete hooks during core startup.
- [ ] 2.3 Register one collision-free `market bare-metal` namespace in `cli.py` with `listing list/show`, `buy`, `negotiate`, `settle --from`, `status --from`, `result --from`, `access --from`, and `teardown request/status --from`; keep profile and mechanism utilities in their existing core/registration namespaces and do not ship a `market-bare-metal`, module-owned `market`, daemon, or callback server.
- [ ] 2.4 Add `domains/bare_metal/buyer/tests/{test_plugin.py,test_package_boundary.py,test_cli.py}` using `market_core.domain_conformance.DomainConformanceCase`. Prove installed/uninstalled and multiple-domain command composition, unique namespace/identity, unsupported version/injection failure, built-wheel metadata, and runtime plus `TYPE_CHECKING` exclusion of storefront, site/resource-pool/fulfillment/provisioning implementations, sibling buyers, hosted service/Stripe SDK, and test/e2e packages.

## 3. Implement exact discovery, demand, and negotiation

- [ ] 3.1 Add `domains/bare_metal/buyer/src/arkhai_bare_metal_buyer/listing.py` over the authenticated registry client and filter-spec compiler. Always compile the declared `offer_resource.virtualization_type=bare_metal` predicate, verify signed authority and immutable `bare_metal.v1` binding/payload, preserve ETag and duplicate-listing conflict behavior, and fail rather than broad-query/post-filter or infer the domain from payload fields.
- [ ] 3.2 Add `demand.py` to accept positive duration and exactly one OpenSSH `ssh-ed25519` public key, validate its base64 body, and reject private-key markers, multiline/options/unknown algorithms, `access_ref`, seller-owned resource/routing/commercial fields, and any loose payload before calling `arkhai_bare_metal.make_bare_metal_provision_terms`.
- [ ] 3.3 Add `negotiation.py` as a thin adapter around `core_buyer.orchestration` and the accepted storefront client. Verify the authority-authenticated listing, original provision envelope, exact canonical parties, listed access/duration bounds, immutable resource binding, accepted settlement selection/plan, and terminal terms before settlement; do not copy VM/API-credit orchestrators or call the bare-metal storefront/provisioner implementation.
- [ ] 3.4 Make fresh `buy`/`negotiate` resolve the selected buyer profile once and construct core `BuyConfig` from that resolved identity. Make continuation use the recorded negotiation, listing, authority, provision envelope, transcript, profile UUID/principal, and selected option rather than current defaults, current listing contents, or a new negotiation.
- [ ] 3.5 Add `domains/bare_metal/buyer/tests/{test_listing.py,test_demand.py,test_negotiation.py,test_resume.py}` for schema/ETag failures, VM/unknown-domain results, duplicate conflicts, strict SSH parsing, every forbidden buyer authority field, listing/terms/resource/party/option mismatch, closed listings during resume, conflicting durable transcripts, and no remote mutation or run event for invalid fresh demand.

## 4. Compose settlement, persistent identity, transient actions, and recovery

- [ ] 4.1 Add `domains/bare_metal/buyer/src/arkhai_bare_metal_buyer/settlement.py` using `core_buyer.settlement`, the installed mechanism registry, and accepted storefront transport. Select exactly one advertised option or legacy Alkahest alternative, persist its canonical mechanism/option/obligation/operation identities, and verify server acceptance without importing VM settlement helpers or provider behavior.
- [ ] 4.2 Route hosted `card.v1`, `us_bank_transfer.v1`, and `us_ach_debit.v1` through the accepted shared hosted adapter and transient `BuyerActionHandler`; persist only safe action kind/expiry metadata, re-fetch current state on resume, and never persist an action URL, payer/instrument/provider object, or raw hosted response.
- [ ] 4.3 Require every `settle/status/result/access/teardown --from` path to call `resolve_recovery_buyer_identity(run_id)` and load exact accepted state before any client call. Reject current-profile substitution, missing/retired run principal, changed authority, changed option/profile/amount/parties, and current-priority fallback.
- [ ] 4.4 Prove hosted-only Ed25519 operation initializes and reaches pending/funded/status recovery with all wallet/chain values absent; prove an accepted `alkahest.v1` option activates its explicit wallet/chain validation and does not change the marketplace signer/profile contract.
- [ ] 4.5 Add `domains/bare_metal/buyer/tests/{test_settlement.py,test_actions.py,test_identity_recovery.py}` covering option distinctness/compatibility, mechanism mismatch and readiness changes, commit-then-response-loss, profile rotation, action open/print/fail and URL non-persistence, delayed bank state, exact hosted retry, Alkahest explicit dependencies, and secret-free run/profile/config/output representations.

## 5. Add trusted result, evidence, access, and teardown commands

- [ ] 5.1 Add `domains/bare_metal/buyer/src/arkhai_bare_metal_buyer/lifecycle.py` against the accepted public storefront client methods frozen in Section 1. Address every status/result/evidence/access/teardown call from the run-recorded storefront authority and agreement only; verify signed response authority, canonical buyer, agreement, obligation, fulfillment, resource/lease, and safe reference bindings before decoding.
- [ ] 5.2 Decode only the accepted closed versioned lease result/evidence carrier. Permit agreement/obligation/fulfillment, Physical Resource, Capacity Reservation/lease, canonical buyer, access method/readiness/expiry, and opaque evidence/teardown references; reject unknown keys, arbitrary `details`, connection/user data, credential/private material, provider/executor objects, raw results, and mismatched references. Persist only safe stable references/digests through `core_buyer.run_log`.
- [ ] 5.3 Implement explicit authenticated SSH access retrieval as a non-durable view containing only host, port, username, host-key fingerprints, validity, and opaque grant reference. Never log, cache, include in generic status/JSONL/TOML/portable evidence, or infer an SSH private key/path; add `presentation.py` so default status/result rendering cannot accidentally include the access response.
- [ ] 5.4 Implement `teardown request/status` using one persisted idempotent request/teardown reference for the exact run. Distinguish requested, running, complete, failed/operator-action, and already-expired; never call provisioning directly, claim revocation from request acceptance, mark capacity available, or turn physical teardown into financial reclaim.
- [ ] 5.5 Add `domains/bare_metal/buyer/tests/{test_lifecycle.py,test_access_redaction.py,test_teardown.py,test_presentation.py}` for wrong signer/authority/agreement/principal/reference, unknown/credential-bearing public fields, raw executor leakage, non-durable access, response-loss retry, concurrent duplicate teardown, running-versus-complete, terminal failure, expiry, collection independence, and sanitized output/repr/exception behavior.

## 6. Wire build, install, reinit, configuration, and review scope

- [ ] 6.1 Add `dist-bare-metal-buyer` and `test-bare-metal-buyer` to `domains/Makefile` and the root `Makefile`, include the wheel in aggregate `dist`/wheelhouse/package-review targets, and enforce a pure wheel with entry-point metadata. Update `scripts/resolve-review-scope.py` and `scripts/tests/test_resolve_review_scope.py` so changes to core buyer, bare-metal domain, registry/storefront clients, identity/config/policy, settlement runtime/Alkahest, or hosted adapter select the bare-metal buyer project and its focused targets.
- [ ] 6.2 Implement `domains/bare_metal/buyer/Makefile` `init`, `reinit`, `test`, `build`, and installed-entry-point smoke targets. `reinit` MUST rebuild prerequisites and explicitly `--upgrade-package`/`--reinstall-package` `arkhai-core`, `arkhai-core-buyer`, `arkhai-core-registry-client`, the accepted storefront client, `arkhai-bare-metal`, `arkhai-bare-metal-buyer`, identity/config/policy, settlement runtime/Alkahest, and accepted hosted client/adapter from `.dist`.
- [ ] 6.3 Update `install.sh` and `scripts/package-review-wheelhouse.sh` so clean installation/review installs `arkhai-bare-metal-buyer` beside the core-owned `market` script and verifies its entry point; do not symlink a second executable, use a source checkout, or make the VM buyer distribution the authority for bare-metal command registration.
- [ ] 6.4 Add `config.bare-metal-ed25519.toml` as a safe public example using common registry/storefront trust, selected profile, `bare_metal.v1` defaults, and typed settlement hierarchy. Include an SSH public-key reference only if the accepted common configuration model supports it; omit marketplace seed, SSH private-key path/value, wallet/chains in hosted-only mode, hosted/provider data, action/access URLs, and compose-local/test bypass values.
- [ ] 6.5 Add packaging/config tests under `domains/bare_metal/buyer/tests/{test_wheel.py,test_config.py}` and repository deployment-packaging tests where the installer/wheelhouse contract is currently owned. Prove remove/reinstall behavior, no undeclared/editable paths, safe generated configuration, hosted-only wallet absence, Alkahest-only wallet activation, and no dependency by seller/site/provisioner startup on the buyer plugin.

## 7. Run focused and integration verification

- [ ] 7.1 Run the new package's focused suite from the reinitialized staged-wheel environment, the shared market-domain conformance case, `core/buyer` plugin/orchestration/profile/action/run-log tests, and affected `arkhai_bare_metal`/storefront-client producer-consumer fixture tests; record exact commands and results in this task without substituting source-tree imports.
- [ ] 7.2 Run import-boundary, typing, wheel build/contents/metadata, installed entry-point, installer, configuration, review-scope, and package-review checks. Prove a core-only install, bare-metal-only plugin install, and VM/API-credit/bare-metal multi-plugin install all expose the intended commands and collision failures.
- [ ] 7.3 Run authenticated integration scenarios against the accepted runnable storefront for discovery, exact negotiation, seller rejection, Alkahest and each hosted funding profile, transient action, delayed funding, result/evidence/access retrieval, restart after every durable boundary, teardown response loss, terminal teardown failure, and expiry. Attribute seller/provisioner contract failures to their owning suites rather than bypassing them in buyer tests.
- [ ] 7.4 Scan run JSONL, profile metadata, TOML/examples, stdout/stderr intended as safe output, logs, diagnostics, reprs, portable evidence, staged wheels, manifests, and rendered deployment inputs for marketplace seeds, SSH private material/paths, wallet keys, passwords, bearer tokens, action URLs, hosted/provider IDs/payloads, raw executor result, and access responses; fail on any match and preserve only redacted evidence.

## 8. Prove installed whole-host access and revocation

- [ ] 8.1 Add `e2e-tests/config/bare-metal-buyer.toml`, `e2e-tests/src/e2e/test_bare_metal_buyer.py`, and the applicable `e2e-tests/Makefile` target. Install only staged wheels, discover the plugin through metadata, and use ordinary authenticated registry/storefront URLs and trust pins; do not import repository source, inject lifecycle ports, mutate databases, pre-seed success, hard-code site/resource selection, or call provisioning directly.
- [ ] 8.2 Run the hosted-only Ed25519 path with wallet/chains absent: discover a published `virtualization_type=bare_metal` listing, negotiate exact demand, select the configured hosted profile, handle any transient action, wait for authoritative funding and real selected-site fulfillment, verify portable evidence, retrieve SSH connection data, and observe successful host access using the operator-owned private key.
- [ ] 8.3 Interrupt/restart after acceptance, settlement acknowledgement uncertainty, action, funding, lease readiness, and teardown acceptance; prove the same profile/principal, agreement, obligation/operation, fulfillment/access reference, and teardown are reused without double negotiation, funding, provisioning, access grant, collection, or teardown.
- [ ] 8.4 Request or await normal teardown, then observe authoritative teardown completion, failed SSH access, and the accepted capacity/lease revocation evidence. Run the same domain lifecycle with Alkahest under explicit wallet/chain configuration as regression. Reject a success flag, no-op executor, fake result, or mere request acknowledgement as evidence.
- [ ] 8.5 Run the protected release/package qualification that owns the staged buyer wheel, dependency manifest/SBOM/provenance, clean install, real whole-host effect, and credential scan; record immutable artifact identities and disclose any environment-protected evidence not run locally.

## 9. Promote verified behavior and close out

- [ ] 9.1 After behavioral and code review, promote the independent wheel/core executable/dependency boundary to `openspec/specs/market-composition/{spec.md,architecture.md}` and `docs/development/ARCHITECTURE.md`; promote exact demand and seller-authoritative resource/terms ownership to `openspec/specs/negotiation-protocol/{spec.md,architecture.md}`.
- [ ] 9.2 Promote persistent signer injection/recovery, exact settlement continuity, strict result/evidence, separate access retrieval, and teardown behavior to `openspec/specs/buyer-orchestration/{spec.md,architecture.md}`; promote wheel/entry-point/install/reinit/config/wallet-free rules to `openspec/specs/deployment-state/{spec.md,architecture.md}` and `docs/development/DEPLOYMENT_AND_CONFIG.md`.
- [ ] 9.3 Promote test jurisdiction and installed real-access/revocation evidence to `openspec/specs/test-compatibility/{spec.md,architecture.md}` and `docs/development/TESTING.md`. Update `docs/buyer-quickstart.md` and add `docs/bare-metal-buyer-quickstart.md` only if a separate guide remains necessary for exact bare-metal commands, SSH public-key handling, settlement examples, resume, access, and teardown.
- [ ] 9.4 Complete the repository closeout in one final pass: run `make check-comment-hygiene` and directly read touched comments/docstrings for change/task/migration provenance; move touched local imports to module scope unless a reproduced circular import or documented deliberate lazy-load reason requires otherwise and rerun the owning tests; re-check every accepted decision against `openspec/README.md` placement rules; compress completed task notes to final behavior/evidence/deferred work while preserving all completed tasks; update Compute-30 current state and remove/remap the `bare-metal-buyer-domain` gap in `docs/development/ROADMAP.md` (or record an explicit no-impact finding); complete the design-promotion record in `design.md`; remove temporary commentary, review-only tombstones, fake/test-only buyer paths, obsolete aliases, and superseded config; then run strict change and repository OpenSpec validation and record any unrelated pre-existing failures separately.

## 10. Closeout

Per `openspec/README.md#plan-closeout-requirements`.

- [ ] 10.1 **Comment hygiene.** Run `make check-comment-hygiene`, then direct-read the comments and docstrings this change touches for the fuzzier provenance-narration rule the target cannot catch mechanically.
- [ ] 10.2 **Import placement.** Review every import this change adds or touches and move it to module level where safe; retain a local import only against an observed circular import or a documented lazy-load reason, verified against the real suite.
- [ ] 10.3 **Documentation compliance.** Re-check this change's accepted decisions against `openspec/README.md`'s placement rules. It carries delta specs for `buyer-orchestration`, `deployment-state`, `market-composition`, `negotiation-protocol`, `test-compatibility`; confirm each landed in the owning `openspec/specs/<capability>/spec.md`, and that durable conceptual rationale sits in the companion `architecture.md` rather than only in `design.md`.
- [ ] 10.4 **Narrative compression.** Compress completed-task notes to final behavior, material validation evidence, unresolved or deferred work, and permanent-documentation destinations, moving durable rationale into `design.md` first.
- [ ] 10.5 **Roadmap currency.** Update the “One storefront serving several compute-family domains” goal's current-state description and gap-to-change mapping in `docs/development/ROADMAP.md`, and name that update in the design-promotion record.
- [ ] 10.6 **Campaign index currency.** Update this change's row, and its campaign's dependency graph, in `openspec/changes/README.md` to match its state at completion, or record the disposition here if its status and campaign placement are both unchanged.
- [ ] 10.7 **Promotion.** Add a design-promotion record, mapping every accepted decision to its exact permanent heading, and verify no production source references `openspec/changes/bare-metal-buyer-domain`.
