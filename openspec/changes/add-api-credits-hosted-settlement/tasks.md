## Completed local behavior

- [x] Shared hosted buyer transport now lives in `core_buyer.hosted_settlement`, with `make_hosted_settle_hook` owning action/poll/resume behavior; VM and API-credit buyers consume it and the VM transport copy is removed.
- [x] Common signed `/api/v1/settlements` start/status/reclaim mechanics now live in `market_settlement_runtime.hosted_routes` behind prepare/authorize/reserve/fulfill/project/cleanup/before-reclaim callbacks; VM and API-credit storefronts consume the same service while their Alkahest routes remain independent.
- [x] API-credit listing, negotiation, and buyer flows carry strict mechanism-neutral `settlement_options`, exact `SettlementSelection`, integer quantity-scaled pricing, canonical parties, exact condition/profile/currency/key target, and optional independent `accepted_escrows`.
- [x] API-credit buyer and storefront roots register Alkahest and hosted Stripe through `SettlementConfigurationRegistry`; hosted-only Ed25519 composition resolves no wallet, chain, RPC, provider model, VM package, or bare-metal package.
- [x] API-credit buyer fresh and recovery commands prepare exact hosted authorization, use the shared transport, handle transient action policy, preserve accepted refs, and expose status/reclaim without importing the released client directly.
- [x] Quota-backed publication compiles independently ready hosted profiles into ordered deterministic options, preserves optional Alkahest escrows, admits hosted-only listing shape/query projections, and keeps quota closure independent from per-profile readiness blockers.
- [x] Server-authoritative API-credit preparation reloads trusted listing/negotiation state and derives service, quantity, key mode/key ID, buyer/claimant, amount/currency/profile/account, expiry, condition, obligation, and operation identity without buyer overrides.
- [x] The credits client/authority contract carries deterministic `fulfillment_id`, obligation/mechanism, canonical owner, service/resource, quantity/key target, immutable request digest, and safe result projection; changed reuse conflicts before mutation.
- [x] Credits-authority migrations add canonical owner and unique fulfillment/grant/digest state, backfill valid Alkahest grants with `fulfillment_id=escrow_uid`, preserve unowned keys, and reject ambiguous principal migration transactionally.
- [x] Issuance reserves authoritative quota and creates or tops up exactly once per fulfillment identity; retry retrieves the grant, complete canonical owner comparison protects existing keys, and new-key credential retry retains the unused/used disclosure rules.
- [x] API-credit fulfillment derives its grant identity from the accepted obligation, runs only after authoritative funded state and a shared fulfillment lease, reconciles commit-then-fail through credits-authority lookup, and persists credentials only in the owner-authenticated private result repository.
- [x] Canonical seller-signed `api-credits` issuance evidence binds condition anchor, obligation/fulfillment/grant, service/resource, quantity, public key target, canonical parties, issuer/time/request digest; its authenticated resolver is digest-keyed, idempotent, freshness/signature/anchor checked, and bearer/provider secret-free.
- [x] Shared servicing orders materialize/status, authoritative funding, exact-once issuance, portable evidence, condition evaluation, and collection; before reclaim it retrieves uncertain grants, blocks reclaim when issuance committed, and leaves authoritative no-grant outcomes eligible without cross-profile/mechanism fallback.
- [x] API-credit status projects safe delayed card/bank/ACH/action/reason/deadline state and buyer-authenticated credentials while existing Alkahest fulfillment, compensation, status, and credential behavior remain on their original path.
- [x] API-credit registry schema, package dependencies, staged-wheel image inputs, service/storefront migrations, config defaults/examples, and wallet-optional composition surfaces include the shared hosted/runtime contract without source sharing or provider credentials.
- [x] Focused contract coverage exists for shared transport/hook semantics, strict carrier/party selection, hosted-only composition, credits exact-once ownership/digest/migration behavior, secret-safe evidence/private results, and the common runtime's funding/restart/collect-reclaim exclusion; execution is intentionally delegated to the integration lead by the no-validation instruction for this lane.
- [x] Permanent API-credit, buyer, publication, composition, servicing, deployment, and test specifications/architecture plus repository architecture, deployment, testing, roadmap, buyer quickstart, and vLLM cookbook describe the implemented current state. `design.md` contains the exact design-promotion headings.

## Validation handoff

Consolidated validation passed for the shared buyer/storefront/runtime suites, all API-credit domain/service/buyer/storefront/sample-app and Python/TypeScript/Rust middleware suites, VM non-regression suites, changed-file Ruff lint/format checks, adopter wheel builds, buyer/storefront CLI smoke tests, Helm rendering, strict Compose interpolation, comment hygiene, and this change's strict OpenSpec validation. Repository-wide strict OpenSpec validation now passes every modified permanent spec; seven unrelated active changes remain invalid. Aggregate kit/storefront entrypoints remain blocked before test execution by the unavailable independently signed hosted `v0.2.0` trust input described below.

## External blocked evidence

- [ ] Verify and record the exact independently signed `expand-stripe-payer-funding` producer manifest/client/image/provenance and its protected acceptance run. **Blocked:** this checkout contains the consumer pins and local deterministic contract only; no independently verifiable signed producer acceptance record is available here.
- [ ] Run the wallet-free API-credit hosted protected scenario for available card, US bank transfer, ACH, off-session action, new-key use to HTTP 402, same-owner top-up, other-owner rejection, restart, evidence, collection/reclaim, and sign the sanitized report. **Blocked:** requires the exact accepted producer artifacts, protected Stripe account capabilities/credentials and browser inputs, plus a deployed authenticated portable evidence resolver; deterministic local ports are not substitutes.
