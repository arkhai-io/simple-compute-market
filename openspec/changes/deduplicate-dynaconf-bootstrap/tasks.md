## 1. Characterize current behavior

- [x] 1.1 Record the provisioning/e2e bootstrap inventory in `openspec/changes/deduplicate-dynaconf-bootstrap/design.md`, including roots, profile parsing, include order, prefixes, nested separators, dotenv/secrets handling, missing-file behavior, merge flags, validators, wrappers, helpers, and the absence of the stale provisioning storefront fallback.
- [x] 1.2 Add consumer characterization coverage before cutover: extend `provisioning/compute/service/tests/unit/test_config.py` and add `e2e-tests/tests/unit/test_settings_loader.py` to prove default and multiple profiles, include order/missing includes, nested environment overrides, dotenv/secrets behavior where owned by that consumer, and the existing wrapper/helper behavior.

## 2. Extract shared construction

- [x] 2.1 Add `kit/config/src/market_config/dynaconf_bootstrap.py` with an immutable loader-options contract plus pure profile/include resolution helpers and a Dynaconf builder. Export the public contract from `kit/config/src/market_config/__init__.py`; keep all consumer imports out of kit.
- [x] 2.2 Add focused shared unit coverage in `kit/config/tests/unit/test_dynaconf_bootstrap.py` for profile parsing, ordered include construction, missing-include filtering/preservation, constructor options, and nested-separator compatibility. Add Dynaconf to `kit/config/pyproject.toml` and regenerate `kit/config/uv.lock` without unrelated dependency drift.
- [x] 2.3 Migrate `provisioning/compute/service/src/compute_provisioning_service/config.py` to the shared builder while retaining the `Settings` wrapper and current public globals. Add `arkhai-kit-config` to the service `Makefile` reinit package list so the rebuilt wheel is actually refreshed; keep its direct runtime dependency set otherwise unchanged and refresh `provisioning/compute/service/uv.lock` only if rebuilt wheel metadata requires it.
- [x] 2.4 Migrate `e2e-tests/src/settings.py` to the shared builder while retaining `Validator`, `validate_all()`, `active_profiles()`, and `config_directory()`. Add `arkhai-kit-config` as a direct e2e dependency instead of relying on transitive installation; keep `.secrets.toml`, project `.env`, and pass-through missing includes unchanged and refresh `e2e-tests/uv.lock` without unrelated dependency drift.
- [x] 2.5 Build `arkhai-kit-config` into `.dist`, verify the wheel declares Dynaconf, reinstall the rebuilt wheel into both consumers using their existing wheel-only Make/uv flows, and compare the characterization fixtures against the pre-cutover expectations. Final verification built `arkhai_kit_config-0.1.2-py3-none-any.whl` with `market_config/dynaconf_bootstrap.py` and `dynaconf>=3.0.0`; provisioning and e2e reinit both installed `arkhai-kit-config==0.1.2`, and the focused characterization fixtures pass under those environments.

## 3. Verify and promote

- [x] 3.1 Run `kit/config` unit tests; compute-provisioning config/unit and startup-relevant integration tests; e2e settings/unit tests; and wheel/install/lock checks. Run profile-based integration checks that are locally available, and disclose any deployment/system suites that cannot run in this environment. Final repository-standard verification: kit/config 134 passed; provisioning 632 unit and 215 integration tests passed under `ACTIVE_PROFILES=mock`; e2e focused settings 2 passed and full unit suite 236 passed under the local profile; all three `uv lock --check` commands passed. The full deployment/system e2e suite was not run because this refactor's planned evidence is satisfied by wheel-installed unit and locally available profile/integration coverage.
- [x] 3.2 Promote the shared bootstrap contract and preserved compute-provisioning/e2e semantics to `openspec/specs/deployment-state/spec.md` and the foundation/composition rationale to `openspec/specs/deployment-state/architecture.md`. Scope the permanent contract to these migrated consumers; the API-credit service remains out of scope. No repository-wide `ARCHITECTURE.md` edit is expected because the existing package-layer rule already classifies configuration as a foundation capability.
- [x] 3.3 Update `openspec/changes/deduplicate-dynaconf-bootstrap/design.md` with the design-promotion record and final validation evidence; scan the two consumers for obsolete duplicate profile/include constructors; run strict OpenSpec validation when the repository tool is available. The active change passes `npx @fission-ai/openspec@latest validate deduplicate-dynaconf-bootstrap --strict`; repository-wide `validate --all --strict` reports 76 passed and 11 failures in other active changes, so those baseline failures are deferred outside this change.

## 4. Closeout

- [x] 4.1 Complete the plan closeout required by `openspec/README.md#plan-closeout-requirements`: (1) run `make check-comment-hygiene` and directly review touched comments/docstrings for present-intent wording; (2) review every local import added or touched by this change and move it to module scope when safe, verifying any necessary local import against the real suite; (3) re-check every accepted design decision against documentation ownership and cross-reference rules; (4) compress completed task notes to final behavior, material validation, unresolved/deferred work, and permanent-documentation destinations while preserving any useful rationale in `design.md`; (5) update `docs/development/ROADMAP.md` if this change affects a roadmap goal, otherwise record explicitly in the design-promotion record that it has no roadmap impact; and (6) complete the design-promotion record for every material decision. Closeout passes: comment hygiene and `git diff --check` are clean; touched imports are covered by the repository-standard suites; documentation ownership, roadmap no-impact, task narrative compression, and the complete design-promotion record were rechecked.
