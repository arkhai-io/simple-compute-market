## 1. Establish the single-domain contract boundary

- [x] 1.1 In `domains/vms/storefront/src/market_storefront/domain_runtime.py`, separate ordinary VM storefront-contract construction from VM-root validation; require stable identity `compute.v1`, the core-supported contract version, complete codecs, and declared/implemented publication, storefront, settlement, fulfillment, and compute-provisioning capabilities, and return the supplied immutable object unchanged. Add wrong-type, wrong-identity, unsupported-version, missing-declaration, undeclared-implementation, incomplete-codec, and incomplete-hook cases to `domains/vms/storefront/tests/unit/test_domain_runtime_wiring.py`, asserting failure before any app/repository collaborator is constructed.
- [x] 1.2 In `domains/vms/storefront/src/market_storefront/server.py`, add `build_vm_storefront_app(*, domain=...)` and a domain-bound lifespan factory; construct the current VM contract exactly once only at the outermost default `app` statement, keep `run_serve` and routes unchanged, and bind every lifecycle callback to the selected object rather than `get_market_domain_contract`.
- [x] 1.3 In `domains/vms/storefront/src/market_storefront/container.py` and `startup.py`, publish the lifespan-selected contract as request-wiring state, reject app/container/service disagreement before settlement preflight or workers, clear lifespan-owned state according to the existing shutdown pattern, and keep startup-step order and behavior unchanged. Extend `domains/vms/storefront/tests/unit/test_server_app_composition.py` to compose two distinct compatible contract objects and prove exact app/container identity, no cross-app leakage, invalid-input fail-closed ordering, and unchanged route/lifespan behavior.

## 2. Thread the contract through persistence and domain-sensitive services

- [x] 2.1 In `domains/vms/storefront/src/market_storefront/utils/sqlite_client.py`, make `SQLiteClient` and `get_sqlite_client(*, domain)` require and retain the validated contract, expose it read-only for composition agreement checks, and fail instead of reusing an existing settings-bound singleton under a different contract object or identity. Update direct constructors and fixtures in `domains/vms/storefront/tests/` to pass the ordinary VM contract explicitly; extend `tests/unit/test_migrations.py` to prove a pre-change database opens with no new migration/schema/data rewrite and stable listing, negotiation, obligation, fulfillment, and operation identifiers.
- [x] 2.2 In `domains/vms/storefront/src/market_storefront/services/listing_service.py`, require the injected contract, verify repository agreement, and replace the module accessor in listing validation with its listing codec. Update `domains/vms/storefront/tests/unit/test_publications_wiring.py` and `domains/vms/storefront/tests/integration/test_listings_api.py` to prove the exact injected codec governs create/reload/republish, current settlement alternatives and deterministic public payloads are unchanged, and incompatible composition fails before persistence or registry publication.
- [x] 2.3 In `domains/vms/storefront/src/market_storefront/controllers/negotiate_controller.py` and `utils/sync_negotiation.py`, inject/pass a required contract through new and continued rounds, message normalization, default seller policy, accepted artifact/plan construction, and the callback bound into core `NegotiationService`; retain the optional seller-round-hook seam but remove every lower-layer domain default. Update `tests/unit/test_sync_negotiation_seller_round_hook.py`, direct callers in `tests/unit/test_order_pause_state.py`, `tests/unit/services/test_negotiation_service.py`, and `tests/integration/test_negotiations_api.py` to prove exact-object use, envelope-kind rejection before policy/state, accepted-plan parity, and no module-accessor monkeypatch.
- [x] 2.4 In `domains/vms/storefront/src/market_storefront/settlement_composition.py`, require the contract in `VmSettlementComposition` and `build_vm_settlement_composition`, verify repository agreement, and bind accepted-plan and fulfillment callbacks to that object; remove both `domain_runtime` lookups while leaving mechanism registration, `SettlementSQLiteRepository`, worker, operation journal, readiness, failure actions, and identifiers unchanged. Extend `tests/unit/test_settlement_composition.py` and `tests/integration/test_settle_controller.py` to prove injected plan/fulfillment hook use, failure before settlement registration on mismatch, and unchanged Alkahest preparation, fulfillment, projection, collect/reclaim ordering, and public results.
- [x] 2.5 Perform the clean cutover in the VM storefront production package: remove the getter-style domain-selection API and every semantic `get_market_domain_contract` call, update all callers in the same changeset, and add an assertion in `domains/vms/storefront/tests/unit/test_architecture_imports.py` that only the outermost VM root constructs the default contract and that core/kit import no concrete domain, VM imports no bare-metal composition package, and no container/module lookup can select domain behavior below the root. Do not leave an alias, optional default, deprecated path, or fallback.

## 3. Focused, integration, and packaging proof

- [x] 3.1 Focused unit ownership is covered by the complete installed-wheel VM storefront run: `1073 passed, 1 skipped, 2 warnings`; exact-object, pre-side-effect, capability, parity, and architecture assertions passed.
- [x] 3.2 Owning HTTP integrations and standalone publication-command coverage passed in the same `1073`-test VM storefront run.
- [ ] 3.3 The complete VM storefront package suite passes (`1073 passed, 1 skipped, 2 warnings`). No repository-owned VM storefront typing target exists; an ad hoc mypy run is not completion evidence because installed internal distributions lack `py.typed` markers and produce 319 unrelated `import-untyped` errors.
- [x] 3.4 The integrated consumer build rebuilt and force-reinstalled the VM/core/kit wheel graph from `.dist`; the installed-wheel full storefront suite passed, package review/deployment packaging passed (`66` script tests plus Helm schema/render checks), and no editable sibling or VM-to-bare-metal dependency was introduced.

## 4. Confirm the deliberate no-migration/no-deployment boundary

- [x] 4.1 Verify implementation changed no persistence or carrier contract: do not add or edit `domains/vms/storefront/src/market_storefront/utils/migrations.py`, `core/storefront/src/core_storefront/sqlite_migrations.py`, public request/response models, contract fixtures, database columns, migration version, or backfill. Record in `design.md` if implementation evidence overturns this decision before introducing any such change.
- [x] 4.2 Verify no operator selection or package-topology input was introduced: leave `domains/vms/storefront/settings.toml`, `domains/vms/storefront/src/market_storefront/utils/config.py`, Compose files, `helm/`, `domains/vms/storefront/Dockerfile`, `pyproject.toml`, and `uv.lock` unchanged except an explicitly justified test-target inclusion. Confirm existing VM and Alkahest configuration, routes, default command, wheel dependencies, and deployment renders require no migration step.
- [ ] 4.3 The bare-metal reference suite was run after integration: `39 passed, 9 failed`. Failures expose existing canonical-principal, authenticated-opening, health-identity, and typed settlement-plan incompatibilities in the unfinished `market-platform-bare-metal-10-storefront-composition` prerequisite; this change does not weaken or bypass those contracts.

## 5. Repository closeout

- [x] 5.1 **Comment hygiene:** direct review retained only current-state rationale; `make check-comment-hygiene` passed.
- [x] 5.2 **Import placement:** inspect imports added or touched by this change, move them to module level wherever safe, and retain a local import only after proving a real circular import or deliberate lazy-load need with the owning VM storefront tests; do not broaden this into unrelated import cleanup.
- [x] 5.3 **Permanent documentation compliance:** after code review and behavioral proof, synchronize the delta requirements into `openspec/specs/market-composition/spec.md`, `openspec/specs/storefront-publication/spec.md`, and `openspec/specs/test-compatibility/spec.md`; promote injection flow/rationale to `openspec/specs/market-composition/architecture.md` and `openspec/specs/storefront-publication/architecture.md`; record test ownership in `openspec/specs/test-compatibility/architecture.md` and `docs/development/TESTING.md#boundary-change-validation`; and update `docs/development/ARCHITECTURE.md#composition-from-above-and-below` with the current explicit storefront-contract boundary. Permanent documents describe the resulting system, not this change's history.
- [x] 5.4 **Narrative compression:** preserve all completed checkboxes, reduce completed-task notes to final behavior, material validation evidence, unresolved/deferred work, and permanent destinations, and move any durable alternative or review rationale into `design.md` before deleting duplicated narrative.
- [x] 5.5 **Roadmap currency:** update `docs/development/ROADMAP.md` under the multi-domain storefront goal so current state says the VM and bare-metal roots both inject contracts and the remaining gap is explicit per-record domain/offering-mode selection and persistence owned by `multi-domain-storefront-composition`; keep the gap-to-change mapping current and record the exact roadmap destination in the design-promotion record.
- [x] 5.6 **Promotion record and closeout evidence:** update `design.md#permanent-promotion-map` with every accepted decision's final permanent location or explicit temporary/rejected/superseded classification, verify no production source references `openspec/changes/storefront-domain-parameterization`, disclose every focused/integration/package/type check run or unrun, and confirm `multi-domain-storefront-composition` and `kit-storefront-composition-seam` consume this seam without a fallback before marking the change complete.
