## Completed local implementation

- [x] Resolve the runnable bare-metal buyer, dedicated storefront, POOLS-7 selected-site fulfillment, shared one-domain composition, persistent buyer identity, and expanded hosted-consumer prerequisites; record the accepted inventory and ownership boundaries in `design.md` without copying VM or hosted wire/runtime code.
- [x] Add strict versioned bare-metal hosted option, buyer-demand, accepted-binding, public physical-result, lease-ready-evidence, and lifecycle carriers in `domains/bare_metal/src/arkhai_bare_metal/`; bind canonical digests and reject unknown fields, buyer-invented seller/site/resource/executor facts, credentials, provider data, and unrestricted connection material.
- [x] Carry typed `settlement_options` independently of legacy `accepted_escrows`, preserve trusted selected-site/resource derivation, and publish one deterministic alternative per ready `card.v1`, `us_bank_transfer.v1`, and `us_ach_debit.v1` profile with exact rate, condition, party, offer, funding, fulfillment, and access bounds.
- [x] Ship the installed `arkhai-bare-metal-buyer` contribution with strict secret-free TOML, authenticated registry discovery, exact option selection and negotiation, complete signed accepted-state recovery, hosted funding authorization/start/resume/status/reclaim, physical status/result/access/teardown, and one `complete` command; refresh the recorded publisher subject and never accept seller/site/resource identities on recovery commands.
- [x] Compose `fiat.stripe.v1` and Alkahest through the existing `SettlementConfigurationRegistry`, `SettlementRuntime`, canonical `market_settlement_runtime.HostedSettlementRouteService`, hosted adapter facade, and injected `BareMetalHostedDomainCallbacks`; hosted-only storefront startup constructs no wallet, chain, RPC, EVM, or Alkahest client.
- [x] Reconstruct and persist one immutable server-authoritative accepted binding from the signed negotiation thread, trusted listing, exact selected option, accepted plan, canonical parties, and physical feasibility; expose hosted accepted-state start/status/reclaim plus signed content-addressed evidence resolution through the shared authenticated route/service boundary.
- [x] Gate Capacity Reservation, selected-site scheduling, fulfillment dispatch, access grant, and evidence on authoritative funding; derive stable reservation/fulfillment identities, resume equivalent retries and restart exactly once, and fail closed rather than substitute another site/resource/executor or conflicting result.
- [x] Produce credential-free portable lease-ready evidence binding agreement, obligation, canonical buyer/seller/claimant, site/resource, allocation/reservation/lease/fulfillment, access mode/readiness/expiry, condition anchor, and public result digest; resolve the exact canonical evidence with marketplace-signer proof before collection.
- [x] Persist monotonic hosted/physical lifecycle references and results in the bare storefront database and ordered migrations; preserve existing Alkahest/verified state, reject accepted-binding rewrites, block reclaim after access evidence or possible collection, converge funding return/loss separately from physical revocation/teardown/quarantine/release, and keep post-collection loss incident-only.
- [x] Add focused domain publication/contract and buyer composition tests and retain the existing storefront lifecycle, persistence, migration, site-client, fulfillment, settlement-runtime, Alkahest, and package-boundary suites as the owners of deterministic local contracts. No local fixture is attributed as Stripe or live-host evidence.
- [x] Update buyer/storefront pyprojects and entry points, root/domain wheel targets, dedicated image dependencies, `compose.bare-metal.yml`, bare-metal Helm values/schema/template/example, registry publication command, and hosted-only secret/config surfaces without editable sibling imports, Stripe SDK/provider credentials, payer bindings, or cross-role secret mounts.
- [x] Promote current behavior to permanent buyer-orchestration, market-composition, storefront-publication, settlement-servicing, physical-provisioning, deployment-state, and test-compatibility specs/architectures plus `docs/development/{ARCHITECTURE,DEPLOYMENT_AND_CONFIG,TESTING,ROADMAP}.md` and `docs/bare-metal-seller-quickstart.md`; record exact promotion headings in `design.md`.
- [x] Add the buyer-authenticated transient SSH delivery seam: provisioning returns current host/port/user only from an active access-grant result, the storefront strips coordinates from durable state/evidence and re-fetches them for the accepted buyer, and focused provider/storefront tests prove both delivery and redaction.

## External release qualification blockers

- [x] Verify the exact independently signed hosted producer manifest, client wheel, service image, OpenAPI/schema/conformance set, SBOM/provenance, source commit, and trusted workflow/repository attestations. The protected preflight pinned hosted release `v0.2.1`, image digest `sha256:c7f70fb4755db1010c6a6e6731e7da7fc7a5d7f464d9577a1bed4a0826908f6c`, client wheel digest `sha256:0d57c91249855798d6485b8850e4a7470033a896b721460123d9b9dc6cd57a07`, producer workflow run `31911858874`, and source commit `bb68a08bf717d13326641be4405831bc23229c95`.
- [ ] Execute protected `card.v1`, `us_bank_transfer.v1`, and `us_ach_debit.v1` Stripe test-mode flows including interactive/delayed funding, off-session action fallback, authoritative retrieval, return/loss, collection/reclaim, and signed reporting. **Blocked:** three independently signed protected card reports reached the real connected account and loopback webhook but stopped at `payer_profile_unavailable`. The code is a stage-level classification: the protected marketplace subprocess exited while loading its ephemeral durable buyer profile, creating or reusing the authority-scoped hosted payer profile through the released client, or persisting the opaque binding, before it returned a payer fixture. It is not a funding decline, and the sanitized report currently discards the child exception. **Diagnostic obtained** by `openspec/changes/archive/2026-08-19-separate-protected-run-release-gate`, which made the body runnable outside a protected run and disclosed the discarded child exception to a development run only. Nothing raised `payer_profile_unavailable` where it could be read: the staged bridge caught every startup failure and returned exit 2 with nothing written anywhere, so the code was accurate and empty. With the cause visible, one interactive `card.v1` collection lane against the real test account passes payer profile and browser consent and stops later, at `funding_authorization`, where hosted materialization returns no `settlement_ref` — a marketplace-side question that belongs to this change. That development run does not qualify any lane: complete the matrix under a protected run. **Settlement identity resolved** by `openspec/changes/archive/2026-08-19-name-hosted-rejection-reasons`: the released authority refuses every escrow a storefront opens while its storefront caller allowlist is unset, and nothing ever set it. With the harness stating which storefront it built, one interactive `card.v1` lane materializes a real escrow against the real test account and advances past funding authorization, stopping at buyer status polling — `GET /api/v1/settlements/{ref}` is refused as malformed or legacy response authentication, which belongs to this change. **Throughput constraint:** after repeated automated sessions the provider answers Checkout with an interactive hCaptcha, which the harness detects and classifies as an environment failure at payer setup. Every interactive lane depends on not being challenged, so the matrix cannot be run back to back and a run that reports `chromium_unavailable` at that stage is not evidence of a defect. Remaining: diagnose that response authentication, then saved-card/off-session fallback, bank-transfer, ACH success/failure/return, collection/reclaim, restart, and loss lanes. The successful real `us_bank_transfer.v1` whole-host lane does not substitute for that matrix. **Development lanes now passing end to end** against a locally built 0.3.0 authority and the real Stripe test account, which qualify nothing but do narrow what is left to find: `us_bank_transfer.v1` interactive collection; `us_ach_debit.v1` saved-instrument collection and off-session collection, both headless through `payer-direct-instrument-setup.v1` with no Checkout page at all. Each charges, transfers to the connected account with a matching source transaction and transfer group, satisfies the condition, fulfils, and collects on both sides. Two constraints found while running them, neither a defect: an ACH lane needs the authority's ACH availability delay shortened, because the released four-day policy is correct and unreachable in a test run; and `card.v1` interactive is refused by the provider's hCaptcha on this account, reported now as `chromium_unavailable` at `browser_checkout` rather than as a funding timeout three steps later. The ACH return and post-collection-loss lanes stop at `loss_boundary`: nothing implements `induce_test_ach_return`, so the harness refuses rather than pretending, and driving them would mean creating the instrument against a failing test account number rather than the succeeding one. **Matrix scope narrowed 2026-08-25:** the `ach_return` and `post_collection_loss` lanes are withheld rather than attempted, and the harness now reports them as `excluded` with `loss_projection_unimplemented` instead of stopping at `loss_boundary`. The reason is not the test hook it looked like. A return reaches the authority as a Stripe dispute, which is the only authoritative loss `list_terminal_losses` reads, and the lane's funding instrument now uses the documented disputed test account rather than a declining one, which never funds at all. What is missing is on this side: `kit/settlement-runtime` carries no funding-loss handling, so nothing observes an authoritative loss or blocks fulfilment on one, and the `fulfillment_blocked` projection both lanes assert does not exist. Building it is servicing behaviour shared by every domain, so it belongs in its own change; the gap it names is a real one, because a disputed funding does not currently stop delivery. The remaining eleven lanes qualify independently. Interactive `card.v1` lanes are likewise excluded when unattended, reported as `interactive_lane_not_automated`, and driven by `attended-lanes.sh` with someone at the browser; a visible window is what avoids the provider's CAPTCHA, so this replaces the `chromium_unavailable` misreport. **First attended run, 2026-08-25:** interactive `card.v1` collection passed end to end for the first time in any mode, confirming the CAPTCHA reading. The same run reached the `decline` and `insufficient_funds` lanes, which had never been reachable, and both failed as `product` at `provider_inspection` with `provider_invariant_failed`. The harness assumed a refused card leaves a failed PaymentIntent carrying `last_payment_error`. It does not: checked directly against the account, a refused Checkout session stays `open`/`unpaid` with a null `payment_intent`, and no intent or charge for the operation exists anywhere. Only the one completed session has an intent. The provider-side proof of a refusal is therefore the absence of every funding artifact, asserted account-wide rather than on the session alone; which refusal the payer met is what the browser proves, by waiting for Checkout's own error and refusing a page that left for the success URL. `inspect_payment_outcome` now asserts that shape and evidence ties the counts to the outcome. No test had covered the decline path, which is why the wrong assumption survived. The `authentication` lane is unjudged: it hit the CAPTCHA as the fourth consecutive lane of one session. **Both refusal lanes now pass** against the real test account, at `complete` with no diagnostic, reporting one Checkout session and zero intents, charges, transfers and refunds. What they do not prove is *which* refusal occurred: no PaymentIntent means no `decline_code`, so provider-side the two lanes assert an identical absence and differ only in the `outcome` each copies from its own scenario. The reason is an input to the lane -- which test card was typed -- not something observed coming back, and `_wait_for_decline` waits for any Checkout alert rather than a specific one. Checkout's message is the only place the reason is visible at all, so a lane that claims `insufficient_funds` has to read it there or stop claiming it. **The CAPTCHA reading was wrong, corrected 2026-08-26:** a visible browser does not reliably avoid the challenge. Run alone, first of its session, window showing, the `authentication` lane still reported `chromium_unavailable` at `browser_checkout` with `Stripe Checkout requires an interactive CAPTCHA`. So the earlier explanation -- headless mode, plus four lanes back to back -- does not account for it, and the three lanes that passed visibly did so without being challenged rather than because the window was shown. The deeper fault is that the harness refused a challenge an attended run exists to answer: `chromium_unavailable` misreports it besides, because Chromium was working and it was the provider that stopped. An attended run now waits for the person to answer, gives the time spent back to the challenge's own deadline, and retries a Checkout page that was held rather than refused; an unattended run fails exactly as before. Whether that clears the lane is unproven -- it needs another attended run. **Measured against a live Checkout page, 2026-08-26, and the CAPTCHA reading was wrong again -- this time at the root.** There was no challenge. hCaptcha loads its challenge frame on every submitted Checkout, button and all, and hides it in the parent document (`visibility: hidden`, `opacity: 0`, parked at `y: -9999`). `_interactive_captcha_visible` asked the frame about its own contents, and inside the frame the button is laid out and unhidden, so it answered `visible` on a page showing nothing: the hosting iframe reports `is_visible() == False` at the same instant. Only `authentication` ever tripped it, because it is the one lane that polls the detector on its happy path; `_complete_authentication` raised on its first iteration, before it had once looked for the 3DS control. So the lane has never been attempted in any mode. The detector now asks the parent, and off-screen counts as hidden however the page spells it; a live page with four hCaptcha frames present now correctly reports no challenge. What headless Chromium actually meets is a silent stall, not a challenge: driven through the harness's own `pay()`, the submit button sits at `Processing`, the page reports nothing, and no PaymentIntent is ever created -- for a plain `4242` success card exactly as for the 3DS one. Headless card Checkout cannot fund on this account at all, which is the real reason the interactive card lanes need a person, and `interactive_lane_not_automated` already withholds them. Whether the `authentication` lane passes once it can reach 3DS is still unproven: it has never got that far.
- [ ] Execute the release-qualified whole-host path against an isolated disposable physical target and observe authenticated SSH action, lease expiry/revocation, failed subsequent access, executor teardown, Capacity Reservation release, and capacity republication for each attributable profile/recovery path. The `us_bank_transfer.v1` lane completed against `gcp:yang-workspace-1:us-central1-a:3357572205426540820`: Stripe funding transferred and collected, portable evidence satisfied the condition, SSH access succeeded, teardown revoked the key, subsequent SSH failed, Capacity Reservation released, and the listing republished. **Blocked:** card, ACH debit, automatic-fallback, and failure/recovery whole-host lanes remain unqualified.

## Integration handoff

Consolidated validation passed the bare-metal domain (71), buyer (3), storefront (93), and provisioning-adapter (2) tests; shared buyer/storefront/runtime and full VM non-regression suites; provisioning unit/integration targets; changed-file Ruff lint/format checks; adopter wheel builds; buyer/storefront CLI smoke tests; Helm rendering; strict Compose interpolation; comment hygiene; and this change's strict OpenSpec validation. Repository-wide strict OpenSpec validation passes every modified permanent spec; seven unrelated active changes remain invalid. The signed hosted `v0.2.1` producer artifacts and one real `us_bank_transfer.v1` whole-host lane are now qualified as recorded above; the remaining protected and whole-host profile/recovery matrices stay blocked and are not represented by local fixtures.
