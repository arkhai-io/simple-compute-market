## 1. Prove every hard prerequisite before implementation

- [ ] 1.1 In `design.md`'s prerequisite record, resolve the missing `bare-metal-buyer-domain` to an accepted shipped buyer change/package and cite its permanent `buyer-orchestration`/`market-composition` headings plus discovery-through-result focused and integration evidence; stop here if no equivalent exists.
- [ ] 1.2 Verify `market-platform-bare-metal-10-storefront-composition` has resolved every API-affecting open question, promoted the runnable seller negotiation/settlement/fulfillment/result/teardown contract out of the active change, and shipped package, HTTP, migration, and deployment evidence; do not treat its completed checkbox count as acceptance.
- [ ] 1.3 Verify `pools-7-storefront-fulfillment-cutover` has promoted and shipped selected-site reservation/scheduling, durable fulfillment result/recovery, access/lease teardown, and capacity release contracts used by bare metal, with no legacy direct-executor production path required by this adoption.
- [ ] 1.4 Resolve `storefront-domain-parameterization` and `multi-domain-storefront-composition` to completed accepted replacements, complete `kit-storefront-composition-seam`, and prove one shared mechanism registry, buyer transport/policy, storefront routes/runtime, and domain hooks are used by VM, API credits, and bare metal without copied role orchestration.
- [ ] 1.5 Verify `consume-expanded-stripe-funding` is implemented and promoted with the exact signed producer release, persistent buyer profiles, shared hosted adapter/runtime, all three funding profiles, safe actions, migrations, deployment, and release evidence required here.
- [ ] 1.6 Record the exact accepted package/API/model/config/test file inventory supplied by tasks 1.1–1.5 in `design.md`; if any accepted interface changes this proposal's requirements, lifecycle, or later task boundaries, update and strictly validate the OpenSpec artifacts before production edits.

> Tasks 2–12 are blocked until every task in Section 1 passes. Missing prerequisites are implemented and reviewed in their owning changes, never copied, stubbed, mocked, or silently narrowed here.

## 2. Establish bare-metal hosted carriers and persistence

- [ ] 2.1 Extend the shared/domain listing projection used by `domains/bare_metal/src/arkhai_bare_metal/{storefront_adapter.py,storefront_publication.py,schema.py}` to accept independent typed `settlement_options` while keeping physical vocabulary in `BareMetalListing` and hosted alternatives out of legacy `accepted_escrows`; add canonical round-trip/rejection tests under `domains/bare_metal/tests/`.
- [ ] 2.2 Add versioned bare-metal buyer demand/authorization and accepted-binding codecs in the prerequisite-supplied buyer/domain modules for exact advertised profile, bounded off-session policy, public SSH/access input, trusted seller/site/resource/access/deadline facts, and canonical digests; reject unknown versions/fields and buyer overrides in domain tests.
- [ ] 2.3 Add the credential-free public bare-metal physical result/evidence-input codec in `domains/bare_metal/src/arkhai_bare_metal/schema.py` (or the prerequisite-accepted owning module), binding opaque reservation/allocation, fulfillment/lease, accepted resource constraints, access method/readiness, and expiry; include Physical Resource identity only for a trusted signed specific-resource offer, keep an ordinary fungible assignment internal, and reject credentials, unrestricted connection details, provider metadata, and raw executor results.
- [ ] 2.4 Map the accepted binding and monotonic hosted/capacity/fulfillment/evidence/collection/reclaim/lease/teardown references onto the shared settlement obligation/journal and prerequisite bare-metal persistence; add a versioned migration only for fields the accepted shared model cannot represent, with constraints preventing conflicting identity rewrites.
- [ ] 2.5 Add migration create/upgrade/rerun/legacy-row/conflict tests in the owning shared and `domains/bare_metal/storefront/tests/test_migrations.py`/`test_persistence.py` suites, proving existing Alkahest/verified settlement rows keep their effective behavior.
- [ ] 2.6 Remove legacy bare-metal lease/settlement carriers and adapters that the accepted shared fulfillment cutover supersedes, including chain-specific `escrow_uid` or direct executor/provider paths where no remaining Alkahest caller requires them; migrate every caller and retain no compatibility alias or dual write.

## 3. Compose hosted settlement into runnable bare-metal roles

- [ ] 3.1 Add the exact released `market_hosted_settlement`/`hosted_settlement_client` and shared composition dependencies to the prerequisite-supplied bare-metal buyer/storefront `pyproject.toml`, lockfiles, Makefiles, wheelhouse build/reinit targets, and package-boundary tests; add no editable sibling source or Stripe/provider dependency.
- [ ] 3.2 Register Alkahest and `fiat.stripe.v1` in the bare-metal storefront's shared settlement registry from `domains/bare_metal/storefront/src/arkhai_bare_metal_storefront/{runtime.py,server.py,domain_runtime.py}` (or their accepted replacements), injecting seller signer/trust, account binding, shared hosted client, settlement runtime, selected-site capacity/fulfillment clients, and domain evidence hooks.
- [ ] 3.3 Register the same mechanisms in the runnable bare-metal buyer root, injecting the selected persistent buyer profile/signer, payer-profile resolver, shared hosted buyer transport/policy, safe action presenter, run log, and bare-metal domain adapter.
- [ ] 3.4 Make hosted-only buyer/storefront construction skip wallet, RPC, chain, EVM address, and Alkahest clients; add composition/startup tests for hosted-only, Alkahest-only, both mechanisms, disabled hosted config, and missing exact authority/profile capability.
- [ ] 3.5 Replace `BareMetalSettlementService`'s verified-only special path with the accepted shared route/runtime and domain plan hooks, or delete the obsolete service after migrating its callers; preserve truthful legacy behavior until the atomic cutover and leave no endpoint claiming fulfillment from verification alone.
- [ ] 3.6 Run import/package conformance proving core/kit remain domain-opaque, bare metal imports no VM composition, and buyer/storefront/domain packages contain no Stripe SDK, webhook, provider credential, hosted service implementation, or copied wire/signature/action code.

## 4. Publish exact hosted whole-host alternatives

- [ ] 4.1 Extend bare-metal publication configuration with exact enabled profile policy, USD rate/amount mapping, seller/claimant and hosted account binding, condition/evidence resolver, and offer/hold/funding/fulfillment deadline inputs using the shared typed settlement config; reject unknown/duplicate profile IDs and secret/provider fields.
- [ ] 4.2 In the bare-metal publication adapter/service, intersect one complete trusted site/resource generation with current access capability, authoritative availability, exact hosted release/account/profile/currency/condition readiness, and compatible deadlines before emitting each deterministic hosted option.
- [ ] 4.3 Omit only the unavailable profile, close/omit hosted alternatives on stale or incoherent site/resource/offer readiness according to existing reconciliation policy, and never infer another site/resource, renew a hold, or mutate an old accepted option after republication.
- [ ] 4.4 Add focused publication tests for card, bank transfer, ACH, multiple alternatives, partial profile readiness, stale/incomplete/empty generations, site/resource identity collisions, capacity loss, unsupported access, deadline incompatibility, deterministic retries, and credential/provider-data exclusion.
- [ ] 4.5 Extend storefront HTTP/registry integration tests to prove signed `settlement_options` survive publication/discovery independently from `accepted_escrows` and a buyer cannot request an omitted profile.

## 5. Implement exact buyer selection and safe funding actions

- [ ] 5.1 Add bare-metal buyer listing display/filter/selection for exact compatible mechanism/profile, USD amount/rate, condition, access method, and deadlines through the shared settlement policy; preserve legacy Alkahest selection and reject ambiguous/nonadvertised options before negotiation.
- [ ] 5.2 Derive the signed buyer demand and hosted authorization only from the selected option plus buyer-owned profile/off-session/public-access input, and verify the accepted seller response cannot rewrite buyer/payer identity or introduce a different site/resource/seller/price/condition/deadline.
- [ ] 5.3 Persist the exact selected option/profile, accepted digests, hosted operation, safe transient action, and opaque physical result/access references in the shared buyer run log; implement deterministic resume without creating another payment, hold, reservation, or fulfillment.
- [ ] 5.4 Present shared card interaction, bank-transfer instructions, ACH mandate/pending/action, user-decline, and expiry states through the bare-metal buyer CLI/API without persisting URLs, credentials, private SSH material, provider payloads, or unrestricted connection details.
- [ ] 5.5 Verify signed portable whole-host evidence before reporting success, retrieve access material only through the accepted authenticated domain seam, and distinguish pre-collection reclaim eligibility from post-collection lease revocation/teardown status.
- [ ] 5.6 Add buyer unit/conformance tests for all profiles, opt-in off-session success and interactive fallback, delayed funding/restart, unknown profile/option, malicious seller or buyer override, invalid/no-op evidence, access-secret redaction, reclaim, and teardown observation.

## 6. Order funding, holds, and selected-site fulfillment

- [ ] 6.1 Extend the bare-metal storefront domain adapter to reconstruct one immutable accepted binding from signed demand/listing/seller terms, original billable hold, and trusted site/resource mapping before registering the shared hosted plan; reject every conflicting caller field before financial or physical mutation.
- [ ] 6.2 Compute and persist the funding deadline as the minimum of present offer, original billable-hold, hosted profile authorization, and fulfillment-feasibility bounds; never synthesize, renew, replace, rebind, or extend a bound because funding is pending.
- [ ] 6.3 Drive shared hosted materialization/authorization/action/status recovery with the exact accepted profile and operation, and assert nonfunded states perform no hold mutation, capacity commitment/scheduling, allocation, executor, lease, or access-grant call.
- [ ] 6.4 On authoritative funded state, commit the exact still-valid accepted hold or create/commit the ordinary reservation at the accepted site, schedule the accepted resource constraint, and begin one provider-neutral fulfillment with stable reservation/fulfillment identities and no site/resource fallback.
- [ ] 6.5 Poll the provisioning-owned status/result seam, validate the public bare-metal result through the domain codec, and persist only monotonic references/digests; equivalent retries resume the same allocation/provider operation and conflicting results fail closed.
- [ ] 6.6 At funding expiry, retrieve current authoritative hosted state first; if still unfunded, expire the obligation, release the original hold through its site authority, and request reclaim only through the shared exclusion check without adopting later-republished capacity.
- [ ] 6.7 Add storefront service/integration tests for pre-existing billable hold, no-hold admission, every nonfunded profile state, deadline minimum/equality, delayed webhook, hold expiry/release, funded commit, selected-site refusal, cross-mode conflict, restart after reservation/allocation/dispatch, and duplicate/conflicting requests.

## 7. Publish real access-ready evidence and collect

- [ ] 7.1 Update the bare-metal provisioning adapter/result normalization to produce the accepted credential-free public physical result only after committed allocation, live lease/fulfillment, completed access grant, and supported buyer-ready access method all agree on one resource and expiry; project Physical Resource publicly only when signed accepted terms selected that exact resource.
- [ ] 7.2 Reject executor success lacking an authoritative access grant, mismatched resource/allocation/lease identifiers, expired readiness, unknown action/result versions, raw provider fields, secrets, private topology, or unrestricted connection details; add adapter/service tests for each rejection.
- [ ] 7.3 Compose `FulfillmentPublicationRequest` through `market_hosted_settlement` with the accepted agreement/obligation, canonical buyer/claimant, condition anchor, fresh fulfillment UID, and canonical physical-result digest; keep access material outside the hosted payload.
- [ ] 7.4 Accept only the signed authoritative hosted evaluation for the exact condition/evidence mode, then collect the same obligation once; no local physical boolean, no-op result, direct browser/provider state, or different fulfillment UID may authorize collection.
- [ ] 7.5 Persist portable evidence/public result and collection operation references for restart, return only normalized domain result/status to the buyer, and add tests for duplicate evidence delivery, replay across obligations, invalid signer/anchor/resource, unknown acknowledgement, and exactly-once collection.
- [ ] 7.6 Add an integration scenario across real shared storefront settlement runtime, hosted client test double at the provider port, site/provisioning services, and bare-metal adapter proving funding precedes physical effects and real access-ready evidence precedes collection.

## 8. Implement failure, reclaim, revocation, and teardown recovery

- [ ] 8.1 Route funded selected-site refusal, capacity loss, terminal scheduling/provider/access failure, invalid evidence, fulfillment expiry, and provider-neutral funding return into noncollectible recovery while preserving exact hosted and physical operation identities; a return before physical work starts prevents it, and a return after allocation/access but before collection stops further effects and starts convergent revocation/teardown or quarantine.
- [ ] 8.2 Enforce shared reclaim exclusion against successful lease/access evidence and any possible collection reservation/effect; on unknown collection acknowledgement, freeze capacity release/reclaim and reconcile the same financial operation rather than issuing a contradiction. Pre-collection funding-return recovery MUST block collection, converge physical cleanup, and delegate financial return/reclaim to the hosted authority; post-collection loss remains incident-only.
- [ ] 8.3 After ordinary collection, or after a pre-collection funding return has blocked collection, drive the applicable lease/access revocation and provider-neutral teardown APIs, then release allocation/Capacity Reservation and allow republication only after authoritative teardown success; financial recovery and physical teardown remain independently journaled.
- [ ] 8.4 Keep unknown, retryable, or terminal revocation/teardown outcomes quarantined and operator-repairable through the provisioning convergence worker; expose bounded references/status without credentials or provider payloads and never infer a provider action or capacity release from financial state.
- [ ] 8.5 Add deterministic restart/concurrency tests after funding, funding return, hold commit, scheduling, provider dispatch, access grant, evidence publication, collection, reclaim, revocation, teardown, and release, proving no duplicate payment/host/access action, no premature capacity publication, and post-collection loss never reclaims.
- [ ] 8.6 Add storefront/site/provisioning integration tests for funding return before access, funding return after access but before collection with blocked collection and convergent teardown/reclaim, terminal physical failure with eligible reclaim, successful access with collection, post-collection loss as incident, unknown collection, lease expiry, revocation retry, teardown failure quarantine, operator resume, final release, and cross-mode availability restoration.

## 9. Migrate configuration and deployment topology

- [ ] 9.1 Extend shared and bare-metal settings/templates with exact mechanism order, funding profiles, role signer/trust, owner-restricted buyer profile-store path and hosted authority/environment scope, seller public account binding, hosted authority/release, USD pricing, condition/evidence resolver, deadlines, site/provisioner trust, and access-delivery trust. Resolve the opaque payer binding only from buyer profile metadata at runtime; never place it in templates/storefront config. Make migration deterministic and reject legacy ambiguity/unknown fields.
- [ ] 9.2 Add role-scoped Helm/compose/bare-metal deployment rendering for the runnable buyer, storefront, shared hosted client/runtime, site authority, compute provisioner, and bare-metal adapter/executor, pinning exact wheels/images/manifests and mounting no wallet/RPC secret for hosted-only roles.
- [ ] 9.3 Add preflight/readiness checks for exact public identities, authority environment/capabilities, seller account/profile/condition readiness, site/provisioner/evidence bindings, schema versions, and artifact pins; reject cross-role secret mounts and keep health/logs/reports credential/provider-ID free.
- [ ] 9.4 Update wheelhouse, image, package review, and reinit targets so changed internal distributions are rebuilt and explicitly upgraded from `.dist`; prove disabling bare-metal hosted or either role leaves no dangling waits, services, routes, or Secrets.
- [ ] 9.5 Add config migration and deployment render tests for hosted-only, Alkahest-only, both, all profiles, invalid authority/account/site/evidence bindings, wrong secret ownership, missing artifact pin, upgrade/rerun, disable-before-mutation rollback, and post-mutation compatible-release retention.

## 10. Add focused, integration, and protected evidence

- [ ] 10.1 Run the domain, buyer, storefront, shared settlement runtime, site, fulfillment, provisioning, bare-metal adapter, package-boundary, typing, migration, and deployment suites that own the changed contracts; fix every attributable failure and record exact commands/results in the change's implementation record.
- [ ] 10.2 Extend the bare-metal E2E driver/fixtures to use only public buyer/storefront APIs, signed role identities, ordinary hosted client/service network calls, selected site/provisioner APIs, and authenticated access delivery; remove any fixture that hard-codes a resource, fakes fulfillment success, or bypasses the shared runtime.
- [ ] 10.3 Add a protected `make hosted-stripe-bare-metal-test` target (or the repository's accepted equivalent) that consumes the exact signed hosted release and marketplace/site/provisioner/bare-metal artifacts, uses real Stripe test mode and an isolated disposable physical target, and fails before mutation on any identity/artifact/capability mismatch.
- [ ] 10.4 Prove the ordinary whole-host servicing sequence end to end for each `card.v1`, `us_bank_transfer.v1`, and `us_ach_debit.v1`: discovery, negotiation, exact profile selection, rail-specific delayed/interactive funding, no pre-funding commit/allocation, selected-site fulfillment, authenticated host action, portable evidence, collection, restart, expiry/termination, access failure after revocation, teardown, release, and capacity republication. A physical target may be reused serially only after authoritative teardown/release, and each outcome remains attributed to its exercised profile.
- [ ] 10.5 Add protected failure/recovery scenarios for funded selected-site refusal or provisioning failure before access, funding return before access, funding return after access but before collection with blocked collection and physical teardown/quarantine plus hosted recovery, post-collection loss as incident-only, eligible reclaim, restart after one financial and one physical mutation, teardown failure/quarantine, and resumed release; preserve Alkahest as an independent lane.
- [ ] 10.6 Emit the signed report with marketplace repository/commit/artifacts, hosted manifest/client/image/source/workflow, site/provisioner/adapter artifacts, protected workflow run, normalized resource/operation references, and stage outcomes, excluding credentials, Stripe/provider IDs, raw access data, and unrestricted logs.

## 11. Promote durable documentation and prepare cutover

- [ ] 11.1 Promote the accepted prerequisite gate, shared one-domain composition, and hosted/physical authority boundaries to `openspec/specs/market-composition/{spec.md,architecture.md}` and `docs/development/ARCHITECTURE.md`; remove current-state claims that bare metal lacks a runnable composition only after the protected contract is true.
- [ ] 11.2 Promote profile/site/resource/deadline publication, immutable accepted binding, exact buyer selection, safe actions, resume, and real-access success to `openspec/specs/storefront-publication/`, `openspec/specs/buyer-orchestration/`, and the accepted bare-metal buyer/seller documentation.
- [ ] 11.3 Promote hold-versus-commit ordering, funding expiry retrieval, selected-site fulfillment, portable evidence, reclaim exclusion, collection, independent teardown/quarantine/release, and recovery to `openspec/specs/settlement-servicing/` and `openspec/specs/physical-provisioning/` spec/architecture files.
- [ ] 11.4 Promote exact artifacts/topology/config/secret ownership, migrations, cutover/rollback, focused ownership, protected real-host evidence, and current roadmap state to `openspec/specs/{deployment-state,test-compatibility}/`, `docs/development/{DEPLOYMENT_AND_CONFIG,TESTING,ROADMAP}.md`, and `docs/bare-metal-seller-quickstart.md` plus the accepted buyer/operator runbooks.
- [ ] 11.5 Complete the `design.md` promotion record with exact permanent headings, verify all permanent documents describe current state rather than announcing the change, and run OpenSpec strict validation before deployment qualification.

## 12. Validate release and close the implementation plan

- [ ] 12.1 Run focused repository checks, packaging/wheelhouse/image checks, type checks, migration tests, bare-metal Alkahest regression, and repository-wide strict OpenSpec validation; run the hosted repository's exact release/client/service checks required by the pinned manifest and disclose any genuinely unavailable external check without substituting evidence.
- [ ] 12.2 Execute startup and public-API smoke tests for hosted-only and dual-mechanism bare-metal buyer/storefront plus site/provisioner workers, then run the protected whole-host target and verify its signed report/manifest identities and access-revocation/teardown/capacity-release outcomes.
- [ ] 12.3 Perform plan closeout: run `make check-comment-hygiene` and resolve every match; review every touched Python import against `AGENTS.md` and `docs/development/ARCHITECTURE.md` dependency layers (including `TYPE_CHECKING`) and verify permanent documentation/current roadmap plus the completed `design.md` promotion record; then compress completed `tasks.md` history exactly as required by `openspec/README.md#plan-closeout-requirements` only after all applicable focused, integration, packaging, typing, deployment, protected evidence, and strict-validation results are recorded.
