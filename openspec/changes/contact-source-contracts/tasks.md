# Implementation plan

- [x] Inspect ticket, frozen contract, source authority and engineering guidance; preserve prior completed history.
- [x] Confirm clean lane and inherit reviewed local prerequisites; record base in external review report.
- [x] Add `ContactText` in `kit/contact-exchange/src/market_contact_exchange/delivery_contract.py`, preserving map and v2 semantics; add strict eligible options in `source_contract.py` and explicit profile/option eligibility in `settlement_config.py`.
- [x] Add declaration/context/general-file carriers and the bounded no-I/O bytes parser in `domains/bare_metal/src/arkhai_bare_metal/contact_contract.py`; update `schema.py` only for strict no-physical-authority declarations with old serialization preserved.
- [x] Implement `domains/bare_metal/storefront/src/arkhai_bare_metal_storefront/contact_context.py`; wire actual acceptance in `negotiation_service.py`, strict admission in `sqlite_client.py`, and accepted integrity checks in `introduction_routes.py`. No old plan rewrite.
- [x] Add source-named contract/vector tests and real SQLite acceptance/restart/refusal evidence. Exercise production review/finalization drift refusal. Publish exact JSON shapes, hashes, field/provenance/compatibility tables and downstream file plan in `contract.md`.
- [x] Build/reinstall internal wheels and verify source/wheel/installed parity. Retain separate evidence for focused typing and the six pre-existing expanded-role typing errors; bare-metal packages have no pre-existing typing marker.
- [x] Prepare and independently review permanent-document proposals; validate promoted paths and retain the existing capability-index links.
- [x] Harden declaration Python inputs in domain `schema.py`/`contact_contract.py` for every supported Mapping and strict string identifiers, with direct boundary regressions in `domains/bare_metal/tests/test_contact_declaration_listing.py` and no-write admission regressions in storefront `tests/test_contact_context.py`. Historical coercion/serialization and existing vector shapes are preserved.
- [x] Place authoritative projection loading, JSON decoding and domain validation within the fixed contact-provenance error boundary in storefront `negotiation_service.py`; extend `tests/test_contact_context.py` with corrupt projection and database-outage regressions. Database outages remain distinct; invalid input persists no negotiation or obligation.
- [x] Rebuild/reinstall affected wheels and rerun complete installed-wheel suites: 126 domain, 263 storefront and 170 contact pass without skips; Bun 63 pass. Source/wheel/installed parity covers 54 files; existing schema/vector snapshots remain unchanged. Six-module typing passes; expanded typing retains six baseline errors. Repository-wide OpenSpec retains 12 unrelated failures (72 pass); neither check is reported globally green.
- [x] Independently review source and permanent proposals before commit. Strict Mapping/identifier admission and safe projection-error findings are corrected and reviewed.
- [x] Closeout: comment hygiene and touched import placement/dependency direction pass. Promote normative behavior and architecture, reconcile roadmap current state and downstream ownership, update campaign status, and retain existing capability links. Parent reruns the installed-wheel suites, Bun, parity, scoped typing and hygiene successfully. All three promoted specs and the scoped change pass strict OpenSpec validation; requirements are inside each main Requirements section. Public source completion does not qualify publication, delivery or activation.
