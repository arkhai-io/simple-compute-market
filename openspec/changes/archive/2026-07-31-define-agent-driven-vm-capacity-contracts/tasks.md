## 1. Freeze the public v2 contract surface

- [x] 1.1 Record the current schema-v1 scenario/finding fixtures and validator behavior in focused compatibility tests so historical behavior remains attributable to its pinned ref rather than silently reinterpreted.
- [x] 1.2 Replace `tools/issue-discovery/schemas/capacity-scenario.schema.json` with the closed, mode-neutral schema-v2 VM contract separating listing topology, independently assignable GPUs, actor/load counts, request choices, outcomes, and retry authority while rejecting evidence/admission fields.
- [x] 1.3 Add closed schemas under `tools/issue-discovery/schemas/` for profile-stage bindings, capacity role plans, substantive role receipts, frozen actions/results, the pre-Q0 evaluation policy, typed reference policy, buyer-frontier receipt, and independent capacity results; define exact artifact-family boundary/trigger combinations without secret-bearing or executor-local fields.
- [x] 1.4 Replace `tools/issue-discovery/schemas/capacity-finding.schema.json` with the closed finding-v2 occurrence contract: immutable `finding_id`; no producer `fingerprint`; exact SCM/scenario/profile/result identities and hashes; the complete destination, working/upstream/inbound/reconciliation authority; result-projected durable correlations and observed fault/witness; exact `{path, sha256}` evidence objects; and cleanup-gated filing readiness.
- [x] 1.5 Add schema fixtures and negative tests for unknown keys, missing typed-result or authority fields, unresolved placeholders, private identity fields/values, malformed digests/timestamps, duplicate request/evidence identities, changed durable projections, all twelve fault categories, expected-refusal/frontier-stop exclusion, and cross-variant fields.

## 2. Implement pinned scenarios and profiles

- [x] 2.1 Refactor `tools/issue-discovery/src/issue_discovery/capacity.py` so in-memory semantic validation is distinct from a pinned Git-backed scenario resolver that rejects absolute/escaping paths, symlinks, non-regular or untracked entries, hidden worktree drift, wrong refs, schema failures, and digest mismatch.
- [x] 2.2 Replace the current v1/G2 files under `tools/issue-discovery/config/capacity/` with the exact mode-neutral envelope: every integer B1…B8/S1/G1 shape, serialized reuse A/B, B2/S2/G1, and B4/S2/S3/S4/G1; delete unqualified G2 files rather than retaining them as current authority.
- [x] 2.3 Add a versioned profile-stage registry that reuses pinned mode-neutral shapes while encoding exact `execution_boundary`/`actor_trigger`, exact `observer-probe` (`readiness`/`none`) and `b1-s1-g1-reference` (`real-reference`/`controller-driven`) identities with a non-counted controller distinct from O1, the five-row qualification matrix, the exact dynamic measured order, optional B3/B5/B6/B7/S3 refinement stages, the observer's scenario-null binding, and the pre-Q0 freeze.
- [x] 2.4 Enforce exact qualification and measured order/O/B/S/H/L/R/G counts, unordered success/refusal cardinalities, deterministic integer bisection, one-dimension below/at/above retention, B4 seller admission, and seller-frontier receipt fencing; reject missing/extra/duplicate stages, predesignated winning buyers, post-Q0 drift, or a G2 row.
- [x] 2.5 Correct semantic validation so expected simultaneous success is bounded by independently assignable GPUs rather than listing count, seller distribution matches distinct logical sellers/listings, every request targets one logical selection, and the portable chain binds a one-to-one private runtime-listing map through typed `runtime_binding` without exposing live IDs.
- [x] 2.6 Update the scenario CLI and runner entry points to require or resolve an exact SCM ref and known relative path for authority-bearing validation/hash output while retaining explicitly named in-memory helpers for tests.
- [x] 2.7 Extend `tools/issue-discovery/tests/test_capacity.py`, `test_cli.py`, and temporary-Git fixtures to cover the exact UTF-8/sorted/compact/one-newline canonical algorithm, non-finite rejection, tracked object modes, symlinks, traversal, hidden drift, ref mismatch, mode-neutral reuse, exact G1 profiles, measured/qualification separation, seller topology, frozen refinement, and G2 rejection.
- [x] 2.8 Expose Git-pinned profile-registry and profile-stage validate/hash operations through the public CLI and runner, returning exact closed semantic projections and all applicable canonical/raw digests so private orchestration consumes public policy without importing or reimplementing it; cover registry, standalone mock, nullable scenario, mismatch, and parser/runner parity cases.

## 3. Model substantive roles and agent-triggered actions

- [x] 3.1 Implement public role-plan and role-receipt validators/canonical digest helpers in the dedicated `capacity_roles.py` module beside `capacity.py`, including exact SCM/instruction and scenario/profile-stage authority, canonical SHA-256 of the nested closed role-specific plan as the single acyclic referent for `prepared_authority_sha256`, non-secret isolated identity, exact per-wave release/policy authority (or null-policy standalone mock/probe authority), role-specific preparation, ordered lifecycle timestamps, and release/observation-barrier liveness.
- [x] 3.2 Implement the closed `{method, domain, value}` privacy-preserving binding type, distinct service and per-listing runtime maps, closed independent-oracle-authority and portable actor-capability referents, and frozen-action/one-shot result validation. Bind the complete role plan, exact prepared intent digest, logical selection, typed private runtime and concrete-payload bindings, sanitized portable VM/KVM/Ansible payload bytes, pinned wrapper, release/policy, successful attempt one, and expected result schema/oracle authority. Reject raw private-identifier hashes, wrong domains, changed plan/action/mapping/bytes/capability, duplicate release, wrapper substitution, early exit, or retry; atomically consume the release with the first terminal result even when rejected, make typed retry/duplicate rejections preserve their attempted counts, make every terminal result bind the action digest before computing its own digest, and keep real Codex-process capability authentication private.
- [x] 3.3 Add CLI/runner commands that validate and canonical-hash each portable role/action artifact without consuming private credentials or executor-local paths.
- [x] 3.4 Add fixtures proving substantive buyer, seller, host-operator, and observer paths and negative tests for readiness-only receipts, controller-synthesized evidence, duplicate identities, premature exit, malformed timestamps, changed refs/instructions/actions/mappings, unauthorized retries, a seller-plan/H1 topology mismatch before release, seller-receipt/plan topology drift, and leaked personal host/GPU identity.
- [x] 3.5 Add aggregate actor-set validation for exact declared cardinality, overlapping process lifetimes, a pre-release concurrency-policy digest, independently observed common-release monotonic offsets and clock evidence, bounded request/publication skew without truncation, distinct service/listing bindings, global action/result/release uniqueness, no controller-side queue/throttle, and exact equality between every seller plan's typed topology authority and the sole H1 plan; reject a disjoint seller authority before release and prove serial B2/B4/B8 execution cannot satisfy a concurrent stage.
- [x] 3.6 Require buyer receipts to prove install/build, wallet/SSH preparation, endpoint/balance/listing checks, exact request ownership, and successful guest SSH/resume plus the pinned compiled CUDA vector-add marker/checksum correlated to `fulfillment_id`; require seller publication ownership and exact repetition of its plan's H1-shared topology authority, plus host-operator G1/baseline/KVM/Ansible/cleanup ownership and result/H1 topology equality, using only typed outcomes and public digests.
- [x] 3.7 Add the pinned public CUDA wrapper/source and deterministic tests, plus a capture-only composition bound to the standalone tracked `b1-s1-g1-mock` profile-stage record proving seller/buyer portable action and capability composition against a mock boundary with an empty live-resource ledger and crash-recoverable single-emission record, while explicitly denying private process authentication, registry admission, the real scenario oracle, and any capacity claim.

## 4. Implement independent VM outcome and reuse oracles

- [x] 4.1 Implement exact closed `vm-succeeded`, `capacity-refused`, and `fault` variants; define the closed normalized public `deal_reference`, keep commercial identity storefront-owned, and validate success joins through reservation, fulfillment, reservation-keyed Settlement Record and typed selected-resource proof, provisioned output, real KVM/Ansible whole-GPU passthrough, pinned compiled CUDA exercise, and teardown while keeping allocation/job IDs optional and avoiding a fabricated buyer-visible Settlement Resource ID.
- [x] 4.2 Implement `capacity-refused` only from independent capture of the final escrow-scoped atomic call under exact deal-hash/campaign-clock/eligible-site base authority and the closed per-site attempt truth table; require exact-once `routine-reservation-null` coverage, null durable capacity/fulfillment/output IDs, terminal commercial/failure-policy compensation, and zero active residue; preserve base-valid partial observation as `atomic-refusal-incomplete`, prove `capacity_hold_unavailable` alone is nonterminal, and classify generic, uncompensated, policy, unknown, or elapsed-at-or-beyond-boundary timeout outcomes as faults.
- [x] 4.3 Derive maximum half-open whole-GPU VM overlap independently; when it exceeds independently assignable GPUs, preserve every request observation, derive `double-allocation`, fail correctness, and retain the row as issue-discovery evidence regardless of actor-reported success or expected counts.
- [x] 4.4 Implement serialized-reuse validation requiring a buyer-frontier receipt only for measured reuse while qualification carries null frontier authority; require measured A to match the frontier topology and B to preserve A's topology in both measured and qualification modes, plus correct teardown/baseline, exact A lineage, a shared logical request slot but distinct result/commercial/durable identities, and SLO-independent equivalence.
- [x] 4.5 Freeze the evaluation policy and post-evaluation/pre-release typed reference policy with exact O1/H1 plans and schedule; implement real-only result boundary/trigger combinations, one private campaign policy, exact campaign-clock propagation, exact reference receipts, and the five frontiers.
- [x] 4.6 Implement exact buyer search/refinement, a typed frontier binding the one topology authority shared by every buyer result before measured reuse, exact frontier reasons, `progression_ready_at` lineage, topology equality across frontier/reuse-B/prior/current seller results, reuse-B H1-derived seller admission, and lower-bound/no-clean results.
- [x] 4.7 Add positive and negative oracle tests for success correlation; exact O1 outcome/cleanup seals; independent timing including equality at the terminal-timeout boundary; the post-evaluation/pre-release reference policy's exact H1/O1/teardown plans, release, schedule, and clock plus the result's exact controller proof and receipts; final per-site atomic refusal plus the base-authority/truth-table and partial-observation cases; aggregate null after swallowed site errors; nonterminal soft holds; all typed faults; uncompensated outcomes; wrong logical/runtime binding; missing durable identity; optional diagnostics; preserved double allocation; negative generator observations; qualification-null versus measured-required frontiers; shared logical reuse slot with distinct lifecycle identities; progression fencing; H1-derived seller admission and count-inflation rejection; a seller-plan/H1 topology mismatch rejected before release, a seller-receipt/plan mismatch, a result/H1 topology mismatch, mixed-topology buyer frontier, changed frontier topology, measured reuse-A/frontier mismatch, measured or qualification A/B topology drift, and seller/frontier/reuse-B/prior topology drift; frontier uniqueness/reasons/lower bounds; malformed inputs; closed context-manifest and CLI/runner parity; and raw private-identifier digest rejection.
- [x] 4.8 Add exact cleanup partitions, zero residue/locks, H1 agreement, O1 cleanup/lifecycle/clock sealing, derived `progression_ready_at`, and next-stage fencing.

## 5. Bind findings to durable VM fulfillment

- [x] 5.1 Implement immutable occurrence identity and SCM-owned fingerprinting as `capacity-<sha256>` over `scm.capacity.finding-fingerprint.v1\0` plus the exact closed semantic preimage; admit the ten request fault categories plus `double-allocation` and `unexpected-outcome`, while excluding expected proven refusal and frontier-stop meta-findings.
- [x] 5.2 Require one fully reconstructed `ValidatedCapacityResult` and its exact artifact; enforce exact scenario/profile/result authority, exact affected-request durable projections, independently derived double-allocation witnesses, cleanup-gated readiness, and redaction-as-rejection.
- [x] 5.3 Verify every evidence object's raw bytes below exact immutable `evidence/` within one explicit evidence root, with exact limits of 1 MiB per artifact and 4 MiB in aggregate; reject the bare namespace and all harness-managed outputs, and permit no root guessing, symlink, traversal, non-regular file, mutation, digest drift, portable field/pattern/sentinel match, or public replacement/redaction. Apply a bounded semantic fixed-point privacy closure to recursively walked JSON keys/strings, decoded JSON/YAML scalar spellings and aliases/fragments, CommonMark-visible HTML-entity/backslash projections, Unicode NFKC projections, and combining-mark-stripped NFKD projections; reject Unicode Default_Ignorable, `Cf`, and disallowed `Cc` controls while also scanning their stripped projections. Make ingest use the explicit run directory as that root and leave the runtime exact-value denylist to private infrastructure before export.
- [x] 5.4 Enforce exact destination/classification/branch mappings and first-parent authority: nullable inbound only for direct first-parent upstream lineage, otherwise an exact two-parent inbound merge on the working first-parent chain whose second parent is the exact upstream ref.
- [x] 5.5 Replace the public finding example with a fully sanitized v2 fixture and cover `public-product`, `public-harness`, `private-orchestration`, and `environment-provider` at their only valid destination/branch mappings.
- [x] 5.6 Update finding validate/hash/ingest and runner/CLI paths to reconstruct the same complete result context; authenticate and hold one current-user-owned mode-0700 run-root descriptor across ingest/replay, serialize compliant writers on both that root-directory descriptor and the authenticated single-link mode-0600 persistent lock, and perform private traversal/read/write through descriptor-relative held authority with ancestor/destination revalidation. Maintain mode-0600 create-once source/index/body files in current-user-owned mode-0700 directories keyed by `finding_id`, deterministic append-only source/index ledgers, and an atomically rewritten manifest that preserves unrelated run metadata while enforcing one exact common run/branch/ref/reconciliation authority and a sorted immutable occurrence projection. Use authenticated hard-link no-replace creation and Linux `renameat2(RENAME_EXCHANGE)` replacement, recover only exact safe temporary peers across pre-publication and post-link crash windows, fsync every publication/recovery directory, and state the explicit crash/non-malicious/compliant-writer threat boundary for pathname syscalls. Recover every valid durable prefix, make identical reingest a no-op, reject changed bytes/authority under one ID or run directory, and retain a new ID with the same fingerprint as a separate occurrence only within that run authority.
- [x] 5.7 Generate only an exact-SHA marker-free human occurrence payload, rendering agent-controlled summary/stable-signature/expected/actual prose as literal four-space-indented CommonMark blocks; generate a capacity-v2 guarded-publication candidate and one exact idempotent local `detected` event in a separate canonical append-only lifecycle ledger. Enforce current-user/type/link/mode and durable atomic-publication rules for every private artifact; keep validation, mock capture, ingest, replay, and promotion preparation-only with no live market/cloud/VM/KVM/Ansible/GPU/wallet/GitHub/cleanup side effects; reject capacity v2 from legacy create (including force/dry-run), transition, and fix-proposal surfaces before write, `gh`, or subprocess while preserving non-capacity behavior.
- [x] 5.8 Extend capacity/finding/issue/runner/CLI tests for reproducible SCM-owned fingerprints across new results/refs, semantic fingerprint separation, all twelve fault variants, exact result/correlation binding, same-defect/new-occurrence behavior, first-parent/inbound authority, readiness, exact evidence budgets, representation-aware JSON/YAML/CommonMark/NFKC privacy and Unicode-control rejection, literal prose rendering, immutable collision handling, manifest preservation/common-authority conflicts, descriptor-rooted ancestor/root/destination observable-drift rejection, dual-lock identity/serialization, write/replace temporary cleanup, authenticated one-link and post-link same-inode crash recovery under the compliant-writer boundary, unsafe-temp refusal, crash recovery through manifest/lifecycle suffixes, one immutable final replay snapshot, marker absence, detected-only lifecycle, preparation-only empty-resource behavior, and guarded-publication ownership.

## 6. Promote permanent behavior and operational truth

- [x] 6.1 Create `openspec/specs/capacity-testing/spec.md` with the verified normative contract and `openspec/specs/capacity-testing/architecture.md` with the scenario/topology, actor/action, oracle, evidence, public/private ownership, and compatibility models.
- [x] 6.2 Synchronize exact execution-boundary/actor-trigger requirements into `openspec/specs/test-compatibility/spec.md` and add the durable rationale to its architecture.
- [x] 6.3 Preserve the executor seam by proving local Codex and future cloud/Tekton execution consume the same repository-relative commands and portable inputs/results while credentials, cancellation, native evidence, and cleanup stay private.
- [x] 6.4 Update `docs/development/ARCHITECTURE.md#testing-strategy` with present-tense public/private test authority, evidence vocabulary, and agent-triggered boundary, and add `capacity-testing` to `openspec/specs/README.md`.
- [x] 6.5 Update `docs/development/ISSUE_DISCOVERY.md` and `tools/issue-discovery/README.md` only after the implementation passes, using the exact v2 commands, profiles, compatibility limits, and publication boundary.
- [x] 6.6 Complete the design-promotion record with final stable headings and verify production code/docs do not depend on or cite `openspec/changes/define-agent-driven-vm-capacity-contracts`.

## 7. Validate, synchronize, and archive

- [x] 7.1 Run focused scenario, role/action, oracle, finding, CLI, runner, and issue tests, then the complete `tools/issue-discovery` package suite under the supported Python environment.
- [x] 7.2 Validate every tracked scenario/profile and reproduce every canonical digest; run a capture-only mock artifact chain with an empty live-resource ledger.
- [x] 7.3 Run strict OpenSpec validation for this change and disclose unrelated inherited global validation failures rather than editing them in this scope.
- [x] 7.4 Run staged whitespace, secret/credential signature, artifact-path, symlink/mode, and owner-only personal-host denylist scans; inspect exact staged paths and the final diff.
- [x] 7.5 Synchronize the verified delta into permanent specifications, mark only actually completed tasks, archive the change, rerun strict validation, and commit/push each bounded implementation outcome through the guarded one-ref procedure.

## 8. Traditional commit checkpoints

- [x] 8.1 Commit scenario/profile/schema behavior and focused tests as `fix(issue-discovery): align VM capacity scenarios with dev`.
- [x] 8.2 Commit substantive roles, logical/runtime binding, actor-owned wrappers, pinned CUDA workload, receipt aggregation, and capture-only mock composition as `feat(issue-discovery): model substantive capacity roles`.
- [x] 8.3 Commit evaluation/reference policies, durable oracles and atomic truth table, request/cleanup/clock seals, mode-specific reuse, `progression_ready_at`, H1 seller admission, frontier lineage, closed runner contexts, and focused tests as `feat(issue-discovery): evaluate VM capacity outcomes`.
- [x] 8.4 Commit finding v2, stable/occurrence identity separation, ingestion/rendering, redaction, and focused tests as `fix(issue-discovery): bind findings to durable VM fulfillment`.
- [x] 8.4a Restore the sanitized public finding-v2 example only after the validator commit exists, pin the example and its schema test to that exact commit, and commit the checkpoint as `test(issue-discovery): pin VM finding example authority`.
- [x] 8.5 Commit permanent specs/architecture, operational docs, completed promotion record, archived change, and complete-suite evidence as `docs(issue-discovery): promote VM capacity testing contracts`.
- [x] 8.6 For every checkpoint, stage only exact reviewed paths; use the repository-traditional body sections in this order: `Plain-language summary`, `Architectural summary`, `Change groups`, `Boundary decisions and deferrals`, `Issue tracking` when applicable, `Validation`, and `Result`. Run staged whitespace, secret, artifact-path, symlink/mode, and owner-only host scans plus the tests proportional to that commit. Then guarded-push only `feat/issue-discovery-harness`, persist the push receipt, and prove remote `dev` and private-infra `main`/scratch refs did not move.
