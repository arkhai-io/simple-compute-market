## 1. Shared query-language foundation

- [x] 1.1 Added the pure `market_core.query_dsl` lexer, source-spanned AST, typed descriptors, validation, and canonical rendering with focused parser and carrier-purity coverage.
- [x] 1.2 Added exact `arkhai-core==0.2.0` dependencies to registry-client and settlement-runtime, refreshed locks and reinit commands, and verified both installed environments import the shared DSL while carrier-purity checks pass.
- [x] 1.3 Added deterministic human/JSON field references and value-redacted positioned diagnostics covering ordering, quoting, duplicates, unknown fields, and malformed input.

## 2. Filter-spec-owned resource queries

- [ ] 2.1 Extend the configured registry filter vocabulary in `core/registry/filter-spec.yaml` and its schema/validation tests with explicit friendly query names/aliases where canonical HTTP parameters encode lower/upper-bound behavior; preserve existing JSON paths, operators, `on_missing`, versioning, and route behavior.
- [ ] 2.2 Implement filter-spec-to-descriptor conversion and resource-query compilation in `core/registry-client/src/registry_client/`, including declared type/operator/list coercion, aliases, canonical query parameters, schema identity, URL+ETag binding, and fail-closed errors; add sync/async client unit tests for heterogeneous specs and 412 rotation.
- [ ] 2.3 Replace generic core buyer `-f/--filter name=value` handling with one `--resource '<query>'` path in `core/buyer/src/core_buyer/cli.py`, compiling independently against every selected authenticated registry before listing requests and rejecting partial multi-registry vocabulary matches; update focused core buyer CLI tests.
- [ ] 2.4 Replace VM buyer resource convenience flags and direct `build_vm_filter_params` construction in `domains/vms/buyer/` with the shared `--resource` compiler across listing, discovery, negotiation, and buy entry points; remove obsolete parsers/helpers/tests and add typed numeric/list/boolean and multi-registry behavior coverage.

## 3. Settlement clause contracts and mechanism projections

- [ ] 3.1 Extend `MechanismRegistration` and settlement-runtime configuration validation in `kit/settlement-runtime/src/market_settlement_runtime/` with role-scoped public settlement-clause descriptors, pure option projectors, and typed publication-input validation hooks; reject duplicate/unqualified/secret-like fields and update registry fingerprint/configuration tests.
- [ ] 3.2 Implement settlement-clause compilation/evaluation in settlement runtime over exact `SettlementOption` objects: common `mechanism`, `asset`, and `option_id` fields; configuration-key-to-canonical-ID normalization; same-option conjunction; ordered repeated-clause alternatives; configured-priority tie-breaking; missing-value rules; and installed/enabled/role compatibility before ranking. Add cross-option false-positive, empty-clause, and deterministic-order unit tests.
- [ ] 3.3 Register only public listing projections and typed publication inputs in `kit/hosted-settlement` (`stripe.method`, `stripe.funds_flow`) and `kit/alkahest` (`alkahest.chain`, `alkahest.escrow_kind`); prove projector purity, mechanism qualification, option-ID stability, and secret-canary rejection in each kit's unit suite.
- [ ] 3.4 Replace VM buyer `--settlement-asset`, `--settlement-option-id`, and any documented `--settlement-mechanism` selection surface with repeatable `--settlement '<clause>'` on normal listing/negotiation/buy commands; preserve no-clause configured-priority behavior and remove all obsolete selection branches and help assertions.

## 4. Buyer action policy, explanation, and utility namespaces

- [ ] 4.1 Add one `open|print|fail` buyer-action policy shared by fresh buy and `market settle --from`; default to `open` only for an interactive terminal and `print` otherwise, keep `--yes` confirmation-only, remove `--no-browser`, and add stable action-required exit behavior without persisting action URLs. Cover fresh, resumed, interactive, noninteractive, and no-action responses in `domains/vms/buyer/tests/`.
- [ ] 4.2 Add mutation-free human/JSON `--explain` planning to generic and VM listing/buy paths: report registry schema/version/ETag, canonical resource AST and pushed parameters, canonical settlement clauses, stage survivor counts, sanitized rejection categories, and deterministic selected option; prove it stops before negotiation, run persistence, prerequisite resolution, client construction, RPC, hosted action retrieval, or financial mutation.
- [ ] 4.3 Move buyer raw `market escrow create|show|reclaim` commands into `market settlement alkahest escrow ...`, register only real mechanism-owned buyer utilities under `market settlement <mechanism>`, and remove old aliases. Update command composition, completion/help snapshots, utility tests, and imports without changing raw Alkahest behavior.
- [ ] 4.4 Remove legacy `market settle` chain/token/token-decimals overrides and derive accepted mechanism, option, escrow, chain, token scale, and operation identity exclusively from the persisted accepted run plus typed configuration; add recovery tests for pre-cutover Alkahest and hosted runs and for changed current mechanism priority/enablement.

## 5. Typed storefront publication and exact rates

- [ ] 5.1 Add the role-neutral structured `SettlementPublicationClause` carrier and strict config models in settlement runtime, including canonical mechanism ID, asset, decimal-text rate/per unit, common public fields, and validated mechanism-owned input; add JSON/TOML round-trip, unknown-field, and no-binary-float tests.
- [ ] 5.2 Extend generated storefront configuration and `domains/vms/storefront/src/market_storefront/utils/config.py` to accept structured settlement publication defaults, with complete-clause replacement rather than partial merging; update config generation/validation/migration-template tests while keeping trust, wallet, chain, account, readiness, and secrets outside clauses.
- [ ] 5.3 Extend storefront inventory CSV/import and projected resource records with a structured `settlements` JSON-array field; implement per-resource-clause-over-command-default-over-config-default precedence as whole-list replacement, reject malformed/partial records, and update import, projection, stable-listing-identity, and reconciliation tests.
- [ ] 5.4 Replace storefront publication's ambiguous `min_price`-to-option construction with repeatable `--settlement '<clause>'` compilation and registered option builders in `domains/vms/storefront/`; keep `min_price` only in negotiation policy, suppress unready mechanisms independently, and update publish/watch/help/completion and dual-mechanism tests.
- [ ] 5.5 Implement exact mechanism-owned rate normalization with `Decimal`: Stripe currency exponent and Alkahest token decimals each convert human asset units exactly once to integer `RateValue`; reject fractional base units, unsupported assets/scales, zero/negative values where disallowed, and implicit rounding. Add boundary tests showing equal human rates become the correct currency-minor/token-base integer values without changing option/plan wires.
- [ ] 5.6 Add preview/check/write/backup migration commands for storefront TOML and resource CSV inputs. Auto-convert only single-mechanism unambiguous legacy pricing; report dual-mechanism, hidden-reserve, per-row, missing-scale, and conflicting cases without mutation; use restrictive atomic writes and add idempotence, byte-preserving check, backup, rollback, and conflict diagnostics tests.

## 6. End-to-end behavior and compatibility evidence

- [ ] 6.1 Add focused registry-to-buyer integration coverage for typed resource comparisons, list membership, aliases, ETag rotation, heterogeneous registry rejection, zero resource results, and the distinct "resources exist but no compatible settlement option" explanation path.
- [ ] 6.2 Add focused dual-mechanism buyer/storefront integration coverage for correlated settlement predicates, ordered alternatives, explicit incompatible/disabled mechanisms, no-clause priority, post-acceptance pinning, resume, and no cross-mechanism fallback or reinterpretation.
- [ ] 6.3 Add storefront publication integration coverage for config defaults, command clauses, per-resource clauses, ready-peer/unready-peer behavior, exact Stripe/Alkahest rate normalization, deterministic option/listing identities, and migration from representative unambiguous and conflicting legacy inputs.
- [ ] 6.4 Exercise installed-wheel CLI smoke for generic buyer, VM buyer, and VM storefront: verify removed flags/aliases fail with actionable replacements; new `--resource`, repeatable `--settlement`, `--action`, `--explain`, mechanism utility namespaces, generated help, and shell completions work from staged wheels rather than source paths.

## 7. Permanent documentation promotion

- [ ] 7.1 Create `openspec/specs/cli-query-language/spec.md` and `architecture.md` from the verified delta/design and add the capability to `openspec/specs/README.md`, documenting the comparison-only grammar, descriptor authorities, correlation, units, action policy, explanation, and non-goals as current system behavior.
- [ ] 7.2 Synchronize verified requirements into `openspec/specs/buyer-orchestration/spec.md`, `registry-discovery/spec.md`, `settlement-configuration/spec.md`, and `storefront-publication/spec.md`; update their architecture companions with ownership, dependency direction, typed publication precedence, and mechanism qualification without change-history prose.
- [ ] 7.3 Update `docs/development/ARCHITECTURE.md` with the two-DSL boundary and normal-path/utility ownership; update `docs/development/DEPLOYMENT_AND_CONFIG.md` with structured publication configuration and migration/rollback; update `docs/development/TESTING.md` with DSL/correlation/rate evidence jurisdiction where material.
- [ ] 7.4 Rewrite `docs/buyer-quickstart.md`, `docs/seller-quickstart.md`, `docs/domain-authoring/README.md`, generated role configuration examples, command examples, and operational scripts to use `--resource`, repeatable `--settlement`, `--action`, `--explain`, structured publication clauses, and mechanism namespaces; remove every stale pre-cutover name and ambiguous pricing example.

## 8. Verification and packaging

- [ ] 8.1 Run focused unit and typing checks for `core`, `core/registry-client`, `core/buyer`, `kit/settlement-runtime`, `kit/alkahest`, `kit/hosted-settlement`, `domains/vms/buyer`, and `domains/vms/storefront`; record exact commands/results and resolve failures rather than narrowing the claimed boundary.
- [ ] 8.2 Run affected registry, buyer, storefront, negotiation, settlement, recovery, config-migration, listing-publication, and staged-wheel integration suites plus package-boundary/content checks; build the review wheelhouse and affected buyer/storefront images and smoke their installed CLIs.
- [ ] 8.3 Run strict validation for `add-resource-and-settlement-cli-dsls`, relevant dependent active changes, and repository-wide OpenSpec; run repository-wide `make check` and disclose any genuinely external/unrun provider, image-runtime, or deployment evidence without substituting narrower checks.

## 9. Closeout

- [ ] 9.1 Complete plan closeout: run `make check-comment-hygiene` and directly remove temporary change/review/migration narrative from touched production comments/docstrings; review every local import added or touched and move it to module scope unless a reproduced cycle or documented deliberate lazy-load requirement proves otherwise, then rerun the owning tests; audit every accepted decision against permanent-document placement; compress completed task notes to final behavior, material evidence, unresolved work, and permanent destinations; update the affected `docs/development/ROADMAP.md` current-state/gap mapping or explicitly record no roadmap impact; and complete this change's design-promotion record with exact verified permanent locations and final migration/validation evidence.
