## MODIFIED Requirements

### Requirement: Actionable development documentation gaps
Operators and contributors can find verified explanations for baked contracts, escrow mechanics, schema, watchdog recovery, and GPU passthrough. The implementation MUST preserve the ownership and compatibility constraints in the `test-compatibility` baseline specification.

#### Scenario: Change acceptance
- **WHEN** the implementation is complete and its focused verification runs
- **THEN** operators and contributors can find verified explanations for baked contracts, escrow mechanics, schema, watchdog recovery, and GPU passthrough.

<!-- Provenance: migrated from docs/development/TODO.md and linked ad hoc design notes. -->
