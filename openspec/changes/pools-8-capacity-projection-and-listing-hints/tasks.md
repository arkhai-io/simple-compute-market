## 1. Reconcile landed projection foundations

- [x] 1.1 Verify independent site resource-pool and capacity-bucket producers, revisions/digests, pull endpoints, and in-memory storefront caches landed under POOLS-7.
- [x] 1.2 Remove rebuilt producer/polling/cache mechanics from this change and record remaining persistence/consumption scope in `proposal.md` and `design.md`.
- [x] 1.3 Resolve global versus site-scoped Resource Pool identity and record the durable/public key shape before schema work. **Resolved (2026-08-03, `design.md`, "Resolved Questions"): `pool_id` is a site-local operator slug, never made globally unique under POOLS-7. Every durable/public reference this change introduces must key on `(site_id, pool_id[, resource_id])`, never `pool_id` alone.**
- [x] 1.4 Inventory every `resources`, capacity-pool, CSV import, validator, publication, claim-building, admin, migration, and e2e reader; classify physical authority versus commercial/operational ownership. **Done (2026-08-03, `design.md`, "Local physical-authority inventory"), with two corrections made in later sections rather than here (left as originally written for history, per the amend-don't-replace convention — see the superseding notes):** Section 4 corrected "two independent listing-creation paths" to one path (CLI is an automated caller of `create_listing`, not a second mechanism); Section 5 corrected `compute_capacity_pools`/`compute_pool_members` from "no external reader" to live-read by `domains/vms/listings/reconciler.available_compute_slices`, which this pass hadn't searched.

### Section 1 closeout (per `openspec/README.md#plan-closeout-requirements`, "or a major section within one")

**Added 2026-08-03, in response to the Section 3 review's process finding, applied retroactively to prevent it recurring.** Section 1 is discussion/investigation only -- no code, no schema, no new tests -- so its closeout is correspondingly light:

- **Comment hygiene:** not applicable; no production code was touched by this section.
- **Documentation compliance:** Section 1's findings (pool-identity scoping, the local physical-authority inventory) are recorded in `design.md` and feed later sections' own promotions (task 1.3's identity resolution -> `resource-pool-management/spec.md#pool-identity`, listed in the Design promotion record); Section 1 has no distinct permanent-doc destination of its own.
- **Narrative compression:** already reasonably compressed; the two corrections noted in 1.4 (made in Sections 4 and 5 rather than here) are cross-referenced, not duplicated.
- **Promotion:** none owed directly by this section; see Section 7's Design promotion record for where its findings ultimately land.

## 2. Surface per-site projection load state (revised 2026-08-03; no persistence layer)

**Revised in discussion 2026-08-03** (`design.md`, "Section 2 revised decision"): the originally planned durable persistence layer (migrations, repositories, seeded-cache restart recovery — old tasks 2.1–2.4, struck through below and kept for history per this repository's amend-don't-replace convention) is not being built. No production code consumes the projection caches yet, so the restart-time gap it targeted has no live consumer to protect; the one concrete operational need (the single-site e2e/Helm deployment, where the storefront may start before its site is reachable) is fully covered by the existing indefinite-retry poller plus observable status.

- [x] 2.1 Add per-`(site_id, projection_kind)` load-state fields (`ProjectionState`, last-fetched time, last error) to the storefront's existing operator status surface (`system_service.get_health`/`/api/v1/system/status`), following the existing `resource_count`-diagnostic pattern rather than inventing new blocking/gating behavior. Report per site — one site's failure to load must not present as global degradation when other configured sites are healthy. **Done (2026-08-03):** `site_projection_cache.projection_status_summary()` wired into `SystemService.get_health` under `result["site_projections"]`, gated to `include_registry=True` and independent of the `checks`/`all_ok` gate. **Reopened and re-fixed (2026-08-03, external review):** the field never actually reached HTTP callers — `core_storefront.models.system_models.HealthResponse` had no `site_projections` field, and `SystemController.system_status()`'s `HealthResponse(**body)` silently dropped it (Pydantic v2's default `extra="ignore"`). Existing tests called `SystemService.get_health()` directly and structurally could not have caught this. Fixed: added `site_projections: dict[str, dict[str, ProjectionFamilyStatus]] | None` to `HealthResponse` (new typed `ProjectionFamilyStatus` model, same file), promoted the field to first-class on `storefront_client.models.HealthResponse` too (previously only recoverable via its `.extra` dict), and added `fetched_at` tracking to `ProjectionCache`/`ProjectionCacheView` (`core_storefront/site_projections.py`) to close the "last-fetched time" gap the original task text specified but the implementation had silently dropped — accepted scope addition, not deferred. Verified this time by a real HTTP round-trip integration test (task 2.4), not just a service-level unit test.
- [x] 2.2 Confirm `site_projection_poller_loop` already retries indefinitely on a site that has never successfully loaded (not only on a previously-successful cache going stale), and add a test if this exact "never yet loaded" retry path is not already covered. **Done (2026-08-03):** traced `ProjectionCache.load()`/`refresh()`/`poll_once()` — every failure path catches internally and sets `invalid`/`unavailable` rather than raising, so a failing site was never actually capable of blocking another site's cache population or its own retry. Confirmed by `domains/vms/storefront/tests/unit/services/test_site_projection_cache.py` (present in the delivered tree throughout; an external review incorrectly reported this file as absent — re-verified present and passing as of this reopening, see 2.4's note).
- [x] 2.3 Document the "ignorance ≠ zero" requirement on Section 4's future projection consumer: `not_loaded`/`invalid`/`unavailable` states MUST NOT be treated as authoritative empty capacity. **Done (2026-08-03):** promoted into the permanent `openspec/specs/site-capacity/spec.md` as "Requirement: Per-site projection load-state visibility," directly beside the existing "Storefront projection families" text it extends, with two scenarios and an evidence-block entry. **Correction (2026-08-03, external review, fair):** this promotion was premature at the time it was written — the spec said the state "MUST be visible on the storefront's operator status surface" while 2.1's `HealthResponse` bug meant that was not yet true. Now genuinely true after 2.1's fix; the promoted text needed no wording change, only the underlying implementation catching up to it.
- [x] 2.4 Add focused tests: per-site/family status reporting reflects each `ProjectionState` correctly; one site's `unavailable` state does not affect another configured site's reported state; status reporting requires no new schema/migration (regression guard against silently reintroducing persistence). **Done (2026-08-03), then corrected (2026-08-03, external review):** two real problems in the original close-out, both fixed:
  - The `test_system_service.py` tests patched `market_storefront.services.site_projection_cache.projection_status_summary` at module level, which `docs/development/TESTING.md` explicitly prohibits ("Do not patch module-level imports... constructor injection [is] the natural seam"). Fixed: `SystemService` now takes an injected `projection_status_provider: Callable[[], dict] | None = None` (defaulting to the real production source), and every test in `TestGetHealthSiteProjections` was rewritten to inject a fake instead of patching. One new test (`test_default_provider_is_the_real_projection_status_summary`) exercises the real default path so it isn't only proven by construction.
  - No test exercised the real HTTP contract, which is exactly the layer that hid 2.1's bug. Added `test_system_status_surfaces_site_projection_state` and `test_health_omits_site_projections` to `domains/vms/storefront/tests/integration/test_admin_api.py`'s existing `TestHealthEndpoint` class — real FastAPI app, real `StorefrontClient` over `httpx.ASGITransport`, asserting the projected data survives the full server-model → JSON → client-model round trip.
  - `test_site_projection_cache.py`'s 4 tests (partial multi-site failure isolation, never-loaded retry, status-summary shape, empty-cache case) were re-verified present and passing — an external review reported this file as absent from "the supplied base and diff"; re-confirmed via direct execution that it exists in this tree and always did, so that specific finding does not describe this repository's actual state. Full suite as of this reopening: `core/storefront/tests/unit/test_site_projections.py` (81-file package suite, includes 4 new `fetched_at` tests), `domains/vms/storefront/tests/unit/services/test_site_projection_cache.py` (4), `.../test_system_service.py` (11, including the rewritten site-projections class), `.../tests/integration/test_admin_api.py` (36, including the 2 new HTTP round-trip tests) — all passing.
- [x] 2.5 Small follow-up, not blocking: confirm whether POOLS-7 task 2.3's "storefront-side connection-to-site identity ... process-local aggregation state" durability note is already satisfied by `AggregateCapacityClient._reservation_sites`'s existing documented cache-with-fallback design (`design.md`, "Related finding" under the revised decision) — likely a documentation-only closure against POOLS-7 rather than new work here; confirm scope before doing anything. **Done (2026-08-03):** confirmed and closed directly in `pools-7-storefront-fulfillment-cutover/tasks.md`'s own task 2.3 — no code needed, the existing soft-cache design already satisfies it.
- [ ] 2.6 **Added 2026-08-03, external review — separate follow-up, not this section's to close:** no integration test runs the real `core_storefront.capacity_remote.RemoteCapacityClient` (`resource_pool_projection_version`/`resource_pool_projection`/`capacity_bucket_projection_version`/`capacity_bucket_projection`) against a real in-process provisioning app. `provisioning/compute/service/tests/integration/test_capacity_api.py`'s `CapacityApi` test helper covers reserve/commit/release/snapshot/probe/events but not the projection endpoints; storefront-side coverage uses a hand-rolled `FakeSite`. This predates this section — it did not change the projection producer/consumer wire contract, only added a summary derived from already-existing `ProjectionCache` state — so it is not a blocker for Section 2's closure, but is real test debt worth its own task: an in-process `ASGITransport`-based integration test pairing the real `RemoteCapacityClient` against the real router in `kit/site/src/market_site/router.py`. **Confirmed again (2026-08-03, second review round):** classification agreed, not held for Section 3 either — but flagged as growing in value once real work starts depending on projected data (i.e. relevant again around Section 4's mapping work), not something to let go stale indefinitely.

**Second review round (2026-08-03) — acknowledged, mostly deferred as the reviewer itself suggested:**
- `fetched_at` semantics clarified in place (`ProjectionCacheView`'s and `ProjectionFamilyStatus`'s docstrings now say explicitly: advances on unchanged-poll confirmation, not only full snapshot transfer) rather than renamed to `confirmed_at` — the reviewer offered both options and called neither blocking; documentation was the cheaper, sufficient fix.
- Client-side typed nested models for `site_projections` (currently `dict[str, Any]` on `storefront_client.models.HealthResponse`) and a shared `state` enum on `ProjectionFamilyStatus` (currently plain `str`): both correctly called "hardening, not correctness" and "not required for Section 2" — deferred, not tracked as a numbered task; revisit if a concrete client-side consumer needs the stronger typing.
- Narrative compression of this section's review-response notes: already covered by the existing Section 7 closeout task's "Narrative compression" requirement; not duplicated here.
- The `test_site_projection_cache.py` "missing file" discrepancy was a delivery-packaging issue on the review-coordination side, not a repository state issue — confirmed by the requester after investigation. No repository action needed beyond what task 2.4 already recorded.

### Implementation plan (planned 2026-08-03)

Grounded against the exact current code, not just the design decision. Confirmed while planning: `ProjectionCache.load()`/`refresh()` (`core/storefront/src/core_storefront/site_projections.py`) already catch every exception internally and set `state=invalid`/`stale` rather than raising — so `site_projection_cache.load_site_projections()`'s `asyncio.gather(caches.resource_pools.load(), caches.capacity_buckets.load())` cannot fail one site and block another from being inserted into `_caches`. Task 2.2 is therefore "add a regression test proving this," not "fix a bug" — worth stating plainly since it would be easy to assume otherwise from the design-phase description alone.

**2.1 — status surface.**
- New function `market_storefront.services.site_projection_cache.projection_status_summary() -> dict[str, dict[str, dict[str, Any]]]`, shape `{site_id: {"resource_pool": {...}, "capacity_bucket": {...}}}`, each leaf built from `ProjectionCache.view()` (`ProjectionCacheView`: `identity`, `value` — presence/absence only, never the value itself, to avoid duplicating the full projection payload into a status endpoint — `state`, `last_error`). Iterate `projection_caches()` (already exported, already returns a copy) rather than reaching into `_caches` directly.
- Wire into `SystemService.get_health(*, include_registry: bool = False)` (`market_storefront/services/system_service.py`) as a new `checks`-adjacent key, e.g. `result["site_projections"] = projection_status_summary()`, gated the same way `resource_count` already is (only under `include_registry=True`, i.e. `/api/v1/system/status`, not the fast `/health` probe) and wrapped in the same try/except-returns-`None`-on-failure pattern already used for `resource_count` immediately above it in that function.
- Explicitly do not add a `_check_is_healthy` rule for this key or fold it into `all_ok` — per Section 2's revised decision, one site's `invalid`/`unavailable` state must not present as global degradation. It's reported, not gated.

**2.2 — poller retry regression test.**
- New test in `domains/vms/storefront/tests/unit/services/test_site_projection_cache.py` (create if absent): two configured sites, one client raising on every call, one succeeding; assert after `load_site_projections()` that both sites are present in `_caches` (not just the healthy one) and the failing site's cache state is `invalid`; then call `site_projection_poller_loop`'s single-iteration body (or extract the loop body into a testable `_poll_once_all()` helper if the `while True`/`asyncio.sleep` wrapper makes direct testing awkward) and assert the failing site's `version()` was attempted again.

**2.3 — spec promotion.**
- Already drafted in the delta spec (`openspec/changes/pools-8-.../specs/site-capacity/spec.md`, "Per-site projection load-state visibility"). This task is executing that promotion into the permanent `openspec/specs/site-capacity/spec.md`, not drafting new content.

**2.4 — status-reporting tests.**
- New tests alongside 2.2's, or in `domains/vms/storefront/tests/unit/services/test_system_service.py`: `get_health(include_registry=True)` reflects each configured site's per-family state correctly; a site in `invalid`/`unavailable` state does not change `result["status"]` from `"ok"`; assert no new table/migration file is touched by this section's diff (a cheap regression guard — e.g. a test asserting `market_storefront.utils.migrations` module's migration list length/hashes are unchanged by this section — against silently reintroducing persistence).

**2.5 — POOLS-7 follow-up.** Unchanged from the design-phase description; this is a documentation-only check against POOLS-7's own tasks.md/spec, not code in this change.

**Permanent documentation destination for this section:** `openspec/specs/site-capacity/spec.md#projection-load-state` (per the Design promotion record — Section 7 already covers execution of this).

<details>
<summary>Original tasks 2.1–2.4 (persistence layer), superseded — kept for history</summary>

- [ ] ~~2.1 Add storefront migrations and repositories for trusted configured-site bindings and independent resource-pool/capacity-bucket generations.~~
- [ ] ~~2.2 Persist revision, digest, accepted value, fetched time, and stale/error state transactionally per `(site_id, projection_kind)`.~~
- [ ] ~~2.3 Load complete stored generations as stale on restart before polling and replace one family without mutating the other.~~
- [ ] ~~2.4 Add migration, restart, partial-refresh, malformed-payload, revision-reset, and stale-retention tests.~~

</details>

### Section 2 closeout (per `openspec/README.md#plan-closeout-requirements`, "or a major section within one")

**Added 2026-08-03, retroactively, for the same reason as Section 1's.** Section 2 was implemented and code-reviewed twice before this task existed; recorded here rather than invented fresh, per the amend-don't-replace convention.

- **Comment hygiene:** clean, re-verified directly (`grep`-equivalent to `make check-comment-hygiene`) across every file touched by this section as of the second review round's fixes.
- **Documentation compliance:** "Requirement: Per-site projection load-state visibility" is promoted into the permanent `openspec/specs/site-capacity/spec.md`, with scenarios and an evidence-block entry that was updated a second time once the real `HealthResponse`/`fetched_at`/integration-test fixes landed.
- **Narrative compression:** not yet done -- this section's task notes still carry the full two-round review trail (what the first reviewer got right, what was disputed and re-verified, what the second reviewer confirmed). That trail has real value while Sections 4-6 are still being planned against this foundation, so compressing it now would lose context those sections' authors may still need. Defer actual compression to the whole-change Section 7 closeout, once no further section is likely to need the detailed trail.
- **Promotion:** done (see documentation compliance above).

## 3. Project safe pool metadata

- [x] 3.1 Define allowlisted pool label, enabled state, mechanism reference, opaque policy-tag fields, and a generic `pool_views: dict[str, Any]` field in the site resource-pool projection, mirroring the existing `publication_views` precedent (resource-level, same file) rather than adding any provider-shaped key names directly. `kit/site` must not know what any `pool_views` entry contains. **Done (2026-08-03):** `_POOL_METADATA_FIELDS`/`_project_pool_metadata` added to `kit/site/src/market_site/projections.py`; `resource_pool_projection` and `SiteProjectionService` gained `pool_metadata`/`pool_directory`; `make_capacity_router` gained `get_pool_directory`. No `kit/site/http_models.py` change needed (already `list[dict[str, Any]]`); no VM-storefront client change needed (`pool_metadata` arrives nested in already-forwarded rows) — both confirmed by running the existing suites unmodified, not assumed.
- [x] 3.2 Advance revision/digest on projected metadata changes and prove credentials/provider secrets are redacted. **Done (2026-08-03):** both fall out of existing mechanisms (`canonical_digest` already hashes full row structure; `_project_pool_metadata`'s allowlist structurally drops anything not in `_POOL_METADATA_FIELDS`) — confirmed by test, including a **real bug caught in the process**: the allowlist's `policy_tags`/`pool_views` copy was only shallow (`dict(...)`), so mutating a nested `pool_views` entry a caller got back still mutated the caller's original object. Fixed with `copy.deepcopy`.
- [x] 3.3 Preserve backward compatibility when older producers omit additive metadata. **Done (2026-08-03):** `pool_metadata=None`/omitted and a pool absent from a supplied directory both produce zero `pool_metadata` key (not an empty one) — confirmed byte-identical to pre-3.1 output by test.
- [x] 3.4 Add producer/router/client/cache contract tests for old and new payloads. **Done (2026-08-03), corrected (2026-08-03, second external review round — see "Section 3 closeout" below for detail):** 8 new tests in `kit/site/tests/unit/test_projections.py`, 2 new tests in `kit/site/tests/unit/test_projection_router.py` (real FastAPI `TestClient`, real SQLite-backed ledger), 14 new tests in `provisioning/compute/service/tests/unit/services/test_capacity_inventory.py` for `load_capacity_pool_metadata`. The two end-to-end integration tests in `provisioning/compute/service/tests/integration/test_capacity_api.py` were originally written against a raw HTTP GET and a direct DB insert — corrected to go through the actual canonical clients: `RemoteCapacityClient.resource_pool_projection()` (the real storefront-side projection consumer) on the read side, `ProvisioningClient.create_pool(...)` on the write side. 6 further tests added to `provisioning/compute/service/tests/integration/test_pools_api.py` proving `default_vm_*` is reachable through the real pool-admin API specifically (the original bug 3.5 fixes). Full regression sweep after all of Section 3: 420 passed (`provisioning/compute/service` unit), 171 passed (integration), 145 passed (`kit/site`) — zero failures, zero regressions.
- [x] 3.5 Project the pool's configured VM size defaults (`default_vm_ram`, `default_vm_vcpus`, `default_vm_disk_size` -- `AnsiblePoolConfig`) as a versioned `vm.ansible_pool_defaults.v1` entry inside the additive, allowlisted, generic `pool_views` field (task 3.1) — not a flat VM-shaped key at the top level of pool metadata (`design.md`, "Resolved: `pool_views`, mirroring the existing `publication_views` precedent") — so a storefront can resolve a full four-dimension shape (GPU/vCPU/RAM/disk) at negotiation time instead of only GPU count. The shaping function lives in `vm_provisioning_adapter`'s own runtime module, matching how `project_bare_metal_resource` already builds the `bare_metal.v1` resource-level view from the bare-metal domain package rather than inline in `compute_provisioning_service`. **Correction (2026-08-03, matches `proposal.md`):** these fields are not currently persisted anywhere — they exist only on the fulfillment-time pydantic `AnsiblePoolConfig` model and are unreachable in practice because `AnsiblePoolConfigHandler`'s field allowlist rejects them if an operator tries to set them via the pool admin API. This task must add real persistence (schema column + handler wiring, following the existing `_migrate_ansible_jobs_contract_fields`-style additive-column migration pattern in `provisioning/compute/service/db/migrations.py`) as a prerequisite before there is anything to project. This is the identified prerequisite for negotiation round-0 payload enrichment (see `openspec/changes/negotiation-driven-capacity-resize`, opened 2026-07-29); that change's storefront-side consumption is explicitly out of scope here and depends on this projection existing first. **Done (2026-08-03):** all three layers built and independently validated:
  - **Schema:** `default_vm_ram`/`default_vm_vcpus`/`default_vm_disk_size` added to `AnsiblePoolConfig`; migration `20260803_001_ansible_pool_config_vm_size_defaults` follows the existing `_add_column_if_missing` pattern. Validated directly against a hand-built pre-migration table (adds columns, existing rows get `NULL`, idempotent re-run) and against the full `run_migrations`/fresh-deploy chain. Updated three pre-existing hardcoded-migration-count/id tests in `provisioning/compute/service/tests/unit/test_database.py` that otherwise would have gone stale silently.
  - **Handler:** `AnsiblePoolConfigHandler`'s allowlist, validation (positive-int RAM/vCPUs, non-empty-string disk size, all independently optional), `read_config`, `replace_config` fully wired. New `provisioning/compute/service/tests/unit/services/test_ansible_pool_config_handler.py` — this class had **no test coverage at all before this task** (confirmed absent during Section 3 design) — 23 tests, real-DB round trip.
  - **Shaping + composition:** `project_ansible_pool_defaults` added to `vm_provisioning_adapter/runtime.py` (plain dict, not a new pydantic model — three optional scalars with no cross-field validation need beyond what the handler already enforces). `load_capacity_pool_metadata` added to `capacity_inventory.py`, wired into `main.py`'s `make_capacity_router(get_pool_directory=...)`. 13 unit tests plus the 2 end-to-end integration tests noted under 3.4 — the integration test also surfaced a genuinely useful fact worth recording: the resource-pool projection is built from `Host` rows (`load_capacity_resource_inventory`), not directly from the ledger's registered resources, so a pool with no `Host` row projects nothing regardless of what's registered against it.

### Implementation plan (planned 2026-08-03)

Grounded against the exact current code (`kit/site/src/market_site/{projections,router}.py`, `kit/resource-pools/src/market_resource_pools/db.py`, `provisioning/compute/service/src/compute_provisioning_service/{db/models,db/migrations,services/capacity_inventory,main}.py`, `domains/vms/provisioning/adapter/src/vm_provisioning_adapter/{runtime,services/ansible_pool_config_handler}.py`).

**3.1 — projection field, mirroring `capacity_inventory.py`'s existing `bare_metal_publication`/`publication_views` pattern exactly, not inventing a new one:**
- `kit/site/src/market_site/projections.py`: add `_POOL_METADATA_FIELDS = ("label", "enabled", "mechanism", "policy_tags", "pool_views")` and `_project_pool_metadata(meta: Mapping[str, Any]) -> dict[str, Any]` (allowlist filter, `dict(...)`-copy `policy_tags`/`pool_views` so callers can't mutate cached state through the returned reference). Extend `resource_pool_projection(resources, *, pool_metadata: Mapping[str, Mapping[str, Any]] | None = None)`; each pool row gets a `"pool_metadata"` key only when `pool_metadata.get(pool_id)` is not `None`.
- `SiteProjectionService.__init__` gains `pool_directory: Callable[[], Mapping[str, Mapping[str, Any]]] | None = None`; `resource_pools()` calls it once per invocation (same lifecycle as the existing `resource_inventory` callable) and passes the result through.
- `kit/site/src/market_site/router.py`: `make_capacity_router` gains `get_pool_directory: Callable[[], Mapping[str, Mapping[str, Any]]] | None = None`, threaded into the `SiteProjectionService(...)` construction inside `projections()`.
- `kit/site/src/market_site/http_models.py`: **no change** — `ResourcePoolProjectionResponse.resource_pools` is already `list[dict[str, Any]]`, so the new key needs no schema addition.
- `domains/vms/storefront/.../site_projection_cache.py`/`_RemoteProjectionClient`: **no change** — `pool_metadata` arrives nested inside each already-forwarded resource-pool row, so the client needs no new parsing.

**3.2 — revision/digest + redaction, both free by construction, confirmed by test not just asserted:**
- `ProjectionRevisionTracker.observe()` already hashes the complete row structure via `canonical_digest`, so a `pool_metadata` change advances the revision with no new logic. Test: assert `resource_pool_projection`'s revision advances when only `pool_metadata` changes and resources are identical.
- Redaction is enforced structurally by `_project_pool_metadata`'s allowlist (a field not in `_POOL_METADATA_FIELDS`, e.g. `provider_config`, is silently dropped, not merely undocumented). Test: feed a `pool_metadata` dict containing `provider_config`/`extra_vars` alongside allowlisted fields and assert only the allowlisted keys appear in the projected row.

**3.3 — backward compatibility, free by construction:**
- `pool_metadata=None` is the existing default; a pool absent from the supplied mapping gets no `"pool_metadata"` key at all — not an empty dict, not `None` — so an old caller and a new caller with no pool directory produce byte-identical output. Test: call `resource_pool_projection` with and without `pool_metadata=None` explicit and assert identical results; call with a `pool_metadata` dict missing one of several pools and assert that pool's row has no `"pool_metadata"` key while the others do.

**3.4 — contract tests, by layer:**
- `kit/site/tests/unit/test_projections.py`: 3.2/3.3's tests above, plus one asserting `SiteProjectionService(ledger, pool_directory=...)` end-to-end shape.
- `kit/site/tests/unit/test_router.py` (or wherever router tests live — check before assuming a new file): `make_capacity_router(..., get_pool_directory=...)` surfaces `pool_metadata` on `/site-resource-pools`; omitting `get_pool_directory` reproduces today's response exactly.
- `provisioning/compute/service/tests/unit/services/test_capacity_inventory.py`: new tests for `load_capacity_pool_metadata` (3.5, below).
- No new VM-storefront-side test needed per 3.1's "no change" finding — confirm this by running the existing `test_remote_capacity_client.py` unmodified against a payload that now includes `pool_metadata`, proving forward-compatibility rather than assuming it.

**3.5 — VM size defaults, three layers:**
1. **Schema** (`provisioning/compute/service/src/compute_provisioning_service/db/models.py`): add `default_vm_ram = Column(Integer, nullable=True)`, `default_vm_vcpus = Column(Integer, nullable=True)`, `default_vm_disk_size = Column(String, nullable=True)` to `AnsiblePoolConfig`. Migration in `db/migrations.py`: `_migrate_ansible_pool_config_vm_size_defaults(engine)` using the existing `_add_column_if_missing` helper (three calls, `"INTEGER"`/`"INTEGER"`/`"VARCHAR"`), registered as `Migration("20260803_001_ansible_pool_config_vm_size_defaults", _migrate_ansible_pool_config_vm_size_defaults)` appended to `_MIGRATIONS`.
2. **Handler** (`domains/vms/provisioning/adapter/.../ansible_pool_config_handler.py`): add the three keys to `_FIELDS`; in `validate_config_problems`, each is optional/nullable — `default_vm_ram`/`default_vm_vcpus` must be a positive `int` (not `bool`) when present, `default_vm_disk_size` a non-empty `str` when present; wire into `read_config`/`replace_config` alongside the existing three fields.
3. **Shaping + composition** (mirrors `project_bare_metal_resource`/`_bare_metal_publication_view` exactly): `project_ansible_pool_defaults(raw_view: Mapping[str, Any]) -> dict[str, Any]` added to `vm_provisioning_adapter/runtime.py` (same module `project_bare_metal_resource` lives in, on the bare-metal side — a plain dict pass-through/normalize, not a new pydantic model, since the shape is three optional scalars with no cross-field validation need beyond what the handler already enforces at write time). New `load_capacity_pool_metadata(session_factory) -> dict[str, dict[str, Any]]` in `capacity_inventory.py`: query `ResourcePool` (`label`, `enabled`, `provider` → `mechanism`, `policy_tags`) joined against `AnsiblePoolConfig` by `pool_id`; when an ansible config row has any of the three fields set, call `project_ansible_pool_defaults(...)` and nest the result under `pool_views["vm.ansible_pool_defaults.v1"]`. Wire into `main.py`: `_capacity_pool_directory() -> dict[str, dict[str, object]]` calling `load_capacity_pool_metadata(container.session_factory())`, passed as `make_capacity_router(..., get_pool_directory=_capacity_pool_directory)`.
- Tests: handler validation (reject non-positive/non-int RAM, accept `None`, reject unknown extra field as before); migration idempotency (`_add_column_if_missing` re-run is a no-op); `load_capacity_pool_metadata` — a pool with no `AnsiblePoolConfig` row at all, a pool whose config has all three fields `None` (must omit `pool_views` entirely, not emit an empty dict), and a pool with a full set (must nest correctly under the versioned key).

### Section 3 closeout (per `openspec/README.md#plan-closeout-requirements`, "or a major section within one")

**Added 2026-08-03, external review (second round):** Section 3 previously ended at 3.5 with no closeout task, relying on Section 7's whole-change closeout — `openspec/README.md` requires one per major section, not only once for the whole change. Also corrects two overclaims the same review caught:

- Task 3.4's "producer/router/client/cache contract tests" wording overclaimed what was delivered at the time — the canonical projection client (`RemoteCapacityClient`) and the canonical pool-admin client (`ProvisioningClient`) were not exercised; two integration tests used a raw HTTP GET and a direct `AnsiblePoolConfig` DB insert instead. **Resolved, not just reworded:** `provisioning/compute/service/tests/integration/test_capacity_api.py`'s two projection tests now go through `RemoteCapacityClient.resource_pool_projection()` (real `ASGITransport`, real in-process app) and `ProvisioningClient.create_pool(...)` (via the `client_and_queue` fixture) rather than a raw HTTP call and a direct DB insert. A new `TestVmSizeDefaultsThroughAdminApi` class (6 tests) was added to `provisioning/compute/service/tests/integration/test_pools_api.py`, proving `default_vm_*` is reachable through the real pool-admin API end to end — the actual bug 3.5 exists to fix, previously only proven at the loader/DB level.
- Task 3.5's claimed migration-column and handler-test evidence was accurate about what had been run during development, but the migration claims were not backed by a durable, checked-in assertion — `test_database.py`'s changes at the time only touched migration-count/id bookkeeping, not the three new columns themselves. **Resolved:** real column-existence and idempotency assertions added to both `test_run_migrations_applies_versioned_migrations_to_old_sqlite_schema` and `test_run_migrations_is_idempotent`. The claimed `test_ansible_pool_config_handler.py` (23 tests) was re-verified present and passing — a review-artifact-delivery gap, not a repository gap, same class of issue as Section 2's.
- **New, not previously identified:** `pool_views["vm.ansible_pool_defaults.v1"]` was gated only on an `AnsiblePoolConfig` row existing for a pool, not on that pool's `mechanism` actually being `"ansible"`. `ResourcePoolService` already deletes the old provider's config row on a provider change, so this wasn't a live bug, but it cost nothing to make structural rather than incidental. Fixed in `capacity_inventory.load_capacity_pool_metadata`, with a test proving a stale/orphaned config row for a non-ansible pool never surfaces its view.
- **Comment hygiene:** the mechanical grep (`make check-comment-hygiene`-equivalent) was clean, but a third external review round caught what that grep structurally cannot: two test docstrings describing change history rather than current behavior (`test_capacity_api.py`'s "default_vm_* was previously unreachable through the admin API..."; `test_pools_api.py`'s `TestVmSizeDefaultsThroughAdminApi` class docstring, "These fields were previously accepted by no path at all..."). Both rewritten to state what the test proves about current behavior, with no reference to the fact that anything was ever broken. Swept every other file this section touches for the same pattern afterward; no further instances found (a few "previously"/"no longer" hits elsewhere are legitimate test-scenario narrative describing steps within a test's own sequence, not codebase-history commentary, and were left as-is).
- **Documentation compliance:** the "Resource-pool projection metadata" requirement is promoted into the permanent `openspec/specs/site-capacity/spec.md`, with a full evidence-block entry — not left pointing at a destination that doesn't yet contain the decision.
- **Narrative compression:** this section's task notes retain implementation detail (file/function names, what was tested where) since that's the durable technical record per `openspec/README.md`'s own promotion-record example; the review-response-style commentary ("real bug caught in the process," "genuinely useful fact worth recording") is intentionally kept minimal in this closeout note and should be trimmed further at the whole-change Section 7 closeout rather than duplicated here.
- **Correction to a specific claim:** the earlier note that running `test_remote_capacity_client.py` "confirmed" backward compatibility was imprecise. Re-run: 11 of 14 tests pass; 3 fail (`test_subscriber_*`, `test_poller_positions_at_head_then_emits_new_deltas` — all about `capacity_events` subscriber/poller machinery, unrelated to `resource_pool_projection`/`pool_metadata`). Verified those 3 failures reproduce identically against the unmodified original code (a pre-existing sandbox mock-patching/import-order issue), so they are not a regression from this section, but the earlier "confirmed by running the existing suites" phrasing should have said "11 of 14; the other 3 are pre-existing and unrelated" rather than implying a clean run.

Final regression sweep after all of the above: 420 passed (`provisioning/compute/service` unit, was 419 — +1 net for the provider-gate test since the migration-count tests were edits not additions), 171 passed (integration, was 164 — +7 for the new `TestVmSizeDefaultsThroughAdminApi` class), 145 passed (`kit/site`, unchanged). Zero failures.

## 4. Map projections into commercial inventory

The mapping schema already existed (`derived_compute_listings`/`derived_bare_metal_listings`, no commercial fields of their own -- pricing/settlement/policy live on `listings` via `listing_id`); this section extends it, not replaces it. Full rationale, alternatives considered, and the corrected Section-1 finding (CLI-derived and operator/API-driven listing creation are one `create_listing` path, not two) are in `design.md`, "Section 4 design."

- [x] 4.1 Site-scoped derivation keys. `listing_resource_key`/`listing_pool_key` now require and validate non-empty `site_id`, threaded through every `reconciler.py` call site; `site_id_for_listing(db_path, listing_id)` is the one shared lookup helper every caller reuses, keeping `site_id` off the public offer model (consistent with 4.4's own reasoning for `reserve()`). Unmapped-listing fallback is dynamic and explicit: a listing's own mapping wins when present, `home_site` only when `configured_site_count == 1` (exact, never a guess), quarantined otherwise -- both branches tested. Bare-metal confirmed already correctly site-scoped, nothing to do there. Two real bugs found and fixed along the way: `SQLiteClient` had an independent, hand-maintained copy of this table's schema that had drifted from `reconciler.py`'s own (now delegates to it, single source of truth, with `_has_derived_listings_site_column` guarding against this class of drift recurring); and `derivation_key`'s naive colon-delimited format was not collision-free for operator-chosen strings containing colons (fixed with a length-prefixed encoding, provably injective, adversarial tests included). New `test_reconciler.py` (35 tests, no prior dedicated file existed). Full regression: 630 passed (unit), 147 (integration), zero regressions.
- [x] 4.2 Backfill/quarantine, revised from the originally planned migration-based approach. A migration-time backfill was the wrong mechanism (migrations run once, ever, and can't safely depend on live deployment config that changes after they run); the actual mechanism is 4.1's own read-time `configured_site_count`-gated fallback, already sufficient. A legacy row simply not being found is accepted as idempotent re-derivation, not data loss.
- [x] 4.3 Redirect `available_compute_slices`' structural capacity source to the site-resource-pool projection, behind an opt-in `site_pool_projection` parameter (`None` default, byte-identical to prior behavior). `gpu_model`/`region`/`sla`/pricing don't exist in the projection; `gpu_model` was added end-to-end on the provisioning-service side (`Host.gpu_model` column and migration, threaded through `host_service.py`, forwarded into `capacity_inventory._project_host`). Pricing/`region`/`sla` stay local-only, looked up only for `home_site`'s own pools (`compute_capacity_pools.pool_id` is not site-scoped, so a non-home-site lookup would risk the same collision class 4.1 fixed elsewhere -- proven safe by construction and by a dedicated test). Wired into both real callers behind `settings.capacity.use_site_projection_for_listings` (default off): the server's `_reconcile_listings` and the CLI's own independent synchronous fetch, kept deliberately separate rather than sharing a cache the CLI's short-lived process can't use.
  - **A real correctness bug found and fixed during external review, not merely noted:** `site_pool_projection()`'s `if value:` conflated "never loaded" (`None`) with "loaded, genuinely zero pools" (`[]`) -- an authoritative zero-capacity answer was silently falling back to stale local data. Fixed with `if value is not None:`; both the missing producer-level and consumer-level test cases added. A proposed larger redesign (explicit `use_projection: bool` instead of the overloaded-container pattern) was traced through concretely and rejected on a forward-compatibility argument recorded in `design.md`: once local tables retire, there's nothing left to choose between, so the flag would just be removed again.
  - **`_projected_resource_usage`'s condition order had a second, related bug, found later while building the Section-4-closeout orchestration test:** it checked `member_availability is None` before checking whether the projection's own `available` field was present, so a `None` `member_availability` silently discarded the projection's own authoritative data. Fixed; the gap in unit coverage that let it through (no test combined `member_availability=None` with a present `available` field) is now closed.
  - `gpu_model` integration coverage closed on both ends through the real typed clients (`ProvisioningClient` in `test_hosts_api.py`, `RemoteCapacityClient` in `test_capacity_api.py`). CLI fetch behavior (`_site_pool_projection_sync()`) has 8 dedicated tests against a real fake HTTP boundary. Orchestration coverage from cache to reconciliation exists at two levels: a wiring-level test proving the cache reaches the right function argument, and a full real-DB integration test (`TestRealOrchestrationCacheToReconciliation`) proving cached data reaches real listing state -- the latter is what caught the availability-precedence bug above. Building it also caught two genuinely broken pre-existing tests in `test_remote_capacity_client.py` (stale fixture signatures from this section's own earlier work) -- fixed, not worked around.
  - Cross-change coordination on `gpu_model`'s eventual shape recorded in `structured-capacity-requirements/design.md` ("Symmetric nesting"): lands here already-flattened (`attributes["gpu_model"]`), forward-compatible with that change's eventual nested shape without needing migration.
  - Full regression sweep after all of the above (multiple rounds, including a mid-section external review and this closeout's own orchestration-test bug): every remaining failure confirmed, by exact name and count, against the same pre-existing/environment-only set throughout. See the closeout notes below for the final count.
- [x] 4.4 Pinned-site `reserve()`. `AggregateCapacityClient.reserve()` gained `site: str | None = None`, dispatching to `_reserve_at_site` (exactly one site, no fallback, errors propagate rather than being swallowed -- there is no next site to try) or `_reserve_by_placement` (today's unchanged fan-out, unmapped listings only). Applies whenever a site mapping exists, fungible or resource-pinned alike -- the storefront only ever commits to a site; which physical resource satisfies the claim is the provisioning service's decision, never the storefront's. `site_id` stays off the public offer model; callers resolve it via `site_id_for_listing` immediately before calling `reserve(site=...)`. All four call sites wired, each handled per its own existing error-handling posture rather than uniformly: two gained new `try/except` routing a pinned-site error into their existing failure response, one already had appropriate handling, one needs neither (its caller already wraps the whole body). The actual `site_id` resolution point for the VM-fulfillment chain turned out to be `settlement_jobs.py`, one level up from where originally assumed -- already had everything needed, so no new db access was wired into the fulfillment service. `fulfill_credit_obligation` (api-credits) deliberately left untouched -- an accepted-but-unused parameter there would misrepresent what the function does. 7 new unit tests including the actual collision case (a mapped listing proven to reserve at its recorded site despite placement policy preferring a different one). Full regression: 38/38 in the admin-API integration file, no regressions elsewhere.
  - **Code review (2026-08-04), findings verified and addressed rather than assumed:**
    - **Error-semantics fix, per direct instruction:** the admin `reserve_capacity` endpoint was collapsing every pinned-site failure (unknown mapped site, transport failure) into the same 409 a genuine live refusal returns -- losing a distinction an operator debugging a failed reservation needs. Now distinguishes three cases: live refusal (409, unchanged), unknown/stale site mapping -- `KeyError` from `AggregateCapacityClient`'s plain-dict site lookup -- (500), and mapped-site transport failure (502, matching this same file's own existing precedent for site-authority communication failures at a different endpoint). `fulfillment_resume_runtime.py`'s collapsing behavior kept as-is -- that workflow already has its own recovery/retry semantics, unlike an operator-facing debugging endpoint. 2 new tests, each verified to trigger the actual code path under test (a real stale mapping, a real injected transport failure), not just asserted against a mocked status code.
    - **Two representative mapped-listing tests added, closing a real gap: "all four call sites wired" was true by inspection but only one had behavioral evidence for the mapped case.** `test_two_phase_reserve.py::test_acceptance_hold_pins_to_the_listings_mapped_site` (negotiation hold) and `test_settlement_jobs.py::test_background_task_threads_the_listings_mapped_site_to_fulfillment` (settlement/fulfillment fallback), both proving `site_id` resolution reaches the actual `reserve()`/`fulfillment.fulfill()` call for a real mapped listing, not just that those calls honor a `site_id` kwarg when handed one directly. Both are unit-level (spy fakes for the capacity/fulfillment collaborator, matching each file's own established convention), not full in-process integration -- a real gap in coverage closed at the appropriate proportional level, not a claim to something stronger.
    - **Wording correction:** task 4.6's own note overclaimed "call-counting proof that reserve()'s real endpoint is always hit" for `test_aggregation.py`'s tests, which exercise a `FakeSite` collaborator, not an HTTP boundary. Corrected to state what's actually proven at each level (unit: the aggregate always calls its configured site's method rather than answering locally; integration, separately: a live refusal beats a cached projection).
    - **Bare-metal legacy-key compatibility, previously only reasoned about by analogy to VM, now explicitly tested.** Traced the actual schema (`listing_id TEXT PRIMARY KEY`, `derivation_key TEXT NOT NULL UNIQUE`, separate constraints) to confirm a pre-length-prefix row and a freshly re-derived one never conflict on either key, since `listing_id` is always freshly assigned by `create_listing` and never reused -- confirmed by tracing where `listing_id` actually comes from, not assumed. New test proves both rows coexist without error, matching the accepted idempotent-re-derivation outcome already relied on for VM.
    - **Comment hygiene re-checked and found genuinely non-compliant despite the earlier "clean" claim.** 6 confirmed violations fixed (4 test docstrings referencing "a prior version," "confirmed necessary, not assumed," "this task exists to prevent," "closes the gap X left open"; 2 production comments with temporal/cross-file-provenance framing). Ran a broader free-text sweep afterward (not just the mechanical section/task-number grep relied on all session) across every file this section touches -- one further hit, checked directly, confirmed a false positive.
    - Full regression sweep after all of the above: 1625 tests passed across every package touched this change, every remaining failure matching the confirmed pre-existing/environment-only set.
- [x] 4.5 Bare-metal parity, re-verified fresh rather than reused from earlier findings: no `.reserve()` call anywhere in `domains/bare_metal` (capacity reservation isn't built there yet), schema already correctly site-scoped. Found and fixed, while double-checking, the same key-collision class 4.1 fixed for VM, also present in `bare_metal_listing_key` -- same fix, duplicated locally rather than cross-domain imported (domains don't depend on each other for this). 4 new collision-resistance tests; one stale hardcoded-key-format test assertion caught and fixed while running the full suite.
  - **Implemented (2026-08-04), all findings above carried through to real code, plus two things the plan did not anticipate:**
    - `SiteCapacityClient` added to `kit/site-client`, methods moved mechanically with behavior preserved exactly (verified against the real `FakeSite` fixture, not just by inspection); `register_resource`/`subscribe`/`emit_local`/`bus` dropped as planned. `core_storefront/capacity_remote.py` now contains only `site_events_poller`, typed to take `client: Any` deliberately -- `core_storefront` must not depend on `kit`.
    - **Found during verification, not anticipated in planning: `CapacityClient`'s Protocol (`core_storefront/capacity.py`) still declares `subscribe()` as a required method, and a test asserting the per-site client satisfies that full protocol broke once `subscribe()` was dropped.** Investigated rather than assumed a fix: traced every `isinstance(x, CapacityClient)` check in the repository -- the only other one checks `AggregateCapacityClient` (which still has its own `subscribe()`, unaffected). The per-site client satisfying the full protocol including `subscribe` was never functionally load-bearing; it was incidental to the now-confirmed-dead bus code. Removed the test rather than resurrecting dead code to satisfy it.
    - **Found during the repo-wide sweep, not anticipated in planning: a second test file was testing `RemoteCapacityClient` in complete isolation, equally misplaced.** `core/storefront/tests/integration/test_capacity_client_opacity.py` (proving the opaque-reservation-boundary invariant) had zero `core_storefront`-specific coupling -- moved to `kit/site-client/tests/unit/test_opacity.py`, old file deleted, verified passing. The comment in `provisioning/compute/tests/integration/test_fulfillment_client_opacity.py` cross-referencing it by path was also updated.
    - All import sites fixed directly, no alias, as instructed: `domains/vms/storefront/services/capacity_client.py`, `domains/apicredits/storefront/services/capacity_client.py`, `apicredits_storefront/startup.py`'s comment, `domains/vms/storefront/tests/fake_site.py`, `test_capacity_reservation_boundary.py`, `test_publish_reconcile.py`, `test_site_projection_cache.py`'s fake docstring, and `provisioning/compute/service/tests/integration/test_capacity_api.py` (6 reference points). `test_capacity_reservation_boundary.py` also genuinely has zero VM-storefront-specific coupling and could arguably move too, but relocating it would require adding `sqlalchemy`/`fastapi`/`market_site` to `kit/site-client`'s current minimal `httpx`+`pydantic` footprint -- a bigger decision than this migration was scoped for, so its import was fixed in place and the location question left as a deliberate non-action, not an oversight.
    - `test_remote_capacity_client.py` split as planned: 3 pure client tests moved into `kit/site-client/tests/unit/test_client.py` (verified 9/9 passing there); everything else stays, with the module docstring corrected to describe the file's narrower remaining scope (storefront orchestration, not the wire contract).
    - `FakeSite` duplicated into `kit/site-client/tests/fake_site.py` as planned (the pure capacity-API-scoped class body only, not the storefront-specific `site_capacity()`/`aggregate_over()` helpers, which stayed behind since they patch `build_capacity_client`, a storefront-only concern).
    - Dependency wiring: `domains/vms/storefront/pyproject.toml` gained `arkhai-kit-site-client` as a real dependency, and its own `Makefile` `reinit` target (found and fixed the same way as `provisioning/compute/service`'s earlier) got the matching forced-reinstall entry. `provisioning/compute/service/pyproject.toml`/`Makefile` swapped `arkhai-core-storefront` for `arkhai-kit-site-client` in the dev group. Confirmed via the existing `dist-kits` prerequisite chain (already covers `dist-site-client`) that no new build-target wiring was needed anywhere.
    - `docs/development/ARCHITECTURE.md` line 165 and `openspec/specs/site-capacity/spec.md` (two separate evidence-pointer lines, both caught) updated to the new class name and location -- the latter is a permanent spec making a factual claim about exact test-file paths, not a historical record, so leaving it stale would have been a real documentation-accuracy regression, not just an oversight of convention.
    - **Verified the fix actually achieves its stated goal, not just that tests pass:** ran `provisioning/compute/service`'s full suite and `test_capacity_api.py` specifically with `core/storefront` deliberately excluded from the test environment -- passes cleanly, confirming the dependency is genuinely gone, not just moved to a different import that still resolves by accident.
    - Full regression sweep across every package touched by this task: 1781 tests passed (`domains/vms/storefront` unit+integration, `core/storefront`, `domains/bare_metal`, `domains/bare_metal/storefront`, `kit/site-client`, `provisioning/compute/service`, `provisioning/compute`, `domains/apicredits/storefront`), 5 remaining failures all individually confirmed matching the same pre-existing/environment-only set established earlier in this change (2 FastAPI/Starlette version mismatch, 2 missing native toolchain, 1 unrelated negotiation-logic issue in an untouched file). Comment hygiene clean, including a broader free-text sweep beyond the mechanical section/task-number grep.

- [x] 4.6 Regression proof that projections never participate in live admission/dispatch. Confirmed structurally first (`aggregation.py` has zero references to any projection cache -- not just unused, currently impossible to reach) and investigated "provider dispatch" specifically before proceeding: the provisioning service's actual scheduler operates in a locked DB transaction against the live ledger, and more fundamentally runs in an entirely separate process from the storefront's cache with no shared memory -- no distinct provisioning-side test needed. Two test layers added, at the appropriate level each: unit-level call-counting proof that `reserve()` always calls its configured site's `reserve()` method rather than answering locally (`test_aggregation.py`, against a `FakeSite` collaborator -- both placement and pinned paths, refusal and success), and an integration-level end-to-end proof, against a real in-process app with an external HTTP boundary, that a live refusal wins over an optimistic cached projection (`test_admin_api.py`). The second was verified as a genuine trip-wire, not a tautology, by actually injecting the exact regression the task exists to prevent and confirming the test failed -- which caught a real gap in the test itself (missing `listing_id` meant it silently exercised the untouched placement path) before it was trusted.

- [x] 4.7 Move the buyer-facing site-authority HTTP client out of `core_storefront.capacity_remote.RemoteCapacityClient` into `kit/site-client`, renamed `SiteCapacityClient`. **Raised by external review as a package-layer violation exposed by this section's own work, not pre-existing scope creep -- tracked as part of POOLS-8 per direct instruction, since it is a testability fix discovered while verifying this section's own new integration test, not an unrelated cleanup.** `provisioning/compute/service`'s dev-only dependency on `arkhai-core-storefront` (the earlier fix note above) is architecturally backwards regardless of dependency-group scoping: a composition root should not need to import another role's own package to verify its API against that role's typed consumer. `docs/development/ARCHITECTURE.md` (line 165) already documents the target shape -- two separately typed site-authority clients, buyer-facing and admin -- and `kit/site-client` already houses the admin half (`SiteCapacityAdminClient`); only the buyer-facing half is in the wrong package today.

  **Grounded against the exact current code before planning, not assumed from the review's own description:**
  - Confirmed `kit/site-client` already exists, already has `SiteCapacityAdminClient`, and its own module docstring already says "Deliberately separate from `core_storefront.capacity_remote.RemoteCapacityClient`" -- the destination package requires no new scaffolding, only a new sibling class.
  - Confirmed `ARCHITECTURE.md` line 165 and line 207 verbatim -- the two-client model and `AggregateCapacityClient`'s storefront-owned `site_id` boundary are both already normatively documented; this task completes an already-stated design, not new architecture.
  - Confirmed `RemoteCapacityClient.register_resource()` has zero production callers anywhere in the repository -- traced every call site; the one plausible candidate (`apicredits_storefront/startup.py`) uses `SiteCapacityAdminClient` instead, and its own comment states `RemoteCapacityClient` "only reads/commits, never registers." Drop this method entirely rather than migrate it -- it duplicates `SiteCapacityAdminClient`'s own surface and was never actually reachable.
  - **Found something the review's own description didn't identify: `subscribe()`, `emit_local()`, and the constructor's `bus: CapacityEventBus | None` parameter are entirely dead code, not "storefront orchestration to leave behind."** Traced `site_events_poller` (the only place a delta could plausibly reach this bus) directly -- it calls `aggregate.emit_site_delta(...)`, never `client.emit_local(...)`. Traced every `.subscribe(`/`.emit_local(` call site in the repository -- all real callers are on `AggregateCapacityClient` (which owns its own, separate `CapacityEventBus`), none on `RemoteCapacityClient`. Traced every `RemoteCapacityClient(...)` construction site -- none pass `bus=`. This entire concern was vestigial, likely predating `AggregateCapacityClient` gaining its own bus. Drop it entirely; nothing needs a new home for it.
  - `set_topology_error_handler` is a generic HTTP-client-level hook (installs a callback invoked after topology-sensitive response codes from the client's own `_post`) -- moves with the client, not storefront-specific despite the handler a caller installs potentially being so.

  **Resolved shape:**
  - `kit/site-client/src/market_site_client/client.py` gains `SiteCapacityClient` alongside the existing `SiteCapacityAdminClient`: `__init__` (no `bus` parameter), `base_url`, `_headers`, `_http`, `_get`, `_post`, `set_topology_error_handler`, `snapshot`, `resource_pool_projection_version`, `resource_pool_projection`, `capacity_bucket_projection_version`, `capacity_bucket_projection`, `probe`, `reserve`, `commit`, `release`, `truncate_lease`, `list_reservations`, `events_after`. `kit/site-client/__init__.py` exports it alongside the admin client. `kit/site-client/pyproject.toml`'s own package description (currently says buyer-facing use is covered by `core_storefront.capacity_remote.RemoteCapacityClient`) gets corrected in the same change, not left stale.
  - `core_storefront/capacity_remote.py` loses the `RemoteCapacityClient` class entirely; keeps `site_events_poller` (genuinely storefront-orchestration -- takes a domain `full_reconcile` callback, core never interprets listings) and whatever of its current imports `site_events_poller` alone still needs.
  - **No compatibility alias, confirmed direct per instruction:** the full list of production importers is small and enumerated, not estimated -- `domains/vms/storefront/services/capacity_client.py`, `domains/apicredits/storefront/services/capacity_client.py`, plus the two package-level docstrings named above. All updated atomically to `from market_site_client import SiteCapacityClient` in the same change.
  - `domains/vms/storefront/pyproject.toml` gains `arkhai-kit-site-client` as a real dependency (not dev-only -- this is genuine production usage, unlike provisioning's test-only need). `provisioning/compute/service/pyproject.toml`'s `arkhai-core-storefront` dev dependency (the earlier fix note) is removed and replaced with `arkhai-kit-site-client` dev dependency; `test_capacity_api.py` updated to import `SiteCapacityClient` from `market_site_client` instead of `RemoteCapacityClient` from `core_storefront.capacity_remote`. `domains/apicredits/storefront` already depends on `arkhai-kit-site-client` (for the admin client) -- no new dependency needed there, only an import-path update in its own `capacity_client.py`.
  - **Test split, planned precisely rather than moving the whole file:** `domains/vms/storefront/tests/unit/test_remote_capacity_client.py` mixes pure client-contract tests with storefront orchestration built on top of the client (subscriber/reconciliation tests, `AggregateCapacityClient` placement tests, `build_capacity_client` composition tests, `site_pool_projection()`/cache tests) -- only the former move. Moving: `test_remote_client_speaks_the_capacity_wire_contract` (the comprehensive snapshot/probe/reserve/commit/truncate/release/events_after scenario), `test_commit_without_capacity_reservation_id_is_an_error`, `test_list_reservations_filters` -- into `kit/site-client/tests/unit/test_client.py`, alongside the existing `SiteCapacityAdminClient` tests. Everything else (`test_member_availability_view_reflects_consumption` and the remaining dozen-plus tests) stays, since it tests storefront's own orchestration, not the client itself, and continues to import the client from its new location.
  - `domains/vms/storefront/tests/fake_site.py`'s `FakeSite` is confirmed purely `/api/v1/capacity/*`-scoped with zero storefront-specific coupling (checked its full route table directly) -- a clean fixture to duplicate into `kit/site-client/tests/`, not move outright, since other storefront test files still depend on the original. Matches this session's own precedent (`_length_prefixed`, duplicated into bare-metal rather than cross-domain imported) for the same reason: kit cannot depend on a domain package's test helpers, and this fixture is small and stable enough that duplication is cheaper than the alternative coupling.
  - `docs/development/ARCHITECTURE.md` line 165's `RemoteCapacityClient` reference is updated to `SiteCapacityClient` (`kit/site-client`) in the same change, not left describing a class that no longer exists there.

### Fix note: raw HTTP calls in `test_admin_api.py` (found and fixed 2026-08-04, separate from Section 4's own scope)

Found while re-checking task 4.6's new test against `docs/development/TESTING.md`'s
"no raw calls" rule -- not something Section 4 set out to do, and not folded into
its closeout narrative below; recorded here as its own fix.

`domains/vms/storefront/tests/integration/test_admin_api.py`'s entire
`TestFulfillmentEvents` class -- pre-existing tests, not just the two added this
section -- called `c._post(<path>, <hand-built body dict>, ...)` directly against
route strings instead of the canonical typed `StorefrontClient` methods, for every
one of its fulfillment-event and reservation requests. This is exactly the gap
`TESTING.md` names: a happy-path test that bypasses the typed client stops testing
the client contract at all, and would not catch a route rename or field-shape
change the way a typed-method call would.

- `/api/v1/admin/portfolio/reservations` already had a typed method
  (`admin_reserve_capacity`) that nothing was using. Converted all 5 call sites
  in the file; also updated their downstream assertions from dict-style
  (`response["x"]`) to attribute access (`response.x`), since that method returns
  a parsed `ReserveCapacityResponse` dataclass, not a raw dict.
- `/api/v1/admin/fulfillment/events/capacity-released` already had a typed method
  (`notify_capacity_released`) -- converted its 2 call sites.
- `/api/v1/admin/fulfillment/events/usage-started` and
  `/api/v1/admin/fulfillment/events/failed` had no typed client method at all --
  added `notify_usage_started` and `notify_fulfillment_failed` to
  `core/storefront-client/src/storefront_client/client.py`, matching
  `notify_capacity_released`'s exact pattern (plain `dict` return, optional-field
  body construction), then converted the remaining 5 call sites.
- Zero raw `_post` calls remain in the file. Full file re-run: 38/38 passing.
  Full regression sweep across every package touched this change: unchanged from
  the pre-fix baseline (1618 passed, same pre-existing/environment-only failures
  as already confirmed repeatedly), confirming this was a pure test-quality fix
  with no behavior change.

### Fix note: verification-sandbox packaging gaps masked 3 real bugs (found and fixed 2026-08-04)

**A real correction to this whole change's own reported regression counts, not just a new fix.** Every "matches the confirmed pre-existing/environment-only set" claim recorded throughout this document was checked against a baseline established in a sandbox that never did a proper `pip install -e .` of any package -- only manually-constructed `PYTHONPATH` strings. Prompted by a real failure report from a properly-installed environment showing a *different* failure reason than expected for two supposedly-pre-existing tests, investigated directly rather than assumed: entry points and console scripts are resolved from installed package *metadata*, not from `sys.path` membership, so a package importable via `PYTHONPATH` can still be structurally invisible to `importlib.metadata` -- and every test that depends on that resolution (`core_storefront.publication_plugins`' domain-plugin lookup, console-script/version-metadata checks) fails at that resolution step before ever reaching the behavior it's meant to test, silently absorbing whatever real failure exists underneath.

Properly installed every package this change's own tests touch (`domains/vms/domain`, `domains/vms/storefront`, `domains/bare_metal`, `domains/bare_metal/storefront`, `domains/apicredits/buyer`, `domains/apicredits/storefront`, `kit/alkahest`, `provisioning/compute/service`, each via `pip install -e . --no-deps`) and re-ran every suite this change has touched. The "16 confirmed pre-existing failures" long carried for `test_cli_publish_helpers.py` were **not real** -- 14 were entirely masking artifacts that disappear the moment entry points resolve correctly, and the remaining 2 were real, previously-invisible bugs:

- `test_publish_round_publishes_one_listing_per_available_slice` and `test_publish_round_reopens_existing_derived_listing_id` both asserted against the pre-length-prefix, non-site-scoped `derivation_key` format (`"compute-4x:gpus:1"`), never updated when task 4.1 added site-scoping and the collision-resistant length-prefixed encoding -- because every run of this file in this sandbox failed at the entry-point step first, the stale assertions were never actually exercised or noticed. Fixed both: computed expected keys dynamically via the real `listing_resource_key` function rather than hardcoding a format (matching the pattern already used elsewhere this session for the same class of fix); the second test also had its setup rewritten from a hand-rolled pre-site_id schema and raw `INSERT` to the real `ensure_derived_compute_listings_table`/`record_derived_listing` functions.
- `domains/bare_metal/storefront/tests/test_http_system.py::test_health_is_truthful_about_uncomposed_authorities` asserted exact equality against a health response that predates `HealthResponse` gaining a `site_projections` field (added to the shared `core_storefront` model earlier this session) -- masked the same way, by the entry-point failure occurring first in every prior run. Fixed: added the new field (`None`, since bare-metal has no per-site projection concept) to the expected response.
- Two further bare-metal/storefront test failures (`test_console_entry_point_is_installed`, `test_version_command_uses_distribution_metadata`) turned out to be a second, related sandbox gap, not a code issue: `domains/bare_metal/storefront` is a separate package from `domains/bare_metal` and had never been installed at all. Installing it resolved both with no code change needed.
- Two remaining failures (`test_server_app_composition.py`, and bare-metal/storefront's `test_app_composition.py`, both `AttributeError: '_IncludedRouter' object has no attribute 'path'`) are a real FastAPI/Starlette version mismatch in this sandbox's installed dependencies -- confirmed genuinely unrelated by diffing both the test files and the production code they exercise against the pristine original: byte-identical, never touched by any work in this change.
- Two more (`test_alkahest.py`'s two tests) confirmed as a missing native Node.js/Rust/Foundry toolchain, not reachable in this sandbox at all -- matches this change's own repeated prior characterization exactly, now verified rather than assumed.
- One more (`test_negotiate_controller.py::test_amountless_exact_escrow_can_start_and_accept`) is a genuine pre-existing test failure in negotiation counter-offer logic, confirmed unrelated to this change's scope (site-capacity/reservation routing) by diffing the test file and finding it byte-identical to the pristine original -- a real, separate defect worth someone's attention, but outside what this change touches.

**Corrected final count, this time verified rather than assumed:** 1691 tests passed across every package this change has touched (`domains/vms/storefront`, `core/storefront`, `domains/bare_metal`, `domains/bare_metal/storefront`, `provisioning/compute/service`, unit and integration), with exactly 6 remaining failures, each individually traced to a specific, confirmed, unrelated cause above -- not a number arrived at by pattern-matching test names against an unverified baseline. Every prior "matches the confirmed pre-existing set" claim in this document remains true in the narrow sense that the *set itself* was consistent across runs; what was wrong was trusting that set's members were genuinely all unrelated to this change's own code without having verified the two among them that weren't.

### Fix note: missing `arkhai-core-storefront` packaging dependency for `provisioning/compute/service` (found and fixed 2026-08-04, corrected 2026-08-04 after a real scoping mistake caught in review)

Reported as a test failure in a delivered fileset (`ModuleNotFoundError: No
module named 'core_storefront'` in `test_capacity_api.py`'s two
`site_resource_pools` projection tests) -- not caught earlier because this
session's own verification used manually-constructed `PYTHONPATH` strings
that happened to include `core/storefront/src`, masking a real packaging
gap that a `uv sync`/Makefile-driven environment correctly surfaces.

Traced to its root, not just patched around: `core_storefront.capacity_remote.RemoteCapacityClient`
is imported by a test added during earlier Section 3 work (confirmed via
diff against the pristine original -- this import does not exist anywhere
in the unmodified file), but the package providing it
(`arkhai-core-storefront`, distinct from the already-declared
`arkhai-core-storefront-client`) was never added to `provisioning/compute/service`'s
own dependency chain -- latent since Section 3, not introduced by this
section's own work, only surfacing now under real packaging tooling.

**First fix was wrong in scope, caught directly:** initially added
`arkhai-core-storefront` to `pyproject.toml`'s production `dependencies`
list. Checked afterward, per direct question, whether the provisioning
service has any real reason to depend on storefront code -- it does not:
`grep`-confirmed `core_storefront` is referenced nowhere in
`src/compute_provisioning_service/` at all, only in this one test file.
`RemoteCapacityClient` is used here to verify this service's own capacity
API is correctly consumable by the storefront's real, canonical client
(the "no raw calls" integration pattern this whole session has enforced),
not because the deployed service needs storefront business logic at
runtime. **Corrected:** moved to `[dependency-groups] dev` (with a
matching `[tool.uv.sources]` editable-path entry, matching every other
local package's own entry), not the production dependency list -- a
deployed service wheel should never need to import the other side's code.
The `reinit` target's forced-reinstall entry stays (dev dependencies still
need `uv sync --dev` to pick this up correctly), but the root `Makefile`'s
`dist-compute-provisioning-service` prerequisite on
`dist-arkhai-core-storefront` was reverted -- production wheel building
never touches dev-only dependencies, so that prerequisite was unnecessary
once the dependency moved to the right group.

`capacity_remote.py` is documented in its own module docstring as
"domain-neutral" and has minimal dependencies (one sibling module,
`httpx`) -- a real candidate for eventually living in `kit/` as genuinely
shared code between the two services, rather than one service depending
on the other's package even as a dev-only dependency. Not done here --
raised for a deliberate decision, not undertaken as part of this fix.


**Second correction: the `[tool.uv.sources]` editable-path entry itself was the wrong pattern, caught directly by review rather than repeated as precedent.** Relative parent-directory path sources are explicitly disallowed by an existing, already-planned (not yet implemented) change, `openspec/changes/remove-relative-uv-sources/proposal.md`, which names "compute contract, and compute service projects" -- i.e. `provisioning/compute` and `provisioning/compute/service` -- as two of its five target projects precisely because they already carry this anti-pattern. My fix had extended it with a fourth entry rather than avoiding it. Per direct instruction not to extend a known-bad pattern while already in the file, and to fix the other violations found while here: removed the entire `[tool.uv.sources]` section from `provisioning/compute/service/pyproject.toml` (all 7 entries -- `arkhai-compute-provisioning`, `arkhai-core-storefront`, `arkhai-core-storefront-client`, `arkhai-bare-metal`, `arkhai-bare-metal-provisioning-adapter`, `arkhai-vms-provisioning-operator-client`, `arkhai-vms-provisioning-adapter`), converting fully to the wheel-based `--find-links $(DIST_DIR)` resolution already used by this same file's other dependencies (`arkhai-kit-site`, `arkhai-kit-resource-pools`, `arkhai-kit-fulfillment`). Confirmed every package this removes now needs a corresponding `reinit` forced-reinstall entry to stay fresh: added the two that were missing (`arkhai-core-storefront-client`, `arkhai-bare-metal`) alongside the ones already there. Confirmed via the root `Makefile` that wheels for all seven already exist in the `dist` chain (`dist-domains`, `dist-storefront-client`, `dist-compute-provisioning`, `dist-arkhai-core-storefront`), so no new build-target work was needed -- this file's `reinit`/`dist` infrastructure was already correctly shaped for the wheel-only pattern; only the leftover path-source entries needed removing. Doing the full scope of `remove-relative-uv-sources` (the other four target projects, the repository-wide path-source-rejecting check, `RELEASING.md` corrections) is explicitly out of scope here -- that is a separate, already-planned change with its own sequencing, not something to absorb into a dependency-scoping fix.

**On whether `RemoteCapacityClient`/`core_storefront.capacity` should actually move to `kit/`, asked a second time, answered concretely rather than left as a vague "worth considering":** traced `core_storefront.capacity`'s own imports directly -- zero internal dependencies at all, stdlib only (`itertools`, `logging`, `dataclasses`, `typing`). Combined with `capacity_remote.py`'s own minimal footprint (one sibling module, `httpx`), this pair is a clean, low-risk extraction candidate on its own merits. Checked the blast radius directly rather than estimating: `kit/site-client` already imports from `core_storefront.capacity` today, independent of anything in this change -- confirming this is a genuine cross-cutting concern already leaking into a `kit` package the wrong way around, not a hypothetical future need. The full blast radius also includes `domains/vms/storefront/services/capacity_client.py` and `core_storefront/aggregation.py` -- the two most central files in this whole section's own work (task 4.3's projection redirect, task 4.4's pinned-site `reserve()`). Still not undertaken here: a migration touching this section's own most central files is a deliberate, separately-scoped change in its own right (the same class as `remove-relative-uv-sources`), not something to fold into a review-round dependency fix, even with concrete evidence now in hand that it's the architecturally correct destination.

Validated the corrected TOML parses, `test_capacity_api.py` passes with
the dev dependency correctly resolvable in this sandbox's own manual
`PYTHONPATH` setup, and the root Makefile change reverts to byte-identical
with the pristine original. Full `make dist`/`make reinit` execution still
could not be verified end-to-end in this sandbox (no git repository, full
build toolchain unavailable here) -- needs confirmation in a real
environment.

### Section 4 closeout (per `openspec/README.md#plan-closeout-requirements`, "or a major section within one")

- [x] Comment hygiene: clean across every file this section touches (re-checked after each round of changes, including this closeout's own).
- [x] Documentation compliance: re-checked against `openspec/README.md`'s placement rules directly. Added the rows the Design promotion record was missing for this session's later decisions (`gpu_model` flow, the `None`-vs-`[]` projection semantics, collision-resistant key encoding, the availability-precedence fix below).
- [x] Narrative compression: task list above rewritten to final behavior, material validation evidence, and permanent-doc destinations; detailed alternatives (the `use_projection` redesign rejection) moved into `design.md`, "Section 4 design" first, not just deleted.
- [x] Promotion: **completed 2026-08-04, per direct instruction.** All 6 of Section 4's own accepted decisions promoted into their permanent destinations, not just recorded: `openspec/specs/storefront-publication/spec.md` gained two new requirements, "Commercial mapping identity" (covering both the site-scoped mapping-table decision and the collision-resistant derivation-key encoding) and "Site-pinned claim routing" (the `reserve(site=...)` dispatch, applying to fungible and resource-pinned listings alike). `openspec/specs/site-capacity/spec.md` gained two new scenarios under the existing "Per-site projection load-state visibility" requirement (the `None`-vs-`[]` distinction, and the availability-precedence fix) and a new paragraph plus scenario under "Resource-pool projection metadata" (`gpu_model` omitted, not null, when unset). Evidence lines updated in both specs to point at the real tests proving each claim. Found and fixed while doing this: the promotion table's own anchor references (`#projection-load-state`, `#commercial-mapping`, `#claim-routing`) didn't match the actual requirement-title-derived anchors OpenSpec would generate (`#per-site-projection-load-state-visibility`, `#commercial-mapping-identity`, `#site-pinned-claim-routing`) -- corrected throughout the table, including the one row belonging to Section 2 (not this section's to promote, but the same anchor, so leaving it wrong while fixing the others would have been inconsistent). The remaining unpromoted rows in the Design promotion record belong to Sections 1, 2, 5, and 6, not this section.
- [x] One real orchestration *integration* test for cache -> reconciliation was still owed (flagged in an earlier review round) -- built during this closeout (`test_admin_api.py::TestRealOrchestrationCacheToReconciliation`), real `ProjectionCache`, real subscriber, real `CapacityDelta`, real `SQLiteClient`, nothing patched. It caught a real bug on first run (`_projected_resource_usage`'s condition order, detailed in task 4.3 above) and two further test-environment issues (a fixture identifier mismatch, and `close_order`'s fallback resolving the SQLite client through a separate global singleton rather than the test's own db) -- all three fixed, not worked around, debugged with direct instrumentation rather than guessing once behavior didn't match expectation. Final regression sweep: 1620 tests passed across every package touched this change, every remaining failure matching the confirmed pre-existing/environment-only set.

## 5. Implement advisory hints

**Grounded (2026-08-03, `design.md`, "Section 5 design"):** `listing_mode` formalizes a pooled-vs-specific-resource publication decision `domains/vms/listings/reconciler.available_compute_slices` already makes today, implicitly, from local `compute_pool_members` row counts — this also corrects Section 1's "no confirmed reader" classification of `compute_capacity_pools`/`compute_pool_members`; they are live-read by that function, not orphaned. **Open questions, status after 2026-08-03 follow-up (`design.md` → "Section 5 design" → "Open questions"): Q1 (naming) finalized — `"fungible"/"specific_resource"`; Q2 no action; Q3 (site- vs resource-pinning) resolved, folded back into Section 4's task 4.4; Q4 (bare-metal transferability) investigated — capacity reservation for bare metal turned out to be an already-open, partially-complete change rather than a gap: `openspec/changes/market-platform-bare-metal-10-storefront-composition`'s Sections 3–4 own exactly this, stalled at exactly this point, and were resumed/updated (not duplicated) with POOLS-8's routing fix as a new explicit dependency (see task 5.4 below).**

- [ ] 5.1 Define `listing_mode` and `max_reservation_hold_seconds` key constants and nonnegative hold validation in `kit/resource-pools` (e.g. `hints.py`: `LISTING_MODE_POLICY_TAG`, `MAX_RESERVATION_HOLD_SECONDS_POLICY_TAG`, `raw_listing_mode`, `max_reservation_hold_seconds`, `capped_hold_seconds`). Values: `"fungible"` / `"specific_resource"` (finalized 2026-08-03 — matches `is_fungible_pool`/`pool_id`'s docstring on the VM side and appears verbatim in bare-metal's `count_open_bare_metal_resources` docstring).
- [ ] 5.2 Add VM and bare-metal listing-mode enums/resolvers with structural defaults and operator-visible invalid-value explanations. VM: `resolve_vm_listing_mode` (`"fungible"` default, matching today's member_count>1 structural default; `"specific_resource"` otherwise), replacing the `member_count == 1` heuristic in `available_compute_slices` once Section 4 redirects it to the projection. Bare metal: trivial, always `"specific_resource"` — no pooled concept exists for `derived_bare_metal_listings`, kept only for resolver symmetry.
- [ ] 5.3 Add API-credit hint interpretation only if a concrete publication consumer exists; otherwise prove unknown values remain opaque and defer its enum. **Resolved by investigation: defer, confirmed.** `domains/apicredits` exists and its service uses `market_site.ledger.CapacityLedgerService` directly, but nothing in `domains/apicredits` imports `market_resource_pools` — it has no `ResourcePool`/`policy_tags` concept at all (keys/quota against the base ledger, not GPU-style pools), so there is nothing for an apicredits `listing_mode` resolver to read yet.
- [ ] 5.4 Cap storefront acceptance-hold TTL by a valid projected preference without changing site-ledger admission rules. Concrete target: `sync_negotiation.py`'s acceptance-hold placement, which today applies one global `settings.capacity.hold_ttl_seconds` uniformly. Add a `lookup_pool_policy_tags` lookup (same `site_id` resolution Section 4.4 needs) and pass the result through `capped_hold_seconds(ttl, policy_tags)` before calling `reserve()`. Matches this function's existing fail-open posture: an unresolvable lookup leaves `ttl` unchanged rather than blocking hold placement, same as `capped_hold_seconds` already does for an invalid/missing tag. **VM-only, confirmed deliberately, not by default:** investigated bare-metal transferability in full (`design.md`, Section 5 Q4) — bare metal has no capacity-reservation flow at all (no `AggregateCapacityClient` usage, no `reserve()` call, no hold/TTL placement anywhere in `domains/bare_metal`), so there is nothing for this task to cap there. Notably, bare-metal's *publication* side (`TrustedBareMetalProjection`, already site/revision/digest/stale-modeled, already projection-native listing derivation) is ahead of VM's, not behind — only capacity reservation is the gap. **Correction (2026-08-03): this is not a net-new gap needing a new change.** `openspec/changes/market-platform-bare-metal-10-storefront-composition` already owns exactly this (its Sections 3–4, unstarted, stalled at precisely this point) — that change was updated with POOLS-8's `reserve(site=...)` mechanism as a new explicit dependency rather than duplicating it here. A bare-metal equivalent of this task belongs in that change, once its Section 3–4 land, not in POOLS-8.
- [ ] 5.5 Add focused domain resolver, unknown-tag, invalid-hold, old-producer, and non-cooperating-consumer tests.

### Section 5 closeout (per `openspec/README.md#plan-closeout-requirements`, "or a major section within one")

**Added 2026-08-03 as a planning-time stub, per the Section 3 review's process finding** -- not implemented yet, so nothing to close out yet, but the task exists now so implementation doesn't invent it after the fact:

- [ ] Comment hygiene: run the equivalent of `make check-comment-hygiene` against every file this section touches.
- [ ] Import placement: migrate local (function-level) imports to module level wherever safe -- check each for a real circular-import or documented lazy-load reason before moving it, not by default (see Section 4 closeout's own import audit for the concrete method: cross-reference every local import actually added against the diff, then verify each candidate move against the real test suite before keeping it).
- [ ] Documentation compliance: re-check this section's accepted decisions against `openspec/README.md`'s placement rules directly.
- [ ] Narrative compression: shorten completed-task notes to final behavior, material validation evidence, and permanent-documentation destinations once implementation lands.
- [ ] Promotion: complete this section's rows in Section 7's Design promotion record and actually promote into the permanent specs named there, not only record the destination.

## 6. Retire superseded local physical authority

**Grounded (2026-08-03, `design.md`, "Section 6 design"):** retirement is column-level, not table-level, for two tables read in full for the first time here — `resources` and `compute_capacity_pools` both mix physical-identity columns (retirement candidates) with commercial columns that must survive (pricing, seller policy, accepted escrows). `hosts`, `compute_pool_members`, and `resource_transition_events`' go-forward writes are the clean, fully-physical retirement candidates.

- [ ] 6.1 Remove only physical-identity writers/readers proven replaced by projection mapping, including `resource_capacity_validator.py` if its final caller is gone. **Scoped precisely:** `hosts` (all), `compute_pool_members` (all), `resources`' physical columns only (`resource_type`, `resource_subtype`, `unit`, `value`, `state`, `attributes` — not `min_price`/`token`/`max_duration_seconds`/`accepted_escrows`, confirmed present on this table too), `compute_capacity_pools`' physical columns only (`total_gpu_count`, `gpu_model`, `region`, `sla` — not its seven commercial/pricing columns). This cluster retires together (validator reads `hosts`; `upsert_resource` writes `resources` and, via its `_sync_compute_pool_for_resource` side effect, `compute_capacity_pools`/`compute_pool_members`; CSV import calls `upsert_resource`), contingent on Section 4's redirect of `available_compute_slices` landing first. **Resolved (2026-08-03): CSV import (`host_csv_importer.py`/`resource_csv_importer.py`) is removed, not repurposed** — not useful once the rest of the storefront consumes the projection. Still check whether per-resource price overrides (`resources`' commercial columns, distinct from `compute_capacity_pools`' pool-level equivalents) are actually exercised before deciding whether they can simply be dropped in favor of the pool-level copy.
- [ ] 6.2 Preserve pricing, accepted settlement mechanisms, negotiation/agreement history, transition idempotency, and operator metadata in explicitly owned storage. Concretely: `compute_capacity_pools`' seven commercial columns become the durable pool-level pricing-policy record the CLI reads to construct `create_listing` calls; `resource_transition_events`' existing rows are audit history and must not be deleted even once new events stop.
- [ ] 6.3 Add compatibility diagnostics/migrations for existing deployments and a rollback path to the previous reader during staged rollout. **Resolved shape:** this change's own migration work stops at freeze-then-redirect (stop writing/reading the retiring physical columns/tables, redirect reads to the projection) without dropping the underlying schema — a rollback path requires the previous reader's data to still exist. The actual `DROP`/column removal is a follow-up cleanup change after a full deployment cycle confirms rollback is never needed, matching every other schema change in this document being additive-only.
- [ ] 6.4 Run storefront unit/integration, publication, multi-site capacity, API-credit/bare-metal affected, migration, packaging, and e2e suites.

### Section 6 closeout (per `openspec/README.md#plan-closeout-requirements`, "or a major section within one")

**Added 2026-08-03 as a planning-time stub, per the Section 3 review's process finding** -- not implemented yet, so nothing to close out yet, but the task exists now so implementation doesn't invent it after the fact:

- [ ] Comment hygiene: run the equivalent of `make check-comment-hygiene` against every file this section touches.
- [ ] Import placement: migrate local (function-level) imports to module level wherever safe -- check each for a real circular-import or documented lazy-load reason before moving it, not by default (see Section 4 closeout's own import audit for the concrete method: cross-reference every local import actually added against the diff, then verify each candidate move against the real test suite before keeping it).
- [ ] Documentation compliance: re-check this section's accepted decisions against `openspec/README.md`'s placement rules directly.
- [ ] Narrative compression: shorten completed-task notes to final behavior, material validation evidence, and permanent-documentation destinations once implementation lands.
- [ ] Promotion: complete this section's rows in Section 7's Design promotion record and actually promote into the permanent specs named there, not only record the destination.

## 7. Plan closeout (per `openspec/README.md#plan-closeout-requirements`)

**Grounded (2026-08-03):** this section was previously scoped as promotion-only (4 tasks), but `openspec/README.md`'s closeout requirement has four parts, of which promotion is only the last. Comment hygiene, documentation compliance, and narrative compression were missing as explicit tasks. Also found: this document's own promotion table used the heading "Permanent Documentation Promotion" instead of the README's specified "Design promotion record" and a `Decision | Permanent destination` column shape instead of `Accepted decision | Permanent location` with anchor-style destinations — corrected in `design.md` as part of this section's own scope, not left for implementation to notice.

- [ ] 7.1 **Comment hygiene.** Run `make check-comment-hygiene` and resolve every match before this task is marked done. Note this only catches change IDs/section/task numbers mechanically — separately, direct-read every file this change touches for `AGENTS.md`'s fuzzier "no migration/review provenance in comments" rule, which the tool doesn't catch (e.g. don't leave "added for POOLS-8" or "per Section 4" style comments in production code even if `POOLS-8`/`Section 4` as literal strings don't appear).
- [ ] 7.2 **Documentation compliance.** Re-check every accepted decision in `design.md` against `openspec/README.md`'s placement rules directly (subsystem spec vs. `ARCHITECTURE.md` vs. no permanent change) rather than deferring to review. Confirm the "Design promotion record" table (below) is still complete against the final implementation — decisions can shift during implementation in ways this design-phase table can't anticipate.
- [ ] 7.3 **Narrative compression.** Shorten tasks 1–6's completed-task notes to final behavior, material validation evidence, unresolved/deferred work, and permanent-documentation destinations — move detailed alternatives, corrected-finding narratives, and review rationale (e.g. Section 2's struck-through original decision, Section 5's Q1–Q4 investigation trail) into `design.md` first if not already there. This deletes duplication between `tasks.md` and `design.md`, not information.
- [ ] 7.4 **Promotion.** Complete the design-promotion record (`design.md`, "Design promotion record" — already rebuilt as a full audit during this design-review pass, not just the four rows carried forward from early sections). Concretely:
  - [ ] 7.4.1 Promote projection load-state visibility and resource-pool projection metadata (`pool_views`, allowlisted fields) to `openspec/specs/site-capacity/spec.md` and `architecture.md`.
  - [ ] 7.4.2 Promote commercial mapping (`derived_compute_listings`/`derived_bare_metal_listings` extension), direct claim routing (`reserve(site=...)`), and the local-physical-authority retirement boundary to `openspec/specs/storefront-publication/spec.md` and `architecture.md`.
  - [ ] 7.4.3 Promote domain-neutral hint keys, domain-owned values, and pool-identity site-scoping to `openspec/specs/resource-pool-management/spec.md` and `architecture.md`.
  - [ ] 7.4.4 Update `docs/development/ARCHITECTURE.md` only if the accepted design changes repository-wide authority/topology — audit against this change's decisions before assuming none qualify; the site-vs-resource pinning boundary (Section 4/5) and the "kit provides a generic delegate shape, domain supplies coupling" pattern reused for `pool_views` are repository-wide enough to be worth checking against, not obviously local-only.
  - [ ] 7.4.5 Record every promoted decision's final destination in `design.md`'s "Design promotion record," run strict `openspec` validation for this change and every permanent spec it touches, and archive only after current-state documentation matches verified (not just designed) behavior.
