## 1. Reconcile landed projection foundations

- [x] 1.1 Verify independent site resource-pool and capacity-bucket producers, revisions/digests, pull endpoints, and in-memory storefront caches landed under POOLS-7.
- [x] 1.2 Remove rebuilt producer/polling/cache mechanics from this change and record remaining persistence/consumption scope in `proposal.md` and `design.md`.
- [x] 1.3 Resolve global versus site-scoped Resource Pool identity and record the durable/public key shape before schema work. **Resolved (2026-08-03, `design.md`, "Resolved Questions"): `pool_id` is a site-local operator slug, never made globally unique under POOLS-7. Every durable/public reference this change introduces must key on `(site_id, pool_id[, resource_id])`, never `pool_id` alone.**
- [x] 1.4 Inventory every `resources`, capacity-pool, CSV import, validator, publication, claim-building, admin, migration, and e2e reader; classify physical authority versus commercial/operational ownership. **Done (2026-08-03, `design.md`, "Local physical-authority inventory"), with two corrections made in later sections rather than here (left as originally written for history, per the amend-don't-replace convention — see the superseding notes):** Section 4 corrected "two independent listing-creation paths" to one path (CLI is an automated caller of `create_listing`, not a second mechanism); Section 5 corrected `compute_capacity_pools`/`compute_pool_members` from "no external reader" to live-read by `domains/vms/listings/reconciler.available_compute_slices`, which this pass hadn't searched.

### Section 1 closeout (per `openspec/README.md#plan-closeout-requirements`, "or a major section within one")

**Added 2026-08-03, in response to the Section 3 review's process finding, applied retroactively to prevent it recurring.** Section 1 is discussion/investigation only -- no code, no schema, no new tests -- so its closeout is correspondingly light:

- **Comment hygiene:** not applicable; no production code was touched by this section.
- **Documentation compliance:** Section 1's findings (pool-identity scoping, the local physical-authority inventory) are recorded in `design.md` and feed later sections' own promotions (task 1.3's identity resolution -> `resource-pool-management/spec.md#pool-identity`, listed in the Design promotion record); Section 1 has no distinct permanent-doc destination of its own.
- **Narrative compression:** already reasonably compressed; the two corrections noted in 1.4 (made in Sections 4 and 5 rather than here) are cross-referenced, not duplicated.
- **Promotion:** none owed directly by this section; see Section 7's Design promotion record for where its findings ultimately land.

## 2. Surface per-site projection load state (revised 2026-08-03; no persistence layer)

**Revised in discussion 2026-08-03** (`design.md`, "Section 2 revised decision"): the originally planned durable persistence layer (migrations, repositories, seeded-cache restart recovery) is not being built — no production code consumed the projection caches yet, so the restart-time gap it targeted had no live consumer to protect. Full rationale, the two-round review trail, and every corrected finding (the `HealthResponse` field silently dropped by Pydantic's `extra="ignore"`, the module-level-patching test fix, `fetched_at` semantics) live in `design.md`'s "Section 2 design discussion" — not duplicated here.

- [x] 2.1 Add per-`(site_id, projection_kind)` load-state fields to the storefront's existing operator status surface, following the existing `resource_count`-diagnostic pattern. **Done, reopened once by external review and re-fixed (2026-08-03):** `site_projection_cache.projection_status_summary()` wired into `SystemService.get_health` under `result["site_projections"]`; `core_storefront.models.system_models.HealthResponse` and `storefront_client.models.HealthResponse` both gained the field for real (the first pass silently dropped it before reaching HTTP callers); `fetched_at` tracking added to `ProjectionCache`/`ProjectionCacheView`.
- [x] 2.2 Confirm the poller already retries indefinitely on a site that has never successfully loaded, and add a regression test. **Done (2026-08-03):** confirmed by tracing `ProjectionCache.load()`/`refresh()` (every failure path catches internally, never raises); covered by `domains/vms/storefront/tests/unit/services/test_site_projection_cache.py`.
- [x] 2.3 Document "ignorance ≠ zero" for Section 4's future projection consumer. **Done (2026-08-03):** promoted to `openspec/specs/site-capacity/spec.md#per-site-projection-load-state-visibility`.
- [x] 2.4 Add focused tests for per-site/family status reporting. **Done, corrected once by external review (2026-08-03):** `test_system_service.py` rewritten from module-level patching to constructor injection (`TESTING.md`'s own rule); real HTTP round-trip integration tests added to `test_admin_api.py`'s `TestHealthEndpoint`.
- [x] 2.5 Confirm POOLS-7 task 2.3's durability note is already satisfied. **Done (2026-08-03):** closed directly in `pools-7-storefront-fulfillment-cutover/tasks.md`, no code needed here.
- [ ] 2.6 **Real test debt, not this section's to close, still open:** no integration test runs the real `RemoteCapacityClient` against a real in-process provisioning app for the projection endpoints (`resource_pool_projection`/`capacity_bucket_projection`/their `_version` siblings) -- `test_capacity_api.py`'s `CapacityApi` helper covers reserve/commit/release/snapshot/probe/events but not these. Growing in value as more work depends on projected data; worth its own task whenever picked up.

**Permanent documentation destination:** `openspec/specs/site-capacity/spec.md#per-site-projection-load-state-visibility`.

<details>
<summary>Original tasks 2.1–2.4 (persistence layer), superseded — kept for history</summary>

- [ ] ~~2.1 Add storefront migrations and repositories for trusted configured-site bindings and independent resource-pool/capacity-bucket generations.~~
- [ ] ~~2.2 Persist revision, digest, accepted value, fetched time, and stale/error state transactionally per `(site_id, projection_kind)`.~~
- [ ] ~~2.3 Load complete stored generations as stale on restart before polling and replace one family without mutating the other.~~
- [ ] ~~2.4 Add migration, restart, partial-refresh, malformed-payload, revision-reset, and stale-retention tests.~~

</details>

### Section 2 closeout (per `openspec/README.md#plan-closeout-requirements`, "or a major section within one")

- [x] Comment hygiene: clean, re-verified via the real `make check-comment-hygiene` as part of Section 7's own repo-wide run.
- [x] Documentation compliance: "Requirement: Per-site projection load-state visibility" promoted with scenarios and evidence.
- [x] Narrative compression: done as part of Section 7 (this task list compressed 2026-08-06; full trail retained in `design.md`).
- [x] Promotion: done (see documentation compliance above).

## 3. Project safe pool metadata

Full design rationale and the corrected-finding trail (the shallow-copy mutation bug, the `AnsiblePoolConfig` persistence prerequisite, the provider-mechanism gating fix, the second-review-round test-evidence corrections) live in `design.md`'s "Section 3 design" — not duplicated here.

- [x] 3.1 Define allowlisted pool label/enabled/mechanism/policy-tags/generic `pool_views` in the site resource-pool projection, mirroring the existing `publication_views` precedent. **Done (2026-08-03):** `_POOL_METADATA_FIELDS`/`_project_pool_metadata` in `kit/site/src/market_site/projections.py`; `resource_pool_projection`/`SiteProjectionService`/`make_capacity_router` all threaded through; no VM-storefront-client change needed (confirmed by running the existing suite unmodified).
- [x] 3.2 Advance revision/digest on metadata changes; prove credentials/provider secrets are redacted. **Done (2026-08-03):** both free by construction (existing `canonical_digest`, the allowlist itself); caught and fixed a real bug in the process -- the allowlist's `policy_tags`/`pool_views` copy was only shallow, letting a caller mutate cached state through a nested object. Fixed with `copy.deepcopy`.
- [x] 3.3 Preserve backward compatibility for older producers omitting additive metadata. **Done (2026-08-03):** confirmed byte-identical output by test.
- [x] 3.4 Add producer/router/client/cache contract tests. **Done, corrected once by external review (2026-08-03):** two integration tests originally used a raw HTTP GET and a direct DB insert instead of the canonical `RemoteCapacityClient`/`ProvisioningClient`; rewritten to go through the real clients. 24 new unit tests, 8 new integration tests.
- [x] 3.5 Project configured VM size defaults (`default_vm_ram`/`vcpus`/`disk_size`) as a versioned `vm.ansible_pool_defaults.v1` entry inside `pool_views`. **Done (2026-08-03):** required adding real persistence first (these fields existed only on a pydantic model, unreachable via the admin API) -- schema migration, handler validation/wiring, and the shaping/composition layer all built and independently tested (37 new tests). Caught and fixed a real gap during the second review round: the view was gated only on an `AnsiblePoolConfig` row existing, not on the pool's `mechanism` actually being `"ansible"` -- not a live bug (the service already deletes stale config rows on provider change) but made structural rather than incidental.

### Section 3 closeout (per `openspec/README.md#plan-closeout-requirements`, "or a major section within one")

- [x] Comment hygiene: clean, re-verified via the real `make check-comment-hygiene` as part of Section 7's own repo-wide run. (A second external review round separately caught two test docstrings narrating change history that the mechanical grep can't detect -- both rewritten to state current behavior only.)
- [x] Documentation compliance: "Resource-pool projection metadata" promoted to `openspec/specs/site-capacity/spec.md` with a full evidence-block entry.
- [x] Narrative compression: done as part of Section 7 (this task list compressed 2026-08-06; full trail retained in `design.md`).
- [x] Promotion: done (see documentation compliance above).

Final regression sweep at the time: 420 passed (`provisioning/compute/service` unit), 171 passed (integration), 145 passed (`kit/site`). Zero failures.


## 4. Map projections into commercial inventory

The mapping schema already existed (`derived_compute_listings`/`derived_bare_metal_listings`, no commercial fields of their own -- pricing/settlement/policy live on `listings` via `listing_id`); this section extends it, not replaces it. Full design rationale, alternatives considered (the rejected `use_projection: bool` redesign), and the complete multi-round review trail live in `design.md`'s "Section 4 design" -- not duplicated here.

- [x] 4.1 Site-scoped derivation keys. `listing_resource_key`/`listing_pool_key` require and validate non-empty `site_id`; unmapped-listing fallback is dynamic (a listing's own mapping wins when present, `home_site` only when `configured_site_count == 1`, quarantined otherwise). Bare-metal confirmed already correctly site-scoped. **Done (2026-08-04):** found and fixed two real bugs -- `SQLiteClient` had an independently drifted copy of this table's schema (now delegates to `reconciler.py`'s own); `derivation_key`'s naive colon-delimited format wasn't collision-free for operator strings containing colons (fixed with a length-prefixed encoding). 35 new tests, 630 unit + 147 integration passing.
- [x] 4.2 Backfill/quarantine, revised from a migration-based approach to 4.1's own read-time fallback (a migration can't safely depend on live deployment config that changes after it runs). A legacy row not found is idempotent re-derivation, not data loss.
- [x] 4.3 Redirect `available_compute_slices`' structural capacity source to the site-resource-pool projection, opt-in (`site_pool_projection=None` default, byte-identical to prior behavior). **Done (2026-08-04):** `gpu_model` added end-to-end on the provisioning-service side; pricing/`region`/`sla` stay local-only (home-site pools only, avoiding the same collision class 4.1 fixed). Found and fixed two real bugs: `site_pool_projection()` collapsed "never loaded" (`None`) into "loaded, genuinely zero" (`[]`), silently falling back to stale local data on an authoritative zero; `_projected_resource_usage`'s condition order checked `member_availability is None` before the projection's own `available` field, discarding authoritative projection data. Both caught by a real orchestration integration test (`TestRealOrchestrationCacheToReconciliation`), not by inspection.
- [x] 4.4 Pinned-site `reserve()`. `AggregateCapacityClient.reserve()` gained `site: str | None = None`, dispatching to `_reserve_at_site`/`_reserve_by_placement`; applies to every listing with a known site mapping regardless of `listing_mode`. **Done (2026-08-04), corrected once by review:** the admin `reserve_capacity` endpoint was collapsing every pinned-site failure into the same status code a live refusal returns -- now distinguishes live refusal (409), unknown/stale site mapping (500), and mapped-site transport failure (502). 9 new tests total.
- [x] 4.5 Bare-metal parity. **Done (2026-08-04):** no `.reserve()` exists there yet (capacity reservation isn't built for bare-metal); found and fixed the same key-collision class 4.1 fixed for VM, also present in `bare_metal_listing_key`.
- [x] 4.6 Regression proof that projections never participate in live admission/dispatch. **Done (2026-08-04):** confirmed structurally (`aggregation.py` has zero references to any projection cache) and with a call-counting unit test plus a real end-to-end integration test proving a live refusal wins over a cached projection -- verified as a genuine trip-wire by injecting the actual regression and confirming the test failed first.
- [x] 4.7 Move the buyer-facing site-authority HTTP client from `core_storefront.capacity_remote.RemoteCapacityClient` into `kit/site-client` as `SiteCapacityClient` -- a package-layer violation exposed by 4.6's own new integration test (a composition root importing another role's package to verify its API). **Done (2026-08-04), two review rounds, one genuine blocker each round:** first round found `subscribe()`/`emit_local()`/the constructor's `bus` parameter were entirely dead code (traced every call site; none reachable), dropped rather than migrated; the split left `AggregateCapacityClient`'s `sites` parameter typed against a protocol `SiteCapacityClient` no longer satisfied, fixed by introducing a narrower `SiteCapacityAuthority` protocol. Second round found and fixed a fragile `tests.fake_site` import (no `__init__.py` in `kit/site-client/tests/`, matching every other `kit/*` package) and a third instance of history-narrating test docstrings the same sweep had already fixed two of. All import sites updated directly, no compatibility alias. Final regression after both rounds: `domains/vms/storefront` unit 738 + integration 152, `core/storefront` 92, `domains/bare_metal` 60, `domains/bare_metal/storefront` 44, `kit/site` 145, `kit/site-client` 9, `provisioning/compute/service` 603, `domains/apicredits/storefront` 51 -- all passing, every remaining failure matching the confirmed pre-existing/environment-only set.

**Fix notes (found during Section 4, not pre-planned):**
- `test_admin_api.py`'s entire `TestFulfillmentEvents` class called raw `_post(<route>, <dict>)` instead of the canonical typed `StorefrontClient` methods -- exactly the gap that stops a test from catching a route rename. Two new typed client methods added (`notify_usage_started`, `notify_fulfillment_failed`); all 5+2+5 call sites converted. 38/38 passing, no behavior change.
- **A correction to this whole document's own reported regression baselines, not just a new fix.** Every prior "matches the confirmed pre-existing/environment-only set" claim had been checked in a sandbox that never did a real `pip install -e .` -- only manual `PYTHONPATH` strings, which mask entry-point/console-script resolution failures (resolved from installed package *metadata*, not `sys.path` membership). Properly installing every touched package revealed the long-carried "16 confirmed pre-existing failures" for `test_cli_publish_helpers.py` were mostly masking artifacts (14 of 16) plus two real, previously-invisible bugs: two tests asserted against the pre-length-prefix `derivation_key` format, never updated when 4.1 landed (fixed to compute expected keys dynamically); a bare-metal health-response test asserted exact equality against a payload that predated the `site_projections` field. Corrected, verified final count: 1691 passed, 6 remaining failures each individually traced to a specific confirmed-unrelated cause (2 native-toolchain gap, 2 FastAPI/Starlette version mismatch, 1 unrelated negotiation-logic defect, 1 separate bare-metal/storefront packaging gap).
- A missing `arkhai-core-storefront` packaging dependency for `provisioning/compute/service` (surfaced by the same properly-installed environment) was fixed twice: first added to production dependencies (wrong -- the service has zero runtime references to it, only one test file does), corrected to a dev-only dependency; then corrected again to drop a disallowed relative `[tool.uv.sources]` path pattern entirely (an already-planned separate change, `remove-relative-uv-sources`, exists for exactly this anti-pattern) in favor of the wheel-based resolution this file's other dependencies already use. Whether `core_storefront.capacity`/`capacity_remote.py` should eventually move into `kit/` (zero internal dependencies, already leaking into `kit/site-client`) was investigated concretely and left as a deliberate future decision, not undertaken here -- the blast radius includes this section's own most central files (4.3, 4.4).

### Section 4 closeout (per `openspec/README.md#plan-closeout-requirements`, "or a major section within one")

- [x] Comment hygiene: clean across every file this section touches, re-verified via the real `make check-comment-hygiene` as part of Section 7's own repo-wide run.
- [x] Documentation compliance: re-checked against `openspec/README.md`'s placement rules directly.
- [x] Narrative compression: done as part of Section 7 (this task list compressed 2026-08-06; full trail retained in `design.md`).
- [x] Promotion: completed 2026-08-04. `openspec/specs/storefront-publication/spec.md` gained "Commercial mapping identity" and "Site-pinned claim routing"; `openspec/specs/site-capacity/spec.md` gained two new scenarios and a paragraph. Found and fixed a real anchor-mismatch bug while promoting: the table's own anchor references didn't match the actual requirement-title-derived anchors OpenSpec generates -- corrected throughout.
- [x] One real orchestration integration test for cache -> reconciliation, built during this closeout (`TestRealOrchestrationCacheToReconciliation`) -- caught the `_projected_resource_usage` condition-order bug (4.3) on first run, plus two test-environment issues, all fixed rather than worked around.



## 5. Implement advisory hints

`listing_mode` formalizes a fungible-vs-specific-resource publication decision `available_compute_slices` already made implicitly from local table membership -- also corrects Section 1's "no confirmed reader" classification of `compute_capacity_pools`/`compute_pool_members`. Full design rationale, the Q1-Q4 investigation trail, the scope corrections (5.2's real size, 5.1 needing two write surfaces), and the complete external-review trail (the backward-compatibility bug, the bare-metal-resolver reversal, the sandbox fixes that let a previously-uncollectable test run) live in `design.md`'s "Section 5 design" and "External review round" -- not duplicated here.

- [x] 5.1 Define `listing_mode`/`max_reservation_hold_seconds` key constants, resolvers, and validation in `kit/resource-pools/hints.py`, wired into both write surfaces (bulk YAML and the individual pool admin API, which had zero `policy_tags` content validation before this). **Done (2026-08-04):** shared `_require_valid_hold_preference` service method; 61 tests (kit/resource-pools) including a real-HTTP-API round trip added after review found the service-level proof alone insufficient.
- [x] 5.2 Add VM and bare-metal listing-mode resolvers, wired into real candidate generation -- split into 5.2a-5.2d once the original single-task scope proved undersized:
  - [x] 5.2a Thread `policy_tags` into `resolve_vm_listing_mode` (new, `domains/vms/listings/`), with an operator-visible explanation for unrecognized values. **Done, corrected twice by external review (2026-08-04/05):** bare metal's resolver was built then removed as unwired dead code; the explanation is now actually surfaced on the operator status surface (`listing_mode_explanations()`); a blocking backward-compatibility bug (the structural default was hardcoded to `"fungible"` instead of depending on member count) was found and fixed once a sandbox fix let a previously-uncollectable integration test finally run.
  - [x] 5.2b Move fungible-mode candidate generation onto `site_capacity_buckets` (a real, unclaimed POOLS-7 design gap). **Done, one blocking bug found and fixed by external review:** `_fungible_availability_from_buckets` conflated "family never loaded" with "family loaded, no matching entries" -- both fell back to a stale resource-list walk instead of trusting a loaded family's authoritative zero. Fixed at both the site and function level; a wire-contract test for `SiteCapacityClient.capacity_bucket_projection()` added.
  - [x] 5.2c Support `specific_resource` on multi-member pools -- one row per enabled member instead of the `member_count == 1` vestige. **Done (2026-08-04):** the main publish loop needed zero changes, confirmed by test.
  - [x] 5.2d Fix `record_derived_listing`'s `use_pool_key` collision bug, required before 5.2c can be trusted -- every specific-resource listing was deriving its key from `pool_id` alone regardless of `resource_id`, harmless only because `member_count == 1` meant one resource per pool. **Done (2026-08-04):** fixed to `pool_id is not None and resource_id is None`.
- [x] 5.3 Add API-credit hint interpretation only if a concrete consumer exists. **Resolved by investigation: defer.** `domains/apicredits` has no `ResourcePool`/`policy_tags` concept at all.
- [x] 5.4 Cap storefront acceptance-hold TTL by a valid projected preference, VM-only (bare metal has no capacity-reservation flow at all yet -- that gap belongs to `market-platform-bare-metal-10-storefront-composition`, updated with this section's `reserve(site=...)` mechanism as a dependency rather than duplicating it here). **Done (2026-08-04):** two-step lookup (`pool_id_for_listing`, `lookup_pool_policy_tags`) reading `policy_tags` live from the in-memory projection cache, never persisted. Found and fixed a real bug during test-writing (an over-clever "genuine vs. backfilled pool_id" distinction that broke ordinary fungible listings); simplified to match `site_id_for_listing`'s own pattern. An orchestration gap (nothing proved the capped value actually reached `reserve()`) was closed by external review.
- [x] 5.5 Add focused tests for all of the above. **Done, expanded twice by external review (2026-08-04/05):** every originally-scoped case covered, plus the loaded-empty capacity-bucket regression, the operator-visible-explanation status surface, the hold-cap orchestration proof, and two new provisioning-service integration tests. Final regression: **1830 passing across every touched package, 3 pre-existing/unrelated failures, zero regressions** -- a materially cleaner count than earlier in this section's own history, because sandbox-environment fixes let several previously-uncollectable test files run for the first time, one of which caught the real backward-compatibility bug above.

### Section 5 closeout (per `openspec/README.md#plan-closeout-requirements`, "or a major section within one")

- [x] Comment hygiene: clean, re-verified via the real `make check-comment-hygiene` as part of Section 7's own repo-wide run. (Two earlier sweeps this section did on its own each caught real gaps the previous one missed -- literal-string-only grep missing paraphrased provenance language, then production-files-only scope missing the same pattern in tests -- both fully resolved before this final check.)
- [x] Import placement: every new local import in this section's changed functions matches this codebase's own established local-import convention (keeping `market_resource_pools` out of the buyer distribution's import chain); not a new pattern.
- [x] Documentation compliance: re-checked against `openspec/README.md`'s placement rules directly.
- [x] Narrative compression: done as part of Section 7 (this task list compressed 2026-08-06; full trail retained in `design.md`).
- [x] Promotion: completed 2026-08-04, corrected twice by external review (a premature-promotion timing issue matching Section 2's own earlier case, and a stale evidence-line reference to a file later tombstoned) -- both resolved, not re-promoted from scratch.

## 6. Retire superseded local physical authority

Retirement is column-level, not table-level, for `resources`/`compute_capacity_pools` (both mix physical-identity columns with commercial columns that must survive). Re-grounding against the exact current code (2026-08-05) found the original sketch's scope was wrong in five ways -- most materially, that `region`/`sla` have no projection equivalent and so aren't retirement candidates at all, and that flipping the flag's default and deleting the code it gates in the same change would collapse the staged-rollout safety margin to zero. Full findings, the region/SLA/pricing hint design, and the two-change split rationale live in `design.md`'s "Section 6 design," "Region, SLA, and pricing move to resource-pool hints too," and "Splitting Section 6 across two changes."

**This change's own scope narrowed to: flip the default (6.0), and build the region/SLA/pricing hint mechanism (6.1-6.3) -- all new capability, not retirement.** Everything that actually deletes code (the local-table path, CSV import, `hosts`/`compute_pool_members`, the flag itself) moves to a separately-proposed follow-up change with no assumed trigger condition this document defines (task 6.4).

- [x] 6.0 Flip `use_site_projection_for_listings` to default `true`. **Done (2026-08-05):** flipped in `settings.toml`; confirmed every real caller already reads it unconditionally; both-direction tests added (`test_config_loader.py`).
- [x] 6.1 Region and SLA move to resource-pool hints. New domain-neutral keys in `kit/resource-pools/hints.py` (`raw_region`, `sla_value` -- SLA validated since it has a domain-neutral number shape region doesn't); region has no override tier (a fact, not a promise), SLA gets the same three-tier treatment as pricing. **Done (2026-08-05):** `domains/vms/listings/pool_descriptors.py` (new) holds the VM resolvers, wired through a new `PoolHintResolutionSettings` dataclass; `validate_sla_preference` wired into every write path `max_reservation_hold_seconds` already uses.
- [x] 6.1a SLA needs a storefront-wide trust gate, separate from the override chain -- some storefronts should never publish a site's self-reported SLA claim, independent of any specific pool's own override (direct product guidance). **Done (2026-08-05):** `[pricing].accept_pool_declared_sla`, defaults `false` (new trust-sensitive capability, not a migration of something already trusted).
- [x] 6.2 Pricing gets a three-tier precedence (storefront override -> pool hint -> config default), structured to mirror `structured-capacity-requirements`'s family-grouped shape for the `gpu` family (a real cross-change dependency for extending beyond `gpu`, not assumed away). **Done (2026-08-05), one scope simplification found during implementation:** `domains/vms/listings/pricing_resolution.py` (new) resolves each pricing field independently across all three tiers; the tier-3 admin endpoint originally scoped was not built, since CSV import (still present, deferred to 6.4) already provides that write path through this change's own scope -- a real future enhancement, not blocking here.
- [x] 6.3 Add focused tests for the new hint mechanism. **Done (2026-08-05):** ~65 new tests across `kit/resource-pools`, `domains/vms/storefront`. Regression at the time: 1741 passing, zero regressions.
- [ ] 6.4 Deferred to a follow-up change with no assumed trigger condition this document defines (this product has no fleet-wide deployment signal to gate on -- sellers self-host and self-operate). Scope recorded so it isn't reinvented from scratch: freeze writes to the retiring physical columns and delete the local-table code path entirely; delete `use_site_projection_for_listings`; remove CSV import and `upsert_resource`; retire `hosts`/`compute_pool_members`/`resources`' remaining columns; migrate the six CSV-dependent e2e scenario files; add the freeze-then-redirect migration and rollback documentation.

### External review round 2 (2026-08-05/06)

A second external review found four things worth acting on and one worth explicitly declining, all verified directly against the code (not taken on faith either way) before acting. Full detail in `design.md`'s own record of this round.

- **A real regression I shipped, found while answering a direct import-placement question, not by the review itself.** An eagerly-constructed module-level default (`_DEFAULT_POOL_HINT_RESOLUTION_SETTINGS`) defeated the buyer-dependency-avoidance strategy Section 5's review had already fixed once -- confirmed with a real `ModuleNotFoundError` importing `domains.vms.listings` from the buyer's own path. Fixed at the root (pushed `market_resource_pools` imports into the specific functions that need them across three sibling modules); added `domains/vms/buyer/tests/test_no_resource_pools_dependency.py` as a permanent regression guard.
- **Blocking, confirmed real: `_projected_pool_rows` required a local `compute_capacity_pools` row to exist before three-tier resolution could even start**, killing every non-home-site pool and any home-site pool without a local row before hints or config defaults were ever consulted -- directly contradicting the promoted fallthrough precedence. Fixed; 5 existing tests that had encoded the bug as correct behavior rewritten, new tests added for the exact scenarios the review named.
- **Real robustness gap, confirmed: `resolve_gpu_pricing` accepted any hint field value with no shape check.** Fixed with per-field type validation; a malformed field now falls through to the next tier like a missing one, without blocking its valid siblings.
- **Confirmed not a bug, after the review's own re-verification: `capacity_client.py`'s reconciliation path not threading `PoolHintResolutionSettings` through.** Traced precisely -- that path only toggles status on existing listings by key, never constructs commercial data. Declined to add the threading anyway as defensive future-proofing (direct product guidance, since it would obscure the real invariant rather than protect it); added an explicit comment plus a test proving the invariant instead. The review's own noted "code smell" (discarded commercial-field computation on that path) was investigated directly and confirmed real but minor -- bounded by pool/member count, not capacity size -- and recorded as a legitimate future cleanup, not fixed now.
- **Documentation fixes:** the permanent spec's hold/SLA validation wording corrected to be precise (integer vs. number); this section's own narrative-compression checkbox, checked `[x]` while its text said "not performed" -- the exact bookkeeping error already fixed once for Section 5, reintroduced here.

Final regression after this round: 1755 passing, zero regressions.

### Section 6 closeout (per `openspec/README.md#plan-closeout-requirements`, "or a major section within one")

- [x] Comment hygiene: clean, re-verified via the real `make check-comment-hygiene` as part of Section 7's own repo-wide run. (This section's own closeout swept proactively from the start, catching six real violations across production and test files before a review needed to.)
- [x] Import placement: every new local import in this section's code follows the exact established rationale Section 5 already set; not a new pattern.
- [x] Documentation compliance: re-checked against `openspec/README.md`'s placement rules directly.
- [x] Narrative compression: done as part of Section 7 (this task list compressed 2026-08-06; full trail retained in `design.md`).
- [x] Promotion: `openspec/specs/resource-pool-management/spec.md` and `openspec/specs/storefront-publication/spec.md` both updated with the region/SLA/pricing three-tier precedence, the trust gate, and per-field independent resolution. Evidence lines updated in both, naming only files confirmed to exist at promotion time.

## 7. Plan closeout (per `openspec/README.md#plan-closeout-requirements`)

**Grounded (2026-08-03):** this section was previously scoped as promotion-only (4 tasks), but `openspec/README.md`'s closeout requirement has four parts, of which promotion is only the last. Comment hygiene, documentation compliance, and narrative compression were missing as explicit tasks. Also found: this document's own promotion table used the heading "Permanent Documentation Promotion" instead of the README's specified "Design promotion record" and a `Decision | Permanent destination` column shape instead of `Accepted decision | Permanent location` with anchor-style destinations — corrected in `design.md` as part of this section's own scope, not left for implementation to notice.

- [x] 7.1 **Comment hygiene.** Run `make check-comment-hygiene` and resolve every match before this task is marked done. Note this only catches change IDs/section/task numbers mechanically — separately, direct-read every file this change touches for `AGENTS.md`'s fuzzier "no migration/review provenance in comments" rule, which the tool doesn't catch (e.g. don't leave "added for POOLS-8" or "per Section 4" style comments in production code even if `POOLS-8`/`Section 4` as literal strings don't appear). **Done (2026-08-06):** ran the real tool for the first time this session (not the manual greps every earlier section relied on) -- found two genuine violations in test docstrings from very recent work (both fixed), confirming manual sweeps alone hadn't been sufficient. Re-ran clean across the entire repository, not just this change's own files.
- [x] 7.2 **Documentation compliance.** Re-check every accepted decision in `design.md` against `openspec/README.md`'s placement rules directly (subsystem spec vs. `ARCHITECTURE.md` vs. no permanent change) rather than deferring to review. Confirm the "Design promotion record" table (below) is still complete against the final implementation — decisions can shift during implementation in ways this design-phase table can't anticipate. **Done (2026-08-06):** see 7.4's own audit findings below -- four stale/nonexistent-anchor promotion-table rows found and corrected in the process.
- [x] 7.3 **Narrative compression.** Shorten tasks 1–6's completed-task notes to final behavior, material validation evidence, unresolved/deferred work, and permanent-documentation destinations — move detailed alternatives, corrected-finding narratives, and review rationale (e.g. Section 2's struck-through original decision, Section 5's Q1–Q4 investigation trail) into `design.md` first if not already there. This deletes duplication between `tasks.md` and `design.md`, not information. **Done (2026-08-06):** confirmed `design.md` already carried the full rationale for every section (a dedicated "Section N design" subsection each) before trimming; this document went from 415 lines to roughly 160. One real mistake caught and fixed during the pass: a large mechanical edit to Section 4 accidentally deleted the "## 5. Implement advisory hints" heading line itself (its body survived, just orphaned under Section 4's own header) -- caught immediately by re-checking the section-heading list after every edit, not assumed correct from the diff alone; restored before moving on. Every remaining `[ ]`/`[x]`-mismatched narrative-compression checkbox from Sections 5 and 6 (both correctly deferred pending exactly this work) updated to reflect it's now actually done.
- [x] 7.4 **Promotion.** Complete the design-promotion record (`design.md`, "Design promotion record" — already rebuilt as a full audit during this design-review pass, not just the four rows carried forward from early sections). Concretely:
  - [x] 7.4.1 Promote projection load-state visibility and resource-pool projection metadata (`pool_views`, allowlisted fields) to `openspec/specs/site-capacity/spec.md` and `architecture.md`. **Done, confirmed during Section 7's own audit (2026-08-06):** already promoted incrementally through Sections 2-4 (see the promotion table's own dated rows); `architecture.md` confirmed not needing a change (7.4.4).
  - [x] 7.4.2 Promote commercial mapping (`derived_compute_listings`/`derived_bare_metal_listings` extension), direct claim routing (`reserve(site=...)`), and the projection-becomes-default-over-a-retained-local-path decision to `openspec/specs/storefront-publication/spec.md`. **Corrected during Section 7's own audit (2026-08-06):** the original wording named "the local-physical-authority retirement boundary" as something this change promotes -- wrong, since Section 6 re-scoped the actual retirement out of this change entirely (task 6.4). What this change genuinely owns and promotes instead: that the projection path becomes the storefront's *default* once it has parity with a local-table path it still retains (not the retirement of that path, which is the deferred follow-up change's own promotion to make). Found and fixed a real gap while auditing this: four promotion-table rows pointed at `#local-physical-authority`, a heading that was never actually created -- two were re-scoped-away content moved out of this change's promotion record entirely (with a pointer to task 6.4 instead), one was struck as superseded by later, more specific rows, and one (the projection-defaults-on decision) got a real destination: `openspec/specs/storefront-publication/spec.md#storefronts-cache-independent-site-projections`, extended with a new scenario for exactly this case. Also removed two early promotion-table rows that had gone stale (pointing at pre-rename anchors `#listing-and-hold-hints`/`#listing-mode`/`#publication-candidates` that no longer exist, duplicating content later rows already promote correctly under the current anchors).
  - [x] 7.4.3 Promote domain-neutral hint keys, domain-owned values, and pool-identity site-scoping to `openspec/specs/resource-pool-management/spec.md` and `architecture.md`. **Done, confirmed during Section 7's own audit (2026-08-06):** already promoted incrementally through Sections 1, 5, and 6 (see the promotion table's own dated rows, including the SLA/pricing/region extension and the hold-vs-SLA validation wording fix from Section 6's review round).
  - [x] 7.4.4 Update `docs/development/ARCHITECTURE.md` only if the accepted design changes repository-wide authority/topology. **Done, confirmed not needed (2026-08-06):** re-checked directly against the file's current text -- "The storefront owns capacity offerings and projections used to publish and negotiate listings. It is not the source of truth for physical resources. A projection may be stale; authoritative admission occurs at the site authority" already states the principle this entire change executes toward, at the correct level of generality. `pool_views`, `listing_mode`, and the hint precedence tiers are correctly spec-level detail, not architecture-doc material.
  - [x] 7.4.5 Record every promoted decision's final destination in `design.md`'s "Design promotion record," run strict `openspec` validation for this change and every permanent spec it touches, and archive only after current-state documentation matches verified (not just designed) behavior. **Done (2026-08-06):** `npx @fission-ai/openspec@latest validate --all --strict` run for real, not assumed -- `change/pools-8-capacity-projection-and-listing-hints`, `spec/resource-pool-management`, `spec/storefront-publication`, and `spec/site-capacity` each validate clean individually. Archival itself (per `openspec/README.md`'s own process) is a separate, later action once every task in this change is genuinely complete, not part of this task.
