# Implementation plan

- [x] Read issue, reviewed carrier/file plan, permanent contracts and engineering guidance; preserve historical completed tasks. Confirm clean state and inherit reviewed prerequisites; base recorded in external report.
- [x] Build internal wheels and initialize independent locked seller/registry environments without editable siblings.
- [x] Extend `domains/bare_metal/storefront/src/arkhai_bare_metal_storefront/contact_offers.py` with bounded general loading, whole-file profile/privacy/schema/conflict preflight, strict intent composition and shared stable upsert sequencing. Reuse existing SQLite admission; no schema changes.
- [x] Add explicit command in `domains/bare_metal/storefront/src/arkhai_bare_metal_storefront/cli.py` and opt-in/conflict startup wiring in `server.py`.
- [x] Add six disposable TEST declarations in `domains/bare_metal/storefront/examples/contact-declarations.json` and package them through `domains/bare_metal/storefront/pyproject.toml`.
- [x] Extend `domains/bare_metal/storefront/tests/test_contact_publication.py` with actual installed CLI, signed registry, whole-file negatives, privacy, restart and partial-effect evidence. Preserve existing fixture/physical suites and default buyer refusal; compatible acceptance uses an explicit test-owned validating adapter.
- [x] Rebuild/reinstall final storefront wheel; complete installed-wheel suites pass: 270 storefront, 126 domain, 170 contact. Source/wheel/installed parity covers 56 files, including both examples. Scoped typing retains the unchanged `ContactDeliveryOffers.schema_version` literal-override error; repository OpenSpec validation reports 73 passed and 12 unrelated failures. The scoped change passes strict validation.
- [x] Prepare exact permanent documentation proposals for publication spec/architecture, configuration, roadmap and campaign index outside the repository. Correct startup guidance to require neither file path for no publication. Verify all five proposed documents apply and all four added relative links, including heading anchors, resolve.
- [x] Run candidate comment hygiene and whitespace checks; inspect touched imports (all additions are module-level). Reconcile completed-task evidence and documentation placement; preserve pending promotion and existing capability ownership.
- [x] Closeout after independent review: comment hygiene and whitespace checks pass; touched imports remain module-level with unchanged dependency direction. Promote reviewed behavior/rationale/configuration, reconcile current-state roadmap and campaign status, and retain existing capability links and repository-wide authority map. Changed-document relative paths resolve and source remains identical to the reviewed/tested candidate. Scoped change and permanent publication specification validate strictly. Detailed command receipts stay external; installed-wheel tests/parity and disclosed baseline failures above remain the behavioral evidence.
