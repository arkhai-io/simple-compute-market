## MODIFIED Requirements

### Requirement: Multi-domain capacity proof
A shared site authority routes allocation jobs and deal events for more than one executor and storefront domain. The implementation MUST preserve the ownership and compatibility constraints in the `site-capacity` baseline specification.

#### Scenario: Change acceptance
- **WHEN** the implementation is complete and its focused verification runs
- **THEN** a shared site authority routes allocation jobs and deal events for more than one executor and storefront domain.

<!-- Provenance: migrated from docs/development/TODO.md and linked ad hoc design notes. -->
