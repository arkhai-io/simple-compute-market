# API-tokens market on the local dev chain — standalone bring-up.
#   docker compose -f compose.apitokens.yml up
# Just the shared Anvil chain + the API-tokens domain stack, with no VM
# services. See domains/apitokens/compose.yml.
include:
  - compose.dev.yml
  - domains/apitokens/compose.yml
