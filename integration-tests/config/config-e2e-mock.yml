# ---------------------------------------------------------------------------
# Profile: e2e-mock
# Extends config-local.yml with agent/registry URLs and DB paths for
# docker-compose e2e mock provisioning tests.
# ---------------------------------------------------------------------------

agents:
  registry_url: "http://localhost:8080"
  buyer_url: "http://localhost:8000"
  seller_url: "http://localhost:8001"
  buyer_db: "src/market_storefront/data/buy-agent/agent.db"
  seller_db: "src/market_storefront/data/sell-agent/agent.db"
  buyer_env: "storefront/.env.alice.docker-compose"
  seller_env: "storefront/.env.bob.docker-compose"

tests:
  e2e_poll_timeout_s: 120
  e2e_poll_interval_s: 3
