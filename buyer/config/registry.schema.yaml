component: registry
schema_version: 1
fields:
  PORT:
    required: true
    default: 8080
    description: "Registry Indexer HTTP port."
  RPC_URL:
    required: true
    default: "http://localhost:8545"
    description: "Chain RPC endpoint."
