# DEPRECATED — kept for backward compat. Import from service.clients.erc8004.registration directly.
from service.clients.erc8004.registration import (  # noqa: F401
    build_agent_card_url,
    build_registration_file_url,
    register_onchain_from_env,
    register_onchain,
    update_existing_agent,
)
