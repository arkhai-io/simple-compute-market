# .secrets.toml
# ---------------------------------------------------------------------------
# Sensitive overrides — DO NOT COMMIT real values.
# This file is listed in .gitignore.  In CI/CD the equivalent values are
# injected via Kubernetes secrets or Vault into config-<profile>.yml files
# that are mounted into CONFIG_DIRECTORY at runtime.
# ---------------------------------------------------------------------------

[default.buyer]
private_key = ""

[default.seller]
private_key = ""
